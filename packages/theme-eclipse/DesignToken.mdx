import { Meta, Unstyled } from '@storybook/blocks'

<Meta title="Intro/디자인토큰" />

<Unstyled>
  <div style={{
    maxWidth: '900px',
    margin: '0 auto',
    fontFamily: '"Pretendard Variable", "Pretendard", -apple-system, BlinkMacSystemFont, system-ui, sans-serif',
  }}>

    {/* Header */}
    <div style={{ marginBottom: '48px' }}>
      <div style={{ fontSize: '12px', fontWeight: '700', color: '#6366f1', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Design Tokens</div>
      <h1 style={{ fontSize: '36px', fontWeight: '800', color: '#1e293b', letterSpacing: '-0.03em', margin: '0 0 12px' }}>디자인 토큰</h1>
      <p style={{ fontSize: '16px', color: '#64748b', lineHeight: '1.6', margin: 0 }}>
        Eclipse 테마의 디자인 토큰은 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '14px', fontWeight: '500' }}>@heejun-com/theme-eclipse/token</code>에서 사용할 수 있습니다.
      </p>
    </div>

    {/* Token Hierarchy Visual */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Token Hierarchy</h2>

      <div style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(3, 1fr)',
        gap: '0',
        borderRadius: '16px',
        overflow: 'hidden',
        border: '1px solid #e2e8f0',
      }}>
        {[
          {
            level: 'Reference Token',
            label: '원시 값',
            example: 'colorBlue500',
            value: '#3b82f6',
            gradient: 'linear-gradient(135deg, #3b82f6, #6366f1)',
            desc: '변하지 않는 원시 디자인 값',
          },
          {
            level: 'Semantic Token',
            label: '의미 기반',
            example: 'fillPrimary',
            value: 'var(--ref-blue-500)',
            gradient: 'linear-gradient(135deg, #6366f1, #8b5cf6)',
            desc: '맥락에 따라 매핑되는 의미 토큰',
          },
          {
            level: 'Component Token',
            label: '컴포넌트 전용',
            example: 'buttonEnabledFill',
            value: 'var(--sem-fill-primary)',
            gradient: 'linear-gradient(135deg, #8b5cf6, #a855f7)',
            desc: '특정 컴포넌트에 바인딩된 토큰',
          },
        ].map((token, i) => (
          <div key={i} style={{
            padding: '28px 24px',
            background: '#ffffff',
            borderRight: i < 2 ? '1px solid #e2e8f0' : 'none',
            position: 'relative',
          }}>
            <div style={{
              position: 'absolute',
              top: 0,
              left: 0,
              right: 0,
              height: '3px',
              background: token.gradient,
            }} />
            <div style={{ fontSize: '11px', fontWeight: '600', color: '#94a3b8', textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: '4px' }}>{token.label}</div>
            <div style={{ fontSize: '16px', fontWeight: '700', color: '#1e293b', marginBottom: '8px' }}>{token.level}</div>
            <div style={{
              padding: '8px 12px',
              background: '#f8fafc',
              borderRadius: '8px',
              marginBottom: '12px',
              fontFamily: '"JetBrains Mono", monospace',
              fontSize: '12px',
              color: '#6366f1',
              fontWeight: '500',
            }}>{token.example}</div>
            <div style={{ fontSize: '12px', color: '#64748b', lineHeight: '1.5' }}>{token.desc}</div>
            {i < 2 && (
              <div style={{
                position: 'absolute',
                right: '-10px',
                top: '50%',
                transform: 'translateY(-50%)',
                width: '20px',
                height: '20px',
                background: '#ffffff',
                border: '1px solid #e2e8f0',
                borderRadius: '50%',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontSize: '10px',
                color: '#94a3b8',
                zIndex: 1,
              }}>{'\u2192'}</div>
            )}
          </div>
        ))}
      </div>
    </section>

    {/* Usage */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Usage</h2>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px' }}>
        {[
          {
            title: 'Inline Style',
            code: `import { vars } from '@heejun-com/theme-eclipse/token'

const Component = () => (
  <div style={{
    color: vars.sem.color.fg_brand_primary,
    background: vars.sem.color.bg_primary,
    padding: vars.sem.spacing.md,
  }}>
    토큰 기반 스타일링
  </div>
)`,
          },
          {
            title: 'CSS-in-JS',
            code: `import { vars } from '@heejun-com/theme-eclipse/token'
import styled from 'styled-components'

const Card = styled.div\`
  color: \${vars.sem.color.fg_brand_primary};
  background: \${vars.sem.color.bg_primary};
  border: 1px solid \${vars.sem.color.border_primary};
  border-radius: \${vars.sem.radius.md};
\``,
          },
        ].map((example, i) => (
          <div key={i} style={{
            borderRadius: '12px',
            border: '1px solid #e2e8f0',
            overflow: 'hidden',
          }}>
            <div style={{
              padding: '8px 16px',
              background: '#f8fafc',
              borderBottom: '1px solid #e2e8f0',
              display: 'flex',
              alignItems: 'center',
              gap: '6px',
            }}>
              <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#f87171' }} />
              <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#fbbf24' }} />
              <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#34d399' }} />
              <span style={{ marginLeft: '8px', fontSize: '11px', color: '#94a3b8', fontWeight: '500' }}>{example.title}</span>
            </div>
            <pre style={{
              margin: 0,
              padding: '16px',
              background: '#0f172a',
              color: '#e2e8f0',
              fontSize: '12px',
              lineHeight: '1.7',
              fontFamily: '"JetBrains Mono", monospace',
              overflow: 'auto',
            }}>{example.code}</pre>
          </div>
        ))}
      </div>
    </section>

    {/* Token Categories */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Token Categories</h2>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '12px' }}>
        {[
          {
            name: 'Color',
            desc: 'Foreground, Background, Border, Fill 컬러 토큰',
            tokens: ['fg_brand_primary', 'bg_primary', 'border_primary', 'fill_primary'],
            color: '#6366f1',
          },
          {
            name: 'Typography',
            desc: 'Font size, weight, line-height, letter-spacing 토큰',
            tokens: ['font_size_h1', 'font_weight_bold', 'line_height_body'],
            color: '#8b5cf6',
          },
          {
            name: 'Spacing',
            desc: 'Padding, margin, gap 간격 토큰 (8px grid)',
            tokens: ['spacing_xs', 'spacing_sm', 'spacing_md', 'spacing_lg'],
            color: '#3b82f6',
          },
          {
            name: 'Radius & Shadow',
            desc: 'Border-radius와 elevation 그림자 토큰',
            tokens: ['radius_sm', 'radius_md', 'shadow_sm', 'shadow_lg'],
            color: '#10b981',
          },
        ].map((cat, i) => (
          <div key={i} style={{
            padding: '20px',
            borderRadius: '12px',
            border: '1px solid #f1f5f9',
          }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '8px' }}>
              <div style={{ width: '8px', height: '8px', borderRadius: '3px', background: cat.color }} />
              <h3 style={{ fontSize: '15px', fontWeight: '700', color: '#1e293b', margin: 0 }}>{cat.name}</h3>
            </div>
            <p style={{ fontSize: '13px', color: '#64748b', margin: '0 0 12px', lineHeight: '1.5' }}>{cat.desc}</p>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: '4px' }}>
              {cat.tokens.map((t, j) => (
                <code key={j} style={{
                  padding: '3px 8px',
                  borderRadius: '4px',
                  background: '#f8fafc',
                  fontSize: '11px',
                  color: '#64748b',
                  fontFamily: '"JetBrains Mono", monospace',
                  border: '1px solid #f1f5f9',
                }}>{t}</code>
              ))}
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* Material 3 Color Role System Comparison */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>
        Material 3 색상 역할 시스템과 비교
      </h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.6', marginBottom: '24px' }}>
        Google Material 3는 색상을 역할(role) 기반으로 체계화합니다. Orbit UI의 Semantic Token과 어떻게 대응되는지 비교합니다.
      </p>

      <div style={{ overflowX: 'auto' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '13px' }}>
          <thead>
            <tr style={{ background: '#f8fafc' }}>
              <th style={{ padding: '10px 16px', textAlign: 'left', borderBottom: '2px solid #e2e8f0', color: '#64748b', fontWeight: '600' }}>
                Material 3 역할
              </th>
              <th style={{ padding: '10px 16px', textAlign: 'left', borderBottom: '2px solid #e2e8f0', color: '#64748b', fontWeight: '600' }}>
                설명
              </th>
              <th style={{ padding: '10px 16px', textAlign: 'left', borderBottom: '2px solid #e2e8f0', color: '#6366f1', fontWeight: '600' }}>
                Orbit UI Semantic Token
              </th>
            </tr>
          </thead>
          <tbody>
            {[
              { m3: 'Primary', desc: '핵심 인터랙티브 컬러', orbit: 'color.fillPrimary / color.foregroundBrand' },
              { m3: 'On Primary', desc: 'Primary 위에 표시되는 텍스트/아이콘', orbit: 'color.foregroundOnFill' },
              { m3: 'Primary Container', desc: 'Primary 배경 컨테이너', orbit: 'color.backgroundBrandSubtle' },
              { m3: 'Surface', desc: '컴포넌트 기본 배경', orbit: 'color.backgroundPrimary' },
              { m3: 'Surface Container', desc: '카드, 모달 등 컨테이너 배경', orbit: 'color.surfaceContainer' },
              { m3: 'Surface Container High', desc: '높은 중요도 컨테이너', orbit: 'color.surfaceContainerHigh' },
              { m3: 'On Surface', desc: '표면 위 기본 텍스트', orbit: 'color.foregroundPrimary' },
              { m3: 'On Surface Variant', desc: '표면 위 보조 텍스트', orbit: 'color.foregroundSecondary' },
              { m3: 'Outline', desc: '컨테이너 테두리', orbit: 'color.borderPrimary' },
              { m3: 'Outline Variant', desc: '구분선, 보조 테두리', orbit: 'color.separatorPrimary' },
              { m3: 'Error', desc: '오류 상태 색상', orbit: 'color.fillDanger / color.foregroundDanger' },
            ].map((row, i) => (
              <tr key={i} style={{ borderBottom: '1px solid #f1f5f9' }}>
                <td style={{ padding: '10px 16px', fontWeight: '600', color: '#1e293b', fontFamily: '"JetBrains Mono", monospace', fontSize: '12px' }}>
                  {row.m3}
                </td>
                <td style={{ padding: '10px 16px', color: '#64748b', lineHeight: '1.5' }}>
                  {row.desc}
                </td>
                <td style={{ padding: '10px 16px' }}>
                  <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 8px', borderRadius: '4px', fontSize: '11px', fontFamily: '"JetBrains Mono", monospace', fontWeight: '500' }}>
                    {row.orbit}
                  </code>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <div style={{
        marginTop: '16px',
        padding: '12px 16px',
        borderRadius: '8px',
        background: '#f8fafc',
        border: '1px solid #e2e8f0',
      }}>
        <p style={{ fontSize: '13px', color: '#64748b', margin: 0, lineHeight: '1.6' }}>
          <strong style={{ color: '#1e293b' }}>핵심 차이점:</strong> Material 3는 색상을 동적 색상 팔레트(Dynamic Color)로 생성하지만, Orbit UI의 Eclipse 테마는 고정된 CSS Variable로 브랜드 일관성을 유지합니다. 두 시스템 모두 Surface/Container 계층을 통해 시각적 깊이(elevation)를 표현합니다.
        </p>
      </div>
    </section>

    {/* Brand Color Override */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>
        커스텀 브랜드 색상 적용
      </h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.6', marginBottom: '20px' }}>
        CSS Variable을 재정의하여 브랜드 색상을 적용할 수 있습니다. <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px' }}>EclipseProvider</code> 하위에서만 동작합니다.
      </p>

      <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
        <div style={{ padding: '8px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', gap: '6px' }}>
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#f87171' }} />
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#fbbf24' }} />
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#34d399' }} />
          <span style={{ marginLeft: '8px', fontSize: '11px', color: '#94a3b8', fontWeight: '500' }}>brand-override.css</span>
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>{`/* Eclipse 테마 CSS Variable 직접 재정의 */
.your-brand-scope {
  /* Primary 색상을 브랜드 컬러로 변경 */
  --sem-eclipse-color-fillPrimary: #0ea5e9;
  --sem-eclipse-color-foregroundBrand: #0ea5e9;
  --sem-eclipse-color-backgroundBrandSubtle: rgba(14, 165, 233, 0.08);

  /* 컨테이너 색상 조정 */
  --sem-eclipse-color-backgroundPrimary: #f0f9ff;
  --sem-eclipse-color-surfaceContainer: #e0f2fe;
}

/* 또는 JS에서 EclipseProvider style prop 사용 */
/* <EclipseProvider style={{ '--sem-eclipse-color-fillPrimary': '#0ea5e9' }}> */`}</pre>
      </div>
    </section>

    {/* Important Note */}
    <div style={{
      padding: '16px 20px',
      borderRadius: '10px',
      background: 'linear-gradient(90deg, rgba(99, 102, 241, 0.04) 0%, transparent 100%)',
      borderLeft: '3px solid #6366f1',
    }}>
      <p style={{ fontSize: '14px', color: '#475569', margin: 0, lineHeight: '1.6' }}>
        디자인 토큰 값은 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>EclipseProvider</code>의 CSS Variable로 동작합니다. 반드시 Provider 하위에서 사용해주세요.
      </p>
    </div>

    {/* Ant Design Token System Comparison */}
    <section style={{ marginTop: '56px', marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>Ant Design 토큰 시스템과 비교</h2>
      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '28px', lineHeight: '1.6' }}>
        Ant Design v5와 Orbit UI의 토큰 시스템은 유사한 3단계 구조를 가지지만, 적용 방식이 다릅니다.
      </p>
      <div style={{ overflowX: 'auto', marginBottom: '24px' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '13px' }}>
          <thead>
            <tr style={{ background: '#f8fafc' }}>
              <th style={{ padding: '12px 16px', textAlign: 'left', fontWeight: '600', color: '#475569', borderBottom: '2px solid #e2e8f0' }}>구분</th>
              <th style={{ padding: '12px 16px', textAlign: 'left', fontWeight: '600', color: '#6366f1', borderBottom: '2px solid #e2e8f0' }}>Orbit UI (Eclipse)</th>
              <th style={{ padding: '12px 16px', textAlign: 'left', fontWeight: '600', color: '#f59e0b', borderBottom: '2px solid #e2e8f0' }}>Ant Design v5</th>
            </tr>
          </thead>
          <tbody>
            {[
              ['1계층 (원시값)', 'Reference Token (colorBlue500)', 'Seed Token (colorPrimary)'],
              ['2계층 (의미값)', 'Semantic Token (fillPrimary)', 'Map Token (colorPrimaryBg)'],
              ['3계층 (컴포넌트)', 'Component Token (button.fillColor)', 'Alias Token (colorBgContainer)'],
              ['적용 방식', 'CSS Variable + vanilla-extract', 'CSS-in-JS (cssinjs)'],
              ['Override 방법', 'EclipseProvider style prop', 'ConfigProvider theme prop'],
              ['타입 안전성', 'TypeScript 완전 지원', 'TypeScript 완전 지원'],
            ].map(([label, orbit, ant]) => (
              <tr key={label} style={{ borderBottom: '1px solid #f1f5f9' }}>
                <td style={{ padding: '12px 16px', fontWeight: '500', color: '#334155' }}>{label}</td>
                <td style={{ padding: '12px 16px', color: '#475569' }}><code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '12px' }}>{orbit}</code></td>
                <td style={{ padding: '12px 16px', color: '#475569' }}><code style={{ background: '#fefce8', color: '#92400e', padding: '2px 6px', borderRadius: '4px', fontSize: '12px' }}>{ant}</code></td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <div style={{ background: '#f8fafc', borderRadius: '12px', padding: '20px 24px' }}>
        <h3 style={{ fontSize: '15px', fontWeight: '700', color: '#1e293b', margin: '0 0 12px' }}>Ant Design에서 마이그레이션 시</h3>
        <pre style={{ background: '#1e293b', color: '#e2e8f0', borderRadius: '8px', padding: '16px 20px', fontSize: '13px', lineHeight: '1.7', overflow: 'auto', margin: '0' }}>{`// Ant Design 방식
<ConfigProvider theme={{ token: { colorPrimary: '#6366f1' } }}>
  <App />
</ConfigProvider>

// Orbit UI Eclipse 방식
<EclipseProvider style={{ '--sem-eclipse-color-fillPrimary': '#6366f1' }}>
  <App />
</EclipseProvider>

// 또는 CSS로 전역 override
:root {
  --sem-eclipse-color-fillPrimary: #6366f1;
  --sem-eclipse-color-fillSecondary: #818cf8;
}`}</pre>
      </div>
    </section>

    {/* Brand Application Guide */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>실전 예제: 회사 브랜드 적용하기</h2>
      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '24px', lineHeight: '1.6' }}>
        EclipseProvider에 CSS 변수를 전달하여 브랜드 컬러를 적용합니다. 단 하나의 파일만 수정하면 됩니다.
      </p>
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '20px', marginBottom: '24px' }}>
        {[
          { brand: 'Kakao', primary: '#FEE500', secondary: '#FFEB3B', fg: '#191600' },
          { brand: 'Naver', primary: '#03C75A', secondary: '#02B350', fg: '#FFFFFF' },
          { brand: 'Toss', primary: '#0064FF', secondary: '#0057E3', fg: '#FFFFFF' },
          { brand: 'Coupang', primary: '#EE2424', secondary: '#CC1F1F', fg: '#FFFFFF' },
        ].map(({ brand, primary }) => (
          <div key={brand} style={{ borderRadius: '10px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
            <div style={{ padding: '14px 16px', background: primary, display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
              <span style={{ fontWeight: '700', fontSize: '14px', color: brand === 'Kakao' ? '#191600' : '#fff' }}>{brand} 브랜드</span>
              <span style={{ fontSize: '11px', color: brand === 'Kakao' ? 'rgba(25,22,0,0.6)' : 'rgba(255,255,255,0.7)', fontFamily: 'monospace' }}>{primary}</span>
            </div>
            <div style={{ padding: '12px 16px', background: '#f8fafc', fontSize: '12px', color: '#64748b', fontFamily: 'monospace' }}>
              {'--sem-eclipse-color-fillPrimary: '}{primary}
            </div>
          </div>
        ))}
      </div>
      <pre style={{ background: '#1e293b', color: '#e2e8f0', borderRadius: '12px', padding: '20px 24px', fontSize: '13px', lineHeight: '1.7', overflow: 'auto' }}>{`// theme/brand.ts - 브랜드별 토큰 파일
export const kakaoBrand = {
  '--sem-eclipse-color-fillPrimary': '#FEE500',
  '--sem-eclipse-color-fillSecondary': '#FFEB3B',
  '--sem-eclipse-color-foregroundOnFill': '#191600',
}

export const naverBrand = {
  '--sem-eclipse-color-fillPrimary': '#03C75A',
  '--sem-eclipse-color-fillSecondary': '#02B350',
  '--sem-eclipse-color-foregroundOnFill': '#FFFFFF',
}

// App.tsx - 브랜드 적용
import { EclipseProvider } from '@heejun-com/theme-eclipse'
import { kakaoBrand } from './theme/brand'

function App() {
  return (
    <EclipseProvider style={kakaoBrand}>
      <YourApp />
    </EclipseProvider>
  )
}`}</pre>
    </section>

    {/* Dark Mode Token Override */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>
        Dark Mode 토큰 재정의
      </h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.6', marginBottom: '24px' }}>
        EclipseProvider의 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px' }}>mode="dark"</code> prop을 설정하거나, CSS Variable을 직접 재정의하여 다크 모드를 구현할 수 있습니다.
      </p>

      {/* 다크/라이트 토큰 비교 */}
      <div style={{ overflowX: 'auto', marginBottom: '24px' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '13px' }}>
          <thead>
            <tr style={{ background: '#f8fafc' }}>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontWeight: '600', color: '#475569', borderBottom: '2px solid #e2e8f0' }}>Token</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontWeight: '600', color: '#f59e0b', borderBottom: '2px solid #e2e8f0' }}>Light Mode</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontWeight: '600', color: '#818cf8', borderBottom: '2px solid #e2e8f0' }}>Dark Mode</th>
            </tr>
          </thead>
          <tbody>
            {[
              ['color.backgroundPrimary', '#ffffff', '#0f172a'],
              ['color.surfaceContainer', '#f8fafc', '#1e293b'],
              ['color.surfaceContainerHigh', '#f1f5f9', '#334155'],
              ['color.foregroundPrimary', '#0f172a', '#f8fafc'],
              ['color.foregroundSecondary', '#64748b', '#94a3b8'],
              ['color.borderPrimary', '#e2e8f0', '#334155'],
              ['color.fillPrimary', '#6366f1', '#818cf8'],
            ].map(([token, light, dark]) => (
              <tr key={token} style={{ borderBottom: '1px solid #f1f5f9' }}>
                <td style={{ padding: '10px 16px' }}>
                  <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 8px', borderRadius: '4px', fontSize: '11px', fontFamily: '"JetBrains Mono", monospace' }}>
                    {token}
                  </code>
                </td>
                <td style={{ padding: '10px 16px' }}>
                  <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                    <div style={{ width: '16px', height: '16px', borderRadius: '4px', background: light, border: '1px solid #e2e8f0', flexShrink: 0 }} />
                    <code style={{ fontSize: '12px', color: '#64748b', fontFamily: '"JetBrains Mono", monospace' }}>{light}</code>
                  </div>
                </td>
                <td style={{ padding: '10px 16px', background: '#1e293b' }}>
                  <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                    <div style={{ width: '16px', height: '16px', borderRadius: '4px', background: dark, border: '1px solid #334155', flexShrink: 0 }} />
                    <code style={{ fontSize: '12px', color: '#94a3b8', fontFamily: '"JetBrains Mono", monospace' }}>{dark}</code>
                  </div>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      {/* 다크모드 적용 코드 */}
      <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
        <div style={{ padding: '8px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', gap: '6px' }}>
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#f87171' }} />
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#fbbf24' }} />
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#34d399' }} />
          <span style={{ marginLeft: '8px', fontSize: '11px', color: '#94a3b8', fontWeight: '500' }}>App.tsx</span>
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>{`import { useState } from 'react'
import { EclipseProvider } from '@heejun-com/theme-eclipse'

function App() {
  const [mode, setMode] = useState<'light' | 'dark'>('light')

  return (
    <EclipseProvider mode={mode}>
      {/* 모드 전환 버튼 */}
      <button onClick={() => setMode(m => m === 'light' ? 'dark' : 'light')}>
        {mode === 'light' ? '다크 모드 전환' : '라이트 모드 전환'}
      </button>
      <YourApp />
    </EclipseProvider>
  )
}`}</pre>
      </div>

      {/* CSS 직접 재정의 방법 */}
      <div style={{ marginTop: '20px', borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
        <div style={{ padding: '8px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', gap: '6px' }}>
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#f87171' }} />
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#fbbf24' }} />
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#34d399' }} />
          <span style={{ marginLeft: '8px', fontSize: '11px', color: '#94a3b8', fontWeight: '500' }}>global.css — CSS Variables 직접 사용</span>
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>{`/* ① CSS Variable을 직접 참조 (컴포넌트 외부에서도 활용 가능) */
.my-component {
  background: var(--sem-eclipse-color-backgroundPrimary);
  color: var(--sem-eclipse-color-foregroundPrimary);
  border: 1px solid var(--sem-eclipse-color-borderPrimary);
  border-radius: var(--sem-eclipse-radius-md);
}

/* ② Tailwind와 CSS Variable 조합 */
.card {
  background-color: var(--sem-eclipse-color-surfaceContainer);
  padding: var(--sem-eclipse-spacing-md);
}

/* ③ 다크 모드 직접 재정의 (EclipseProvider 없이) */
@media (prefers-color-scheme: dark) {
  :root {
    --sem-eclipse-color-backgroundPrimary: #0f172a;
    --sem-eclipse-color-foregroundPrimary: #f8fafc;
    --sem-eclipse-color-borderPrimary: #334155;
    --sem-eclipse-color-fillPrimary: #818cf8;
  }
}`}</pre>
      </div>
    </section>

    {/* Before/After 실제 코드 예시 */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>
        Before / After: 토큰 도입 전후 비교
      </h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.6', marginBottom: '24px' }}>
        하드코딩된 색상값에서 Orbit UI 디자인 토큰으로 전환하는 실제 코드 예시입니다.
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '20px' }}>
        {/* Before */}
        <div style={{ borderRadius: '12px', border: '1.5px solid #fecaca', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#fef2f2', borderBottom: '1px solid #fecaca', display: 'flex', alignItems: 'center', gap: '6px' }}>
            <span style={{ fontSize: '11px', fontWeight: '700', color: '#ef4444', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Before (하드코딩)</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#fca5a5', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>{`/* components/Card.css */
.card {
  background: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 12px;
  padding: 16px;
}

.card-title {
  color: #0f172a;
  font-size: 16px;
  font-weight: 700;
}

.card-body {
  color: #64748b;
  font-size: 14px;
}

/* 다크모드 - 모든 파일에 중복 작성 */
@media (prefers-color-scheme: dark) {
  .card { background: #1e293b; }
  .card-title { color: #f8fafc; }
}`}</pre>
        </div>

        {/* After */}
        <div style={{ borderRadius: '12px', border: '1.5px solid #bbf7d0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f0fdf4', borderBottom: '1px solid #bbf7d0', display: 'flex', alignItems: 'center', gap: '6px' }}>
            <span style={{ fontSize: '11px', fontWeight: '700', color: '#10b981', textTransform: 'uppercase', letterSpacing: '0.06em' }}>After (토큰 사용)</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#6ee7b7', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>{`/* components/Card.css */
.card {
  background: var(--sem-eclipse-color-backgroundPrimary);
  border: 1px solid var(--sem-eclipse-color-borderPrimary);
  border-radius: var(--sem-eclipse-radius-md);
  padding: var(--sem-eclipse-spacing-md);
}

.card-title {
  color: var(--sem-eclipse-color-foregroundPrimary);
  font-size: 16px;
  font-weight: 700;
}

.card-body {
  color: var(--sem-eclipse-color-foregroundSecondary);
  font-size: 14px;
}

/* 다크모드 - EclipseProvider가 자동 처리 */
/* 별도 @media 불필요 */`}</pre>
        </div>
      </div>

      <div style={{ padding: '16px 20px', borderRadius: '10px', background: 'linear-gradient(90deg, rgba(16,185,129,0.04) 0%, transparent 100%)', borderLeft: '3px solid #10b981' }}>
        <p style={{ fontSize: '14px', color: '#475569', margin: 0, lineHeight: '1.6' }}>
          <strong style={{ color: '#0f172a' }}>핵심 이점:</strong> CSS Variable 기반 토큰을 사용하면 다크모드, 브랜드 변경, 접근성 조정 모두 한 곳에서 관리됩니다. 컴포넌트 코드는 변경 없이 토큰 값만 바꾸면 전체 UI가 일관되게 업데이트됩니다.
        </p>
      </div>
    </section>

    {/* Brand Color Customization */}
    <section style={{ marginBottom: '64px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '20px' }}>
        <div style={{ width: '28px', height: '28px', borderRadius: '8px', background: '#6366f1', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
          <span style={{ color: '#fff', fontWeight: '700', fontSize: '14px' }}>B</span>
        </div>
        <h2 style={{ fontSize: '20px', fontWeight: '800', color: '#1e293b', margin: 0 }}>브랜드 컬러 적용</h2>
      </div>

      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '24px' }}>
        Orbit UI의 Reference Token을 재정의해 브랜드 고유 색상을 전역으로 적용할 수 있습니다.
        Arco Design의 ConfigProvider, MUI의 createTheme과 같이 한 곳에서 전체 테마를 변경하는 패턴입니다.
      </p>

      <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden', marginBottom: '24px' }}>
        <div style={{ padding: '8px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
          <span style={{ fontSize: '12px', color: '#6366f1', fontWeight: '700' }}>CSS Variables Override</span>
          <span style={{ fontSize: '11px', color: '#94a3b8' }}>globals.css 또는 :root</span>
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '12px', lineHeight: '1.9', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`:root {
  /* Reference Token 재정의 — 브랜드 기본 색상 */
  --ref-eclipse-color-blue500: #2563eb;   /* 기본: #6366f1 (인디고) */
  --ref-eclipse-color-blue400: #3b82f6;
  --ref-eclipse-color-blue600: #1d4ed8;

  /* 다크모드 재정의는 .dark 클래스 또는 data-theme="dark" */
}

/* Semantic Token 직접 오버라이드 (더 세밀한 제어) */
:root {
  --sem-eclipse-color-fillPrimary: #2563eb;
  --sem-eclipse-color-fillPrimaryHover: #1d4ed8;
  --sem-eclipse-color-fillPrimaryActive: #1e40af;
}`}
        </pre>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}>
        <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '8px 16px', background: '#fff8f0', borderBottom: '1px solid #e2e8f0' }}>
            <span style={{ fontSize: '12px', color: '#f59e0b', fontWeight: '700' }}>기업 브랜드 예시</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* Kakao 스타일 (노란색 계열) */
:root {
  --sem-eclipse-color-fillPrimary: #FEE500;
  --sem-eclipse-color-foregroundPrimary:
    #000000;
}

/* Naver 스타일 (초록색 계열) */
:root {
  --sem-eclipse-color-fillPrimary: #03C75A;
}`}
          </pre>
        </div>
        <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '8px 16px', background: '#f0fdf4', borderBottom: '1px solid #e2e8f0' }}>
            <span style={{ fontSize: '12px', color: '#10b981', fontWeight: '700' }}>EclipseProvider theme prop</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* 컴포넌트 레벨 토큰 오버라이드 */
<SolidButton
  theme={{
    // component token 직접 주입
    fill: '#FEE500',
    fillHover: '#FFCB05',
    foreground: '#000000',
  }}
>
  카카오 로그인
</SolidButton>`}
          </pre>
        </div>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '12px' }}>
        {[
          { name: 'Arco Design', method: 'ConfigProvider theme', desc: '전역 컴포넌트에 theme 객체 주입, token 키-값으로 재정의' },
          { name: 'MUI', method: 'createTheme()', desc: 'palette.primary.main 등 시맨틱 키로 색상 계층 구성' },
          { name: 'Orbit UI', method: 'CSS Variables', desc: 'Reference/Semantic/Component 세 계층 중 원하는 곳에서 오버라이드' },
        ].map((item) => (
          <div key={item.name} style={{ padding: '16px', borderRadius: '10px', background: '#f8fafc', border: '1px solid #e2e8f0' }}>
            <div style={{ fontSize: '12px', fontWeight: '700', color: '#6366f1', marginBottom: '4px' }}>{item.name}</div>
            <div style={{ fontSize: '11px', fontWeight: '600', color: '#1e293b', marginBottom: '6px', fontFamily: 'monospace', background: '#f1f5f9', padding: '2px 6px', borderRadius: '4px', display: 'inline-block' }}>{item.method}</div>
            <div style={{ fontSize: '12px', color: '#64748b', lineHeight: '1.6', marginTop: '6px' }}>{item.desc}</div>
          </div>
        ))}
      </div>
    </section>

    {/* Raycast / Linear Token Usage */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>실무 토큰 활용 패턴</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.6', marginBottom: '24px' }}>Raycast, Linear 등 세계 수준 디자인 시스템이 사용하는 토큰 활용 전략입니다.</p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '20px', marginBottom: '24px' }}>
        <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#faf5ff', borderBottom: '1px solid #e2e8f0' }}>
            <span style={{ fontSize: '12px', color: '#7c3aed', fontWeight: '700' }}>Raycast - 다크 모드 토큰</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#1c1c1e', color: '#e5e5e5', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* Raycast는 모든 색상을 
   semantic token으로 정의 */
:root[data-theme="dark"] {
  --bg-primary: #1c1c1e;
  --bg-elevated: #2c2c2e;
  --fg-primary: #f5f5f5;
  --fg-muted: #888888;
  --border: rgba(255,255,255,0.08);
  --accent: #6366f1;
}`}
          </pre>
        </div>

        <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f0f4ff', borderBottom: '1px solid #e2e8f0' }}>
            <span style={{ fontSize: '12px', color: '#4f46e5', fontWeight: '700' }}>Linear - 컴포넌트 토큰</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* Linear는 컴포넌트별 토큰을
   명확하게 네이밍 */
:root {
  --issue-urgent: #ef4444;
  --issue-high: #f59e0b;
  --issue-medium: #6366f1;
  --issue-low: #10b981;
  --status-todo: #64748b;
  --status-done: #22c55e;
}`}
          </pre>
        </div>
      </div>

      <div style={{ padding: '20px', borderRadius: '12px', background: '#f8fafc', border: '1px solid #e2e8f0' }}>
        <div style={{ fontSize: '13px', fontWeight: '700', color: '#1e293b', marginBottom: '12px' }}>Orbit UI 대응 패턴</div>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '12px', fontSize: '12px', color: '#64748b', lineHeight: '1.7' }}>
          <div>
            <div style={{ fontWeight: '700', color: '#6366f1', marginBottom: '4px' }}>Reference → 브랜드</div>
            colorBlue500, colorGreen400 등 원시값으로 브랜드 팔레트를 정의합니다.
          </div>
          <div>
            <div style={{ fontWeight: '700', color: '#8b5cf6', marginBottom: '4px' }}>Semantic → 의미</div>
            fillPrimary, foregroundMuted처럼 의미 기반으로 참조하여 테마 전환을 지원합니다.
          </div>
          <div>
            <div style={{ fontWeight: '700', color: '#06b6d4', marginBottom: '4px' }}>Component → 명세</div>
            buttonEnabledFill처럼 컴포넌트별로 명세하여 세밀한 커스터마이징이 가능합니다.
          </div>
        </div>
      </div>
    </section>

    {/* ─────────────────────────────────────────────────────
        Linear Design + Notion Design 벤치마크
        ───────────────────────────────────────────────────── */}
    <section style={{ marginTop: '48px' }}>
      <div style={{ fontSize: '22px', fontWeight: '800', color: '#1e293b', marginBottom: '6px' }}>
        Linear &amp; Notion 토큰 설계 벤치마크
      </div>
      <div style={{ fontSize: '14px', color: '#64748b', marginBottom: '24px', lineHeight: '1.6' }}>
        Linear와 Notion의 색상 토큰 설계 방식을 Orbit UI 3-Tier 구조와 비교합니다.
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}>
        <div style={{ padding: '20px', borderRadius: '12px', background: '#f5f3ff', border: '2px solid #ede9fe' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '14px' }}>
            <div style={{ width: '32px', height: '32px', borderRadius: '8px', background: '#5e6ad2', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
              <span style={{ color: '#fff', fontWeight: '700', fontSize: '14px' }}>L</span>
            </div>
            <div style={{ fontWeight: '700', fontSize: '15px', color: '#1e293b' }}>Linear</div>
          </div>
          <ul style={{ listStyle: 'none', padding: 0, margin: 0, fontSize: '13px', color: '#4c1d95', lineHeight: '2' }}>
            <li>의미론적 네이밍: <code style={{ background: '#ede9fe', padding: '1px 5px', borderRadius: '4px' }}>--color-fg-primary</code></li>
            <li>상태 suffix: <code style={{ background: '#ede9fe', padding: '1px 5px', borderRadius: '4px' }}>--color-bg-hover</code></li>
            <li>컴포넌트 독립 레이어 없음 (Semantic = Component)</li>
            <li>다크모드는 CSS 변수 재선언으로 처리</li>
          </ul>
        </div>
        <div style={{ padding: '20px', borderRadius: '12px', background: '#fafaf9', border: '2px solid #e5e7eb' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '14px' }}>
            <div style={{ width: '32px', height: '32px', borderRadius: '8px', background: '#191919', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
              <span style={{ color: '#fff', fontWeight: '700', fontSize: '14px' }}>N</span>
            </div>
            <div style={{ fontWeight: '700', fontSize: '15px', color: '#1e293b' }}>Notion</div>
          </div>
          <ul style={{ listStyle: 'none', padding: 0, margin: 0, fontSize: '13px', color: '#374151', lineHeight: '2' }}>
            <li>역할 기반 네이밍: <code style={{ background: '#f3f4f6', padding: '1px 5px', borderRadius: '4px' }}>--bg-default</code></li>
            <li>색상 팔레트 직접 참조 혼재</li>
            <li>컬러 카테고리별 분리 (text / bg / border)</li>
            <li>라이트/다크 두 세트 별도 관리</li>
          </ul>
        </div>
      </div>

      <div style={{ overflowX: 'auto' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '13px' }}>
          <thead>
            <tr style={{ background: '#f1f5f9' }}>
              <th style={{ padding: '10px 14px', textAlign: 'left', fontWeight: '700', color: '#475569', borderBottom: '2px solid #e2e8f0' }}>기준</th>
              <th style={{ padding: '10px 14px', textAlign: 'left', fontWeight: '700', color: '#5e6ad2', borderBottom: '2px solid #e2e8f0' }}>Linear</th>
              <th style={{ padding: '10px 14px', textAlign: 'left', fontWeight: '700', color: '#374151', borderBottom: '2px solid #e2e8f0' }}>Notion</th>
              <th style={{ padding: '10px 14px', textAlign: 'left', fontWeight: '700', color: '#6366f1', borderBottom: '2px solid #e2e8f0' }}>Orbit UI</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td style={{ padding: '10px 14px', fontWeight: '600', color: '#374151', borderBottom: '1px solid #e2e8f0' }}>레이어 수</td>
              <td style={{ padding: '10px 14px', color: '#4c1d95', borderBottom: '1px solid #e2e8f0' }}>2 (Reference + Semantic)</td>
              <td style={{ padding: '10px 14px', color: '#374151', borderBottom: '1px solid #e2e8f0' }}>2 (Reference + Role)</td>
              <td style={{ padding: '10px 14px', color: '#4338ca', fontWeight: '600', borderBottom: '1px solid #e2e8f0' }}>3 (Reference + Semantic + Component)</td>
            </tr>
            <tr style={{ background: '#f8fafc' }}>
              <td style={{ padding: '10px 14px', fontWeight: '600', color: '#374151', borderBottom: '1px solid #e2e8f0' }}>다크모드</td>
              <td style={{ padding: '10px 14px', color: '#4c1d95', borderBottom: '1px solid #e2e8f0' }}>CSS 변수 재선언</td>
              <td style={{ padding: '10px 14px', color: '#374151', borderBottom: '1px solid #e2e8f0' }}>별도 파일</td>
              <td style={{ padding: '10px 14px', color: '#4338ca', fontWeight: '600', borderBottom: '1px solid #e2e8f0' }}>Semantic 재선언 (자동 전파)</td>
            </tr>
            <tr>
              <td style={{ padding: '10px 14px', fontWeight: '600', color: '#374151', borderBottom: '1px solid #e2e8f0' }}>컴포넌트 커스텀</td>
              <td style={{ padding: '10px 14px', color: '#4c1d95', borderBottom: '1px solid #e2e8f0' }}>Semantic 직접 오버라이드</td>
              <td style={{ padding: '10px 14px', color: '#374151', borderBottom: '1px solid #e2e8f0' }}>지원 없음</td>
              <td style={{ padding: '10px 14px', color: '#4338ca', fontWeight: '600', borderBottom: '1px solid #e2e8f0' }}>Component 토큰 theme prop</td>
            </tr>
            <tr style={{ background: '#f8fafc' }}>
              <td style={{ padding: '10px 14px', fontWeight: '600', color: '#374151', borderBottom: '1px solid #e2e8f0' }}>타입 안전성</td>
              <td style={{ padding: '10px 14px', color: '#4c1d95', borderBottom: '1px solid #e2e8f0' }}>CSS 변수 (타입 없음)</td>
              <td style={{ padding: '10px 14px', color: '#374151', borderBottom: '1px solid #e2e8f0' }}>CSS 변수 (타입 없음)</td>
              <td style={{ padding: '10px 14px', color: '#4338ca', fontWeight: '600', borderBottom: '1px solid #e2e8f0' }}>TypeScript + vanilla-extract</td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

  </div>
</Unstyled>
