import { Meta, Unstyled } from '@storybook/addon-docs/blocks'

<Meta title='Intro/아키텍처' />

<Unstyled>
  <style>{`
    [data-orbit-doc] .orbit-stack {
      margin-top: 32px;
      border: 1px solid var(--orbit-line);
      border-radius: 16px;
      overflow: hidden;
      background: var(--orbit-surface);
    }
    [data-orbit-doc] .orbit-stack .layer {
      display: grid;
      grid-template-columns: 96px minmax(0, 1fr) auto;
      gap: 24px;
      align-items: center;
      padding: 22px 24px;
      border-top: 1px solid var(--orbit-line);
    }
    [data-orbit-doc] .orbit-stack .layer:first-child { border-top: 0; }
    [data-orbit-doc] .orbit-stack .layer .num {
      font-family: var(--orbit-mono);
      font-size: 11px;
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-stack .layer .num strong {
      display: block;
      margin-top: 2px;
      font-family: var(--orbit-display);
      font-size: 22px;
      font-weight: 680;
      letter-spacing: -0.02em;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-stack .layer .desc {
      font-size: 13.5px;
      line-height: 1.55;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-stack .layer .desc code,
    [data-orbit-doc] .orbit-stack .layer .pkg {
      font-family: var(--orbit-mono);
      font-size: 12px;
      padding: 2px 8px;
      border-radius: 5px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      color: var(--orbit-ink-2);
      letter-spacing: -0.005em;
    }
    [data-orbit-doc] .orbit-stack .arrow {
      grid-column: 1 / -1;
      display: flex;
      justify-content: center;
      padding: 4px 0;
      font-family: var(--orbit-mono);
      font-size: 10px;
      color: var(--orbit-ink-4);
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    [data-orbit-doc] .orbit-layer-row {
      display: grid;
      grid-template-columns: 200px minmax(0, 1fr);
      gap: 32px;
      padding: 28px 0;
      border-bottom: 1px solid var(--orbit-line);
      align-items: start;
    }
    [data-orbit-doc] .orbit-layer-row:last-of-type { border-bottom: 0; }
    [data-orbit-doc] .orbit-layer-row .lab {
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-layer-row h3 {
      margin: 4px 0 8px;
      font-size: 20px;
      font-weight: 680;
      letter-spacing: -0.018em;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-layer-row .file {
      margin-top: 8px;
      font-family: var(--orbit-mono);
      font-size: 11.5px;
      color: var(--orbit-ink-3);
      letter-spacing: -0.005em;
    }
    [data-orbit-doc] .orbit-layer-row p {
      margin: 0 0 12px;
      max-width: 60ch;
      font-size: 14.5px;
      line-height: 1.62;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-layer-row p code,
    [data-orbit-doc] .orbit-layer-row .meta code {
      padding: 1px 6px;
      border-radius: 5px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-layer-row .examples {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
    }
    [data-orbit-doc] .orbit-layer-row .examples code {
      padding: 3px 9px;
      border-radius: 6px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 11.5px;
      color: var(--orbit-ink-2);
    }

    [data-orbit-doc] .orbit-code {
      margin: 0;
      border: 1px solid var(--orbit-line);
      border-radius: 12px;
      overflow: hidden;
      background: var(--orbit-surface);
    }
    [data-orbit-doc] .orbit-code-head {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: 16px;
      padding: 9px 16px;
      border-bottom: 1px solid var(--orbit-line);
      background: var(--orbit-surface-sunken);
      font-family: var(--orbit-mono);
      font-size: 11px;
      color: var(--orbit-ink-3);
    }
    [data-orbit-doc] .orbit-code-head .tag {
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      font-size: 10.5px;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-code pre {
      margin: 0;
      padding: 18px 20px;
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      line-height: 1.7;
      color: var(--orbit-ink);
      background: transparent;
      overflow-x: auto;
      white-space: pre;
    }
    [data-orbit-doc] .orbit-twocode {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
      margin-top: 24px;
    }

    [data-orbit-doc] .orbit-flow {
      margin-top: 24px;
      display: grid;
      grid-template-columns: 1fr 24px 1fr 24px 1fr;
      gap: 0;
      align-items: stretch;
      border-top: 1px solid var(--orbit-line-2);
      border-bottom: 1px solid var(--orbit-line-2);
    }
    [data-orbit-doc] .orbit-flow .tier {
      padding: 24px 24px 24px 0;
    }
    [data-orbit-doc] .orbit-flow .tier:not(:first-child) { padding-left: 0; }
    [data-orbit-doc] .orbit-flow .tier .name {
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-flow .tier .title {
      margin-top: 6px;
      font-size: 17px;
      font-weight: 660;
      letter-spacing: -0.012em;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-flow .tier .ex {
      margin-top: 10px;
      padding: 6px 8px;
      font-family: var(--orbit-mono);
      font-size: 11.5px;
      color: var(--orbit-ink-2);
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      border-radius: 6px;
      letter-spacing: -0.005em;
    }
    [data-orbit-doc] .orbit-flow .tier .role {
      margin-top: 8px;
      font-size: 13px;
      color: var(--orbit-ink-3);
      line-height: 1.5;
    }
    [data-orbit-doc] .orbit-flow .sep {
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: var(--orbit-mono);
      font-size: 16px;
      color: var(--orbit-ink-4);
    }

    [data-orbit-doc] .orbit-layer-viz {
      margin-top: 24px;
      padding: 36px 32px;
      border: 1px solid var(--orbit-line);
      border-radius: 14px;
      background: var(--orbit-surface);
      display: grid;
      grid-template-columns: 320px minmax(0, 1fr);
      gap: 40px;
      align-items: center;
    }
    [data-orbit-doc] .orbit-layer-viz .stage {
      position: relative;
      width: 100%;
      height: 220px;
    }
    [data-orbit-doc] .orbit-layer-viz .stage .box {
      position: absolute;
      display: flex;
      align-items: flex-end;
      justify-content: center;
      padding: 6px 10px;
      font-family: var(--orbit-mono);
      font-size: 10px;
      color: var(--orbit-ink-3);
      letter-spacing: -0.005em;
      border-radius: 12px;
    }
    [data-orbit-doc] .orbit-layer-viz .stage .container {
      inset: 0;
      border: 1px dashed var(--orbit-line-2);
      background: transparent;
    }
    [data-orbit-doc] .orbit-layer-viz .stage .state {
      top: 16px; left: 16px; right: 16px; bottom: 36px;
      border: 1px solid var(--orbit-line);
      background: var(--orbit-accent-soft);
      border-radius: 10px;
    }
    [data-orbit-doc] .orbit-layer-viz .stage .border {
      top: 32px; left: 32px; right: 32px; bottom: 56px;
      border: 1.5px solid var(--orbit-accent);
      border-radius: 8px;
      background: transparent;
    }
    [data-orbit-doc] .orbit-layer-viz .stage .content {
      top: 52px; left: 52px; right: 52px; bottom: 76px;
      background: var(--orbit-accent);
      border-radius: 6px;
      color: var(--orbit-surface);
      font-size: 11px;
      font-weight: 600;
      align-items: center;
    }
    :root.dark [data-orbit-doc] .orbit-layer-viz .stage .content {
      color: rgb(15, 17, 19);
    }
    [data-orbit-doc] .orbit-layer-viz .legend {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    [data-orbit-doc] .orbit-layer-viz .legend .item {
      display: grid;
      grid-template-columns: 100px 1fr;
      gap: 16px;
      padding: 10px 0;
      border-bottom: 1px solid var(--orbit-line);
    }
    [data-orbit-doc] .orbit-layer-viz .legend .item:last-child { border-bottom: 0; }
    [data-orbit-doc] .orbit-layer-viz .legend .item .name {
      font-family: var(--orbit-mono);
      font-size: 12px;
      font-weight: 600;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-layer-viz .legend .item .desc {
      font-size: 13px;
      color: var(--orbit-ink-3);
      line-height: 1.55;
    }

    [data-orbit-doc] .orbit-pkg {
      padding: 24px 0;
      border-bottom: 1px solid var(--orbit-line);
    }
    [data-orbit-doc] .orbit-pkg:last-child { border-bottom: 0; }
    [data-orbit-doc] .orbit-pkg .head {
      display: flex;
      align-items: baseline;
      gap: 12px;
      flex-wrap: wrap;
    }
    [data-orbit-doc] .orbit-pkg .head code {
      font-family: var(--orbit-mono);
      font-size: 16px;
      font-weight: 600;
      color: var(--orbit-ink);
      letter-spacing: -0.012em;
    }
    [data-orbit-doc] .orbit-pkg .head .badge {
      padding: 2px 8px;
      border-radius: 999px;
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      color: var(--orbit-ink-3);
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      text-transform: uppercase;
      letter-spacing: 0.06em;
    }
    [data-orbit-doc] .orbit-pkg .head .badge.is-rec {
      color: var(--orbit-accent);
      background: var(--orbit-accent-soft);
      border-color: var(--orbit-accent-soft);
    }
    [data-orbit-doc] .orbit-pkg p {
      margin: 12px 0 16px;
      max-width: 64ch;
      font-size: 14px;
      line-height: 1.6;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-pkg p strong { color: var(--orbit-ink); font-weight: 600; }
    [data-orbit-doc] .orbit-pkg .why {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      margin-bottom: 14px;
    }
    [data-orbit-doc] .orbit-pkg .why code {
      padding: 3px 9px;
      border-radius: 6px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 11.5px;
      color: var(--orbit-ink-2);
    }

    @media (max-width: 760px) {
      [data-orbit-doc] .orbit-stack .layer { grid-template-columns: 1fr; gap: 8px; }
      [data-orbit-doc] .orbit-layer-row { grid-template-columns: 1fr; gap: 12px; }
      [data-orbit-doc] .orbit-twocode { grid-template-columns: 1fr; }
      [data-orbit-doc] .orbit-flow { grid-template-columns: 1fr; }
      [data-orbit-doc] .orbit-flow .sep { display: none; }
      [data-orbit-doc] .orbit-layer-viz { grid-template-columns: 1fr; gap: 24px; }
      [data-orbit-doc] .orbit-layer-viz .stage { height: 180px; }
    }
  `}</style>

  <div data-orbit-doc>
    <div className='orbit-wrap'>

      {/* HEADER */}
      <header className='orbit-page-head'>
        <div>
          <div className='orbit-eyebrow'><span className='num'>01</span>Architecture</div>
          <h1 className='orbit-h1'>3-tier 아키텍처</h1>
          <div className='orbit-lede'>
            Theme · Core · Primitives 세 레이어가 서로 다른 책임을 가집니다.
            소비자는 Theme만 import하고, 커스텀 시스템 구축 시 Core로 내려가며, Primitives는 항상 내부에 머무릅니다.
          </div>
        </div>
        <div className='orbit-page-meta'>
          <div><strong>3</strong> tiers</div>
          <div><strong>3</strong> npm packages</div>
          <div>headless · themable</div>
        </div>
      </header>

      {/* STACK DIAGRAM */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>02</span>Layers at a Glance</div>
            <h2>위에서 아래로, 추상에서 구체로.</h2>
            <div>위쪽으로 갈수록 디자인 결정이 들어가고, 아래쪽으로 갈수록 순수 동작과 빌딩 블록만 남습니다.</div>
          </div>
        </div>

        <div className='orbit-stack'>
          <div className='layer'>
            <div className='num'>Layer 3<strong>Theme</strong></div>
            <div className='desc'>
              디자인 토큰이 적용된 완성 컴포넌트. 대부분의 프로젝트는 이 패키지 하나만으로 충분.
              <code>EclipseProvider</code>로 Light·Dark 전환.
            </div>
            <span className='pkg'>@heejun-com/theme-eclipse</span>
          </div>
          <div className='arrow'>↑ wraps & themes</div>
          <div className='layer'>
            <div className='num'>Layer 2<strong>Core</strong></div>
            <div className='desc'>
              Headless 프리미티브. WAI-ARIA, 키보드, 상태 로직 담당.
              <code>theme</code> 프롭으로 외부 토큰을 받아 스타일링.
            </div>
            <span className='pkg'>@heejun-com/core</span>
          </div>
          <div className='arrow'>↑ composes</div>
          <div className='layer'>
            <div className='num'>Layer 1<strong>Primitives</strong></div>
            <div className='desc'>
              Layer·Slot·Animation·Portal 같은 빌딩 블록. 직접 import 하지 않음. Core를 만들기 위한 도구.
            </div>
            <span className='pkg'>internal</span>
          </div>
        </div>
      </section>

      {/* PER LAYER DETAIL */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>03</span>Per Layer</div>
            <h2>각 레이어의 책임.</h2>
            <div>같은 컴포넌트 이름이 레이어마다 다른 형태로 존재합니다. <code>Button</code>은 Core에 있고, <code>SolidButton</code>은 Theme에 있습니다.</div>
          </div>
          <span className='orbit-eyebrow'>3 layers</span>
        </div>

        <div className='orbit-layer-row'>
          <div>
            <div className='lab'>Layer 3</div>
            <h3>Theme</h3>
            <div className='file'>packages/theme-eclipse</div>
          </div>
          <div>
            <div>
              디자인 토큰이 모두 적용된 최종 컴포넌트. <code>SolidButton color='primary'</code>처럼 결정 한 줄로
              완성된 시각을 얻습니다. 토큰 일부 오버라이드가 필요하면 <code>theme</code> 프롭을 사용.
            </div>
            <div className='examples'>
              <code>SolidButton</code><code>OutlineButton</code><code>FloatingTextField</code>
              <code>DataTable</code><code>Modal</code><code>Toast</code><code>AppBar</code>
            </div>
          </div>
        </div>

        <div className='orbit-layer-row'>
          <div>
            <div className='lab'>Layer 2</div>
            <h3>Core (headless)</h3>
            <div className='file'>packages/core</div>
          </div>
          <div>
            <div>
              스타일이 비어 있는 컴포넌트. 접근성·키보드·상태 로직만 담당합니다.
              자체 디자인 시스템을 만들고 싶을 때 Theme 대신 Core를 직접 사용. <code>theme</code> 프롭으로 토큰 주입.
            </div>
            <div className='examples'>
              <code>Button</code><code>TextField</code><code>Checkbox</code><code>Toggle</code>
              <code>Dropdown</code><code>Sheet</code><code>Badge</code><code>ThemeProvider</code>
            </div>
          </div>
        </div>

        <div className='orbit-layer-row'>
          <div>
            <div className='lab'>Layer 1</div>
            <h3>Primitives (internal)</h3>
            <div className='file'>packages/core/src/primitives</div>
          </div>
          <div>
            <div>
              레이어링, 슬롯, 모션, 포탈, 플렉스 같은 React 프레임워크 유틸리티. 직접 import 하지 않습니다.
              Core가 이 모듈로 자기 자신을 만듭니다.
            </div>
            <div className='examples'>
              <code>Layer</code><code>Slot</code><code>Animation</code><code>Presence</code>
              <code>Flex</code><code>Portal</code><code>Spinner</code>
            </div>
          </div>
        </div>
      </section>

      {/* COMPOSITION */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>04</span>Composition</div>
            <h2>레이어 간 합성.</h2>
            <div>아래 두 코드 조각이 시스템 전체를 요약합니다. Primitives를 Core가 합성하고, Core를 Theme이 입힙니다.</div>
          </div>
        </div>

        <div className='orbit-twocode'>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>Primitives → Core</span>
              <span>core/src/components/Button.tsx</span>
            </div>
            <pre>{`import { Layer, Slot } from '../primitives'

export const Button = ({ theme, children }) => (
  <Layer.Container>
    <Layer.State />
    <Layer.Border />
    <Layer.Content>
      <Slot>{children}</Slot>
    </Layer.Content>
  </Layer.Container>
)`}</pre>
          </div>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>Core → Theme</span>
              <span>theme-eclipse/src/components/SolidButton.tsx</span>
            </div>
            <pre>{`import { Button } from '@heejun-com/core'
import { vars } from '../../styles'

export const SolidButton = ({ color, size, ...rest }) => (
  <Button
    theme={{
      ...vars.com.filledButton,
      ...vars.com.filledButton.variant.color[color],
      ...vars.com.filledButton.variant.size[size],
    }}
    {...rest}
  />
)`}</pre>
          </div>
        </div>
      </section>

      {/* COMPOUND PATTERN */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>05</span>Compound Pattern</div>
            <h2>슬롯으로 합성.</h2>
            <div>props 배열이 아닌 컴포넌트 트리가 API. Leading · Center · Trailing 슬롯이 시각 구조를 노출합니다.</div>
          </div>
        </div>

        <div className='orbit-twocode'>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>Button</span>
              <span>compound</span>
            </div>
            <pre>{`<SolidButton color='black' size='medium'>
  <SolidButton.Leading>
    <IconPlus />
  </SolidButton.Leading>
  <SolidButton.Center>
    새 항목 추가
  </SolidButton.Center>
  <SolidButton.Trailing>
    <IconChevronRight />
  </SolidButton.Trailing>
</SolidButton>`}</pre>
          </div>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>AppBar</span>
              <span>compound</span>
            </div>
            <pre>{`<AppBar>
  <AppBar.Leading>
    <GhostButton size='small' color='black'>
      <IconMenu />
    </GhostButton>
  </AppBar.Leading>
  <AppBar.Center>
    페이지 타이틀
  </AppBar.Center>
  <AppBar.Trailing>
    <Avatar />
  </AppBar.Trailing>
</AppBar>`}</pre>
          </div>
        </div>
      </section>

      {/* TOKEN FLOW */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>06</span>Token Flow</div>
            <h2>Reference → Semantic → Component.</h2>
            <div>같은 값이 세 번 명명되는 게 아니라, 의미 계층이 한 단계씩 더해집니다. 소비자는 어느 단계에서든 끼어들 수 있습니다.</div>
          </div>
        </div>

        <div className='orbit-flow'>
          <div className='tier'>
            <div className='name'>01 · Reference</div>
            <div className='title'>원시 값</div>
            <code className='ex'>blue5 = #2563EB</code>
            <div className='role'>변하지 않는 색·간격·라운드의 사전 정의 스케일.</div>
          </div>
          <div className='sep'>→</div>
          <div className='tier'>
            <div className='name'>02 · Semantic</div>
            <div className='title'>의미 매핑</div>
            <code className='ex'>systemMainPrimary = blue5</code>
            <div className='role'>"primary action color" 같은 *역할*. Light/Dark에 따라 ref 값이 달라짐.</div>
          </div>
          <div className='sep'>→</div>
          <div className='tier'>
            <div className='name'>03 · Component</div>
            <div className='title'>컴포넌트 바인딩</div>
            <code className='ex'>button-fill = systemMainPrimary</code>
            <div className='role'>각 컴포넌트의 specific 토큰. <code>theme</code> 프롭으로 부분 오버라이드.</div>
          </div>
        </div>
      </section>

      {/* INTERNAL LAYER SYSTEM */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>07</span>Internal</div>
            <h2>Layer 프리미티브.</h2>
            <div>하나의 인터랙티브 요소는 사실 네 개의 물리 레이어가 합쳐진 결과입니다. 상태 효과, 보더, 콘텐츠가 분리되어 각자 책임만 집니다.</div>
          </div>
        </div>

        <div className='orbit-layer-viz'>
          <div className='stage' aria-hidden='true'>
            <div className='box container'>Container</div>
            <div className='box state'>State</div>
            <div className='box border'>Border</div>
            <div className='box content'>Content</div>
          </div>
          <div className='legend'>
            <div className='item'>
              <div className='name'>Container</div>
              <div className='desc'>최외곽 래퍼. <code style={{ fontFamily: 'var(--orbit-mono)', fontSize: '0.9em', padding: '1px 6px', borderRadius: 5, background: 'var(--orbit-surface-sunken)', border: '1px solid var(--orbit-line)' }}>border-radius</code>와 클릭 영역을 정의합니다.</div>
            </div>
            <div className='item'>
              <div className='name'>State</div>
              <div className='desc'>hover·pressed·focused 시각 효과 전용 레이어. 메인 컬러를 건드리지 않고 상태만 표현.</div>
            </div>
            <div className='item'>
              <div className='name'>Border</div>
              <div className='desc'>테두리 렌더링 전용. 두께·색·radius를 콘텐츠와 독립적으로 제어.</div>
            </div>
            <div className='item'>
              <div className='name'>Content</div>
              <div className='desc'>텍스트·아이콘·자식 슬롯. <code style={{ fontFamily: 'var(--orbit-mono)', fontSize: '0.9em', padding: '1px 6px', borderRadius: 5, background: 'var(--orbit-surface-sunken)', border: '1px solid var(--orbit-line)' }}>Slot</code> 프리미티브로 합성됩니다.</div>
            </div>
          </div>
        </div>
      </section>

      {/* PACKAGES */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>08</span>npm Packages</div>
            <h2>세 패키지로 분리한 이유.</h2>
            <div>모놀리식 단일 패키지 대신 세 개로 쪼개서 선택적 도입과 번들 최소화를 가능하게 합니다.</div>
          </div>
          <span className='orbit-eyebrow'>3 packages</span>
        </div>

        <div className='orbit-pkg'>
          <div className='head'>
            <code>@heejun-com/icons</code>
            <span className='badge'>zero-dep</span>
          </div>
          <div>
            순수 SVG 아이콘 컴포넌트. React만 있으면 어떤 디자인 시스템과도 독립적으로 사용 가능. <strong>Provider 없음, CSS import 없음.</strong>
            기존 시스템 교체 없이 아이콘만 도입할 수 있고, tree-shaking으로 사용한 아이콘만 번들에 포함됩니다.
          </div>
          <div className='why'>
            <code>Line · Fill 두 스타일</code>
            <code>size·color 프롭</code>
            <code>Tree-shakeable</code>
            <code>332 icons</code>
          </div>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>독립 사용</span>
              <span>import only what you need</span>
            </div>
            <pre>{`import { IconHomeLine, IconBell } from '@heejun-com/icons'

<IconHomeLine size={24} color='currentColor' />
<IconBell size={20} />`}</pre>
          </div>
        </div>

        <div className='orbit-pkg'>
          <div className='head'>
            <code>@heejun-com/core</code>
            <span className='badge'>headless</span>
          </div>
          <div>
            스타일이 비어 있는 헤드리스 컴포넌트. <strong>접근성·키보드·상태 로직만</strong> 담고, <code>theme</code> 프롭으로 외부 토큰을 받아 스타일링.
            자체 디자인 시스템을 구축하면서 접근성·인터랙션은 기존 구현을 재사용하고 싶을 때 사용합니다.
          </div>
          <div className='why'>
            <code>WAI-ARIA 내장</code>
            <code>키보드 내비게이션</code>
            <code>Polymorphic as 프롭</code>
            <code>theme 프롭 주입</code>
          </div>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>커스텀 테마</span>
              <span>build your own design system</span>
            </div>
            <pre>{`import { Button } from '@heejun-com/core'

export const MyButton = ({ children }) => (
  <Button
    theme={{
      backgroundColor: 'var(--brand-primary)',
      color: 'var(--brand-on-primary)',
      borderRadius: 'var(--radius-full)',
    }}
  >
    {children}
  </Button>
)`}</pre>
          </div>
        </div>

        <div className='orbit-pkg'>
          <div className='head'>
            <code>@heejun-com/theme-eclipse</code>
            <span className='badge is-rec'>recommended</span>
          </div>
          <div>
            토큰이 모두 적용된 완성 컴포넌트 + <code>EclipseProvider</code>. 대부분의 프로젝트는 이 패키지 하나로 충분합니다.
            Light·Dark 모드, 모바일·PC 분기, baseTextSize 스케일이 Provider 한 줄로 켜집니다.
          </div>
          <div className='why'>
            <code>56 components</code>
            <code>Light + Dark</code>
            <code>Mobile + PC</code>
            <code>SSR (Next.js)</code>
          </div>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>일반 사용</span>
              <span>just install and ship</span>
            </div>
            <pre>{`import { EclipseProvider, SolidButton } from '@heejun-com/theme-eclipse'
import '@heejun-com/theme-eclipse/style.css'

<EclipseProvider mode='light'>
  <SolidButton color='primary'>결제 진행</SolidButton>
</EclipseProvider>`}</pre>
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer className='orbit-foot'>
        <div>3 tiers · 3 packages · headless by design</div>
        <div>
          <a href='?path=/docs/intro-시작하기--docs'>시작하기 →</a>
        </div>
      </footer>

    </div>
  </div>
</Unstyled>
