import { Meta, Unstyled } from '@storybook/blocks'

<Meta title="Intro/아키텍처" />

<Unstyled>
  <div style={{
    maxWidth: '960px',
    margin: '0 auto',
    fontFamily: '"Pretendard Variable", "Pretendard", -apple-system, BlinkMacSystemFont, system-ui, sans-serif',
  }}>

    {/* Header */}
    <div style={{ marginBottom: '48px' }}>
      <div style={{ fontSize: '12px', fontWeight: '700', color: '#6366f1', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Architecture</div>
      <h1 style={{ fontSize: '36px', fontWeight: '800', color: '#1e293b', letterSpacing: '-0.03em', margin: '0 0 12px' }}>3-Tier Architecture</h1>
      <p style={{ fontSize: '16px', color: '#64748b', lineHeight: '1.7', margin: 0 }}>
        Orbit UI는 <strong style={{ color: '#1e293b' }}>Primitives → Core → Theme</strong> 3계층 구조로 설계되어,
        각 레이어가 명확한 책임을 가지며 유연한 확장이 가능합니다.
      </p>
    </div>

    {/* Architecture Diagram */}
    <section style={{ marginBottom: '56px' }}>
      <div style={{
        borderRadius: '20px',
        border: '1px solid #e2e8f0',
        overflow: 'hidden',
        background: '#ffffff',
      }}>
        {/* Layer 3: Theme */}
        <div style={{
          padding: '32px 32px 24px',
          background: 'linear-gradient(135deg, rgba(99, 102, 241, 0.06), rgba(168, 85, 247, 0.04))',
          borderBottom: '1px solid #e2e8f0',
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '12px' }}>
            <div style={{
              padding: '4px 12px', borderRadius: '100px', fontSize: '11px', fontWeight: '700',
              background: '#6366f1', color: '#ffffff', letterSpacing: '0.05em',
            }}>LAYER 3</div>
            <span style={{ fontSize: '18px', fontWeight: '700', color: '#1e293b' }}>Theme Layer</span>
            <code style={{ fontSize: '12px', color: '#6366f1', background: '#f1f5f9', padding: '2px 8px', borderRadius: '4px', fontFamily: '"JetBrains Mono", monospace' }}>@orbit-ui/theme-eclipse</code>
          </div>
          <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.65', margin: '0 0 16px' }}>
            사용자가 직접 사용하는 완성된 컴포넌트입니다. 디자인 토큰(색상, 크기, 간격)이 적용되어 있으며,
            <code style={{ fontSize: '12px', color: '#6366f1', background: '#f1f5f9', padding: '1px 4px', borderRadius: '3px' }}>EclipseProvider</code>를 통해 Light/Dark 테마를 전환할 수 있습니다.
          </p>
          <div style={{ display: 'flex', flexWrap: 'wrap', gap: '6px' }}>
            {['SolidButton', 'OutlineButton', 'TextField', 'DataTable', 'Modal', 'Toast', 'Avatar', 'Chip'].map(c => (
              <span key={c} style={{
                padding: '4px 10px', borderRadius: '6px', fontSize: '12px', fontWeight: '500',
                background: 'rgba(99, 102, 241, 0.08)', color: '#6366f1', border: '1px solid rgba(99, 102, 241, 0.15)',
              }}>{c}</span>
            ))}
          </div>
        </div>

        {/* Arrow */}
        <div style={{ textAlign: 'center', padding: '8px 0', background: '#ffffff', fontSize: '16px', color: '#cbd5e1' }}>
          extends
        </div>

        {/* Layer 2: Core */}
        <div style={{
          padding: '24px 32px',
          background: 'linear-gradient(135deg, rgba(59, 130, 246, 0.04), rgba(6, 182, 212, 0.03))',
          borderTop: '1px solid #e2e8f0',
          borderBottom: '1px solid #e2e8f0',
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '12px' }}>
            <div style={{
              padding: '4px 12px', borderRadius: '100px', fontSize: '11px', fontWeight: '700',
              background: '#3b82f6', color: '#ffffff', letterSpacing: '0.05em',
            }}>LAYER 2</div>
            <span style={{ fontSize: '18px', fontWeight: '700', color: '#1e293b' }}>Core Layer</span>
            <code style={{ fontSize: '12px', color: '#3b82f6', background: '#f1f5f9', padding: '2px 8px', borderRadius: '4px', fontFamily: '"JetBrains Mono", monospace' }}>@orbit-ui/core</code>
          </div>
          <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.65', margin: '0 0 16px' }}>
            스타일이 없는(Headless) 기본 컴포넌트입니다. 접근성(WAI-ARIA), 키보드 인터랙션, 상태 관리 로직을 담당합니다.
            <code style={{ fontSize: '12px', color: '#3b82f6', background: '#f1f5f9', padding: '1px 4px', borderRadius: '3px' }}>theme</code> prop으로 외부에서 스타일 토큰을 주입받습니다.
          </p>
          <div style={{ display: 'flex', flexWrap: 'wrap', gap: '6px' }}>
            {['Button', 'TextField', 'Checkbox', 'RadioButton', 'Toggle', 'Dropdown', 'Sheet', 'Badge'].map(c => (
              <span key={c} style={{
                padding: '4px 10px', borderRadius: '6px', fontSize: '12px', fontWeight: '500',
                background: 'rgba(59, 130, 246, 0.06)', color: '#3b82f6', border: '1px solid rgba(59, 130, 246, 0.12)',
              }}>{c}</span>
            ))}
          </div>
        </div>

        {/* Arrow */}
        <div style={{ textAlign: 'center', padding: '8px 0', background: '#ffffff', fontSize: '16px', color: '#cbd5e1' }}>
          uses
        </div>

        {/* Layer 1: Primitives */}
        <div style={{
          padding: '24px 32px 32px',
          background: 'linear-gradient(135deg, rgba(16, 185, 129, 0.04), rgba(245, 158, 11, 0.03))',
          borderTop: '1px solid #e2e8f0',
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '12px' }}>
            <div style={{
              padding: '4px 12px', borderRadius: '100px', fontSize: '11px', fontWeight: '700',
              background: '#10b981', color: '#ffffff', letterSpacing: '0.05em',
            }}>LAYER 1</div>
            <span style={{ fontSize: '18px', fontWeight: '700', color: '#1e293b' }}>Primitives Layer</span>
            <span style={{ fontSize: '12px', color: '#94a3b8', fontStyle: 'italic' }}>Internal</span>
          </div>
          <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.65', margin: '0 0 16px' }}>
            가장 낮은 수준의 빌딩 블록입니다. UI를 직접 구성하기보다는 Core/Theme 컴포넌트를 만들기 위한 도구입니다.
            레이어링, 슬롯, 애니메이션, 포탈 등 프레임워크 유틸리티를 제공합니다.
          </p>
          <div style={{ display: 'flex', flexWrap: 'wrap', gap: '6px' }}>
            {['Layer', 'Slot', 'Animation', 'Presence', 'Flex', 'Portal', 'Spinner', 'ThemeProvider'].map(c => (
              <span key={c} style={{
                padding: '4px 10px', borderRadius: '6px', fontSize: '12px', fontWeight: '500',
                background: 'rgba(16, 185, 129, 0.06)', color: '#10b981', border: '1px solid rgba(16, 185, 129, 0.12)',
              }}>{c}</span>
            ))}
          </div>
        </div>
      </div>
    </section>

    {/* How it works */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>레이어 간 관계</h2>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>

        {/* Primitives → Core */}
        <div style={{
          borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden',
        }}>
          <div style={{
            padding: '12px 20px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0',
            fontSize: '13px', fontWeight: '600', color: '#475569',
          }}>
            Primitives → Core: Layer 합성
          </div>
          <pre style={{
            margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0',
            fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto',
          }}>
{`// Core의 Button 내부 구조
import { Layer, Slot } from '../primitives'

const Button = ({ theme, children }) => (
  <Layer.Container>
    <Layer.State />     {/* hover/press 효과 */}
    <Layer.Border />    {/* 테두리 렌더링 */}
    <Layer.Content>
      <Slot>{children}</Slot>
    </Layer.Content>
  </Layer.Container>
)`}
          </pre>
        </div>

        {/* Core → Theme */}
        <div style={{
          borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden',
        }}>
          <div style={{
            padding: '12px 20px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0',
            fontSize: '13px', fontWeight: '600', color: '#475569',
          }}>
            Core → Theme: 토큰 주입
          </div>
          <pre style={{
            margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0',
            fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto',
          }}>
{`// Theme의 SolidButton
import { Button } from '@orbit-ui/core'
import { vars } from '../../styles'

const SolidButton = ({ color, size, ...props }) => (
  <Button
    theme={{
      ...vars.com.filledButton,
      ...vars.com.filledButton.variant.color[color],
      ...vars.com.filledButton.variant.size[size],
    }}
    {...props}
  />
)`}
          </pre>
        </div>
      </div>
    </section>

    {/* Compound Component Pattern */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Compound Component Pattern</h2>

      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px' }}>
        대부분의 컴포넌트는 <strong style={{ color: '#1e293b' }}>서브 컴포넌트 합성 패턴</strong>을 사용합니다.
        내부 로직을 건드리지 않고 원하는 요소를 자유롭게 배치할 수 있습니다.
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>
        <div style={{
          borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden',
        }}>
          <div style={{
            padding: '12px 20px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0',
            display: 'flex', alignItems: 'center', gap: '6px',
          }}>
            <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#f87171' }} />
            <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#fbbf24' }} />
            <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#34d399' }} />
            <span style={{ marginLeft: '8px', fontSize: '11px', color: '#94a3b8', fontWeight: '500' }}>Button Compound</span>
          </div>
          <pre style={{
            margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0',
            fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto',
          }}>
{`<SolidButton color="black" size="medium">
  <SolidButton.Leading>
    <PlusIcon />
  </SolidButton.Leading>
  <SolidButton.Center>
    새 항목 추가
  </SolidButton.Center>
  <SolidButton.Trailing>
    <ChevronRightIcon />
  </SolidButton.Trailing>
</SolidButton>`}
          </pre>
        </div>

        <div style={{
          borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden',
        }}>
          <div style={{
            padding: '12px 20px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0',
            display: 'flex', alignItems: 'center', gap: '6px',
          }}>
            <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#f87171' }} />
            <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#fbbf24' }} />
            <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#34d399' }} />
            <span style={{ marginLeft: '8px', fontSize: '11px', color: '#94a3b8', fontWeight: '500' }}>AppBar Compound</span>
          </div>
          <pre style={{
            margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0',
            fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto',
          }}>
{`<AppBar>
  <AppBar.Leading>
    <GhostButton size="small" color="black">
      <MenuIcon />
    </GhostButton>
  </AppBar.Leading>
  <AppBar.Center>
    페이지 타이틀
  </AppBar.Center>
  <AppBar.Trailing>
    <Avatar />
  </AppBar.Trailing>
</AppBar>`}
          </pre>
        </div>
      </div>
    </section>

    {/* Token Hierarchy */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Design Token Flow</h2>

      <div style={{
        display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '0',
        borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden',
      }}>
        {[
          { level: 'Reference', example: 'blue500 = #3b82f6', desc: '변하지 않는 원시 값', color: '#3b82f6' },
          { level: 'Semantic', example: 'fillPrimary = blue500', desc: '맥락에 따른 의미 매핑', color: '#6366f1' },
          { level: 'Component', example: 'btnFill = fillPrimary', desc: '컴포넌트 전용 바인딩', color: '#8b5cf6' },
        ].map((t, i) => (
          <div key={i} style={{
            padding: '24px', background: '#ffffff',
            borderRight: i < 2 ? '1px solid #e2e8f0' : 'none',
            position: 'relative',
          }}>
            <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: t.color, marginBottom: '12px' }} />
            <div style={{ fontSize: '15px', fontWeight: '700', color: '#1e293b', marginBottom: '4px' }}>{t.level} Token</div>
            <div style={{ fontSize: '12px', color: '#64748b', marginBottom: '12px', lineHeight: '1.5' }}>{t.desc}</div>
            <code style={{
              display: 'block', padding: '8px 10px', borderRadius: '6px', background: '#f8fafc',
              fontSize: '11px', color: t.color, fontFamily: '"JetBrains Mono", monospace', border: '1px solid #f1f5f9',
            }}>{t.example}</code>
          </div>
        ))}
      </div>
    </section>

    {/* Layer Primitive Explanation */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '16px' }}>Internal: Layer 시스템</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '24px' }}>
        Layer는 Orbit UI의 핵심 Primitive입니다. 하나의 컴포넌트를 <strong style={{ color: '#1e293b' }}>4개의 물리적 레이어</strong>로 분리하여 
        상태 효과(hover, press), 테두리, 콘텐츠를 독립적으로 제어합니다.
      </p>

      <div style={{
        borderRadius: '16px', border: '1px solid #e2e8f0', padding: '32px',
        background: '#fafafa',
      }}>
        {/* Layer visual */}
        <div style={{ display: 'flex', justifyContent: 'center', marginBottom: '24px' }}>
          <div style={{ position: 'relative', width: '280px', height: '180px' }}>
            {/* Container */}
            <div style={{
              position: 'absolute', inset: 0, borderRadius: '12px',
              border: '2px dashed #cbd5e1', background: 'rgba(241, 245, 249, 0.5)',
              display: 'flex', alignItems: 'flex-end', justifyContent: 'center', padding: '8px',
            }}>
              <span style={{ fontSize: '10px', color: '#94a3b8', fontWeight: '600' }}>Container</span>
            </div>
            {/* State Layer */}
            <div style={{
              position: 'absolute', top: '12px', left: '12px', right: '12px', bottom: '28px',
              borderRadius: '8px', background: 'rgba(99, 102, 241, 0.08)',
              border: '1px solid rgba(99, 102, 241, 0.2)',
              display: 'flex', alignItems: 'flex-end', justifyContent: 'center', padding: '6px',
            }}>
              <span style={{ fontSize: '10px', color: '#6366f1', fontWeight: '600' }}>StateLayer (hover/press)</span>
            </div>
            {/* Border Layer */}
            <div style={{
              position: 'absolute', top: '28px', left: '28px', right: '28px', bottom: '44px',
              borderRadius: '6px', border: '2px solid #6366f1',
              display: 'flex', alignItems: 'flex-end', justifyContent: 'center', padding: '4px',
            }}>
              <span style={{ fontSize: '10px', color: '#6366f1', fontWeight: '600' }}>BorderLayer</span>
            </div>
            {/* Content Layer */}
            <div style={{
              position: 'absolute', top: '44px', left: '44px', right: '44px', bottom: '60px',
              borderRadius: '4px', background: '#6366f1',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
            }}>
              <span style={{ fontSize: '11px', color: '#ffffff', fontWeight: '700' }}>ContentLayer</span>
            </div>
          </div>
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: '8px' }}>
          {[
            { name: 'Container', desc: '최외곽 래퍼, border-radius 담당', color: '#64748b' },
            { name: 'StateLayer', desc: 'hover/press 시각 효과 오버레이', color: '#6366f1' },
            { name: 'BorderLayer', desc: '테두리 렌더링 전용 레이어', color: '#3b82f6' },
            { name: 'ContentLayer', desc: '텍스트/아이콘 등 실제 콘텐츠', color: '#10b981' },
          ].map((l, i) => (
            <div key={i} style={{
              padding: '12px', borderRadius: '8px', background: '#ffffff', border: '1px solid #e2e8f0',
              textAlign: 'center',
            }}>
              <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: l.color, margin: '0 auto 8px' }} />
              <div style={{ fontSize: '12px', fontWeight: '700', color: '#1e293b', marginBottom: '4px' }}>{l.name}</div>
              <div style={{ fontSize: '11px', color: '#94a3b8', lineHeight: '1.4' }}>{l.desc}</div>
            </div>
          ))}
        </div>
      </div>
    </section>

    {/* Summary */}
    <section style={{
      padding: '24px 28px', borderRadius: '12px',
      background: 'linear-gradient(135deg, rgba(99, 102, 241, 0.04), rgba(168, 85, 247, 0.04))',
      border: '1px solid rgba(99, 102, 241, 0.1)',
    }}>
      <div style={{ fontSize: '14px', fontWeight: '600', color: '#1e293b', marginBottom: '8px' }}>Summary</div>
      <ul style={{ margin: 0, paddingLeft: '20px', display: 'flex', flexDirection: 'column', gap: '4px' }}>
        <li style={{ fontSize: '13px', color: '#64748b', lineHeight: '1.6' }}><strong style={{ color: '#10b981' }}>Primitives</strong> — Layer, Slot, Animation 등 내부 빌딩 블록 (직접 사용 X)</li>
        <li style={{ fontSize: '13px', color: '#64748b', lineHeight: '1.6' }}><strong style={{ color: '#3b82f6' }}>Core</strong> — 스타일 없는 Headless 컴포넌트, 접근성/키보드/상태 로직 담당</li>
        <li style={{ fontSize: '13px', color: '#64748b', lineHeight: '1.6' }}><strong style={{ color: '#6366f1' }}>Theme (Eclipse)</strong> — 디자인 토큰이 적용된 최종 컴포넌트, 사용자가 직접 import</li>
      </ul>
    </section>

  </div>
</Unstyled>
