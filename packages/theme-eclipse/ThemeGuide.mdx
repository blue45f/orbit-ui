import { Meta, Unstyled } from '@storybook/blocks'

<Meta title="Intro/테마 가이드" />

<Unstyled>
  <div style={{
    maxWidth: '960px',
    margin: '0 auto',
    fontFamily: '"Pretendard Variable", "Pretendard", -apple-system, BlinkMacSystemFont, system-ui, sans-serif',
  }}>

    {/* Header */}
    <div style={{ marginBottom: '48px' }}>
      <div style={{ fontSize: '12px', fontWeight: '700', color: '#6366f1', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Theme Guide</div>
      <h1 style={{ fontSize: '36px', fontWeight: '800', color: '#1e293b', letterSpacing: '-0.03em', margin: '0 0 12px' }}>테마 가이드</h1>
      <p style={{ fontSize: '16px', color: '#64748b', lineHeight: '1.7', margin: 0 }}>
        Eclipse 테마의 Light/Dark 모드 시스템과 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '14px', fontWeight: '500' }}>EclipseProvider</code> 활용법을 안내합니다.
      </p>
    </div>

    {/* Theme Preview Side-by-Side */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Light & Dark Mode</h2>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}>
        {/* Light */}
        <div style={{
          borderRadius: '16px', overflow: 'hidden',
          border: '1px solid #e2e8f0',
        }}>
          <div style={{
            padding: '12px 20px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0',
            display: 'flex', alignItems: 'center', justifyContent: 'space-between',
          }}>
            <span style={{ fontSize: '13px', fontWeight: '600', color: '#475569' }}>Light Mode</span>
            <div style={{ width: '20px', height: '20px', borderRadius: '50%', background: 'linear-gradient(135deg, #f8fafc, #ffffff)', border: '1px solid #e2e8f0' }} />
          </div>
          <div style={{ padding: '24px', background: '#ffffff' }}>
            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
              <div style={{ padding: '12px 16px', borderRadius: '10px', background: '#f8fafc', border: '1px solid #e2e8f0' }}>
                <div style={{ fontSize: '13px', fontWeight: '600', color: '#1e293b', marginBottom: '4px' }}>Surface Container</div>
                <div style={{ fontSize: '12px', color: '#64748b' }}>카드, 패널의 기본 배경</div>
              </div>
              <div style={{ display: 'flex', gap: '8px' }}>
                <div style={{ flex: 1, padding: '10px', borderRadius: '8px', background: '#6366f1', color: '#ffffff', fontSize: '12px', fontWeight: '600', textAlign: 'center' }}>Primary</div>
                <div style={{ flex: 1, padding: '10px', borderRadius: '8px', background: '#ffffff', border: '1px solid #e2e8f0', color: '#1e293b', fontSize: '12px', fontWeight: '600', textAlign: 'center' }}>Secondary</div>
              </div>
              <div style={{ display: 'flex', gap: '8px' }}>
                <div style={{ height: '8px', flex: 3, borderRadius: '4px', background: '#1e293b' }} />
                <div style={{ height: '8px', flex: 2, borderRadius: '4px', background: '#64748b' }} />
                <div style={{ height: '8px', flex: 1, borderRadius: '4px', background: '#cbd5e1' }} />
              </div>
            </div>
          </div>
        </div>

        {/* Dark */}
        <div style={{
          borderRadius: '16px', overflow: 'hidden',
          border: '1px solid #1e293b',
        }}>
          <div style={{
            padding: '12px 20px', background: '#0f172a', borderBottom: '1px solid #1e293b',
            display: 'flex', alignItems: 'center', justifyContent: 'space-between',
          }}>
            <span style={{ fontSize: '13px', fontWeight: '600', color: '#94a3b8' }}>Dark Mode</span>
            <div style={{ width: '20px', height: '20px', borderRadius: '50%', background: 'linear-gradient(135deg, #0f172a, #1e293b)', border: '1px solid #334155' }} />
          </div>
          <div style={{ padding: '24px', background: '#0f172a' }}>
            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
              <div style={{ padding: '12px 16px', borderRadius: '10px', background: '#1e293b', border: '1px solid #334155' }}>
                <div style={{ fontSize: '13px', fontWeight: '600', color: '#e2e8f0', marginBottom: '4px' }}>Surface Container</div>
                <div style={{ fontSize: '12px', color: '#94a3b8' }}>카드, 패널의 기본 배경</div>
              </div>
              <div style={{ display: 'flex', gap: '8px' }}>
                <div style={{ flex: 1, padding: '10px', borderRadius: '8px', background: '#818cf8', color: '#ffffff', fontSize: '12px', fontWeight: '600', textAlign: 'center' }}>Primary</div>
                <div style={{ flex: 1, padding: '10px', borderRadius: '8px', background: '#1e293b', border: '1px solid #334155', color: '#e2e8f0', fontSize: '12px', fontWeight: '600', textAlign: 'center' }}>Secondary</div>
              </div>
              <div style={{ display: 'flex', gap: '8px' }}>
                <div style={{ height: '8px', flex: 3, borderRadius: '4px', background: '#f1f5f9' }} />
                <div style={{ height: '8px', flex: 2, borderRadius: '4px', background: '#94a3b8' }} />
                <div style={{ height: '8px', flex: 1, borderRadius: '4px', background: '#475569' }} />
              </div>
            </div>
          </div>
        </div>
      </div>

      <div style={{
        padding: '16px 20px', borderRadius: '10px',
        background: 'linear-gradient(90deg, rgba(99, 102, 241, 0.04) 0%, transparent 100%)',
        borderLeft: '3px solid #6366f1',
      }}>
        <p style={{ fontSize: '14px', color: '#475569', margin: 0, lineHeight: '1.6' }}>
          스토리북 상단 툴바의 <strong style={{ color: '#1e293b' }}>Light/Dark 토글</strong>로 모든 컴포넌트의 테마를 실시간 전환할 수 있습니다.
        </p>
      </div>
    </section>

    {/* EclipseProvider Setup */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>EclipseProvider</h2>

      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px' }}>
        모든 테마 기능은 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>EclipseProvider</code>를 통해 제공됩니다.
        내부적으로 <strong style={{ color: '#1e293b' }}>ThemeProvider, LottieProvider, Portal</strong>을 결합하여 테마 클래스, 애니메이션 엔진, 포탈 렌더링을 한번에 설정합니다.
      </p>

      <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden', marginBottom: '24px' }}>
        <div style={{ padding: '12px 20px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '13px', fontWeight: '600', color: '#475569' }}>
          EclipseProvider 내부 구조
        </div>
        <pre style={{
          margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0',
          fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto',
        }}>
{`<ThemeProvider mode={mode} themeClass={themeClass}>
  <Portal>
    <ReusableSVG />    {/* 공유 SVG 정의 */}
  </Portal>
  <LottieProvider>     {/* Lottie 애니메이션 엔진 */}
    {children}
  </LottieProvider>
</ThemeProvider>`}
        </pre>
      </div>

      {/* Props Table */}
      <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse', margin: 0 }}>
          <thead>
            <tr style={{ background: '#f8fafc' }}>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: '1px solid #e2e8f0' }}>Prop</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: '1px solid #e2e8f0' }}>Type</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: '1px solid #e2e8f0' }}>Default</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: '1px solid #e2e8f0' }}>Description</th>
            </tr>
          </thead>
          <tbody>
            {[
              { prop: 'mode', type: "'light' | 'dark'", def: "'light'", desc: 'Light/Dark 테마 모드' },
              { prop: 'platform', type: "'mobile' | 'pc'", def: "'mobile'", desc: '플랫폼별 최적화 스타일' },
              { prop: 'baseTextSize', type: "'xSmall' | 'small' | 'medium' | 'large' | 'xLarge' | 'xxLarge' | 'xxxLarge'", def: "'medium'", desc: '전역 타이포그래피 스케일' },
            ].map((row, i) => (
              <tr key={i}>
                <td style={{ padding: '10px 16px', borderBottom: '1px solid #f1f5f9' }}><code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '12px', fontWeight: '600' }}>{row.prop}</code></td>
                <td style={{ padding: '10px 16px', borderBottom: '1px solid #f1f5f9', fontSize: '12px', color: '#64748b', fontFamily: '"JetBrains Mono", monospace' }}>{row.type}</td>
                <td style={{ padding: '10px 16px', borderBottom: '1px solid #f1f5f9', fontSize: '12px', color: '#64748b', fontFamily: '"JetBrains Mono", monospace' }}>{row.def}</td>
                <td style={{ padding: '10px 16px', borderBottom: '1px solid #f1f5f9', fontSize: '13px', color: '#475569' }}>{row.desc}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </section>

    {/* Semantic Color Tokens */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Semantic Color Tokens</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px' }}>
        Light/Dark 모드에 따라 자동으로 매핑되는 시맨틱 컬러 토큰입니다. 
        <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>vars.sem.color.*</code>로 접근합니다.
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '12px' }}>
        {[
          {
            group: 'Foreground',
            tokens: [
              { name: 'foregroundPrimary', light: '#1e293b', dark: '#f1f5f9', desc: '주요 텍스트' },
              { name: 'foregroundSecondary', light: '#475569', dark: '#94a3b8', desc: '보조 텍스트' },
              { name: 'foregroundTertiary', light: '#94a3b8', dark: '#64748b', desc: '비활성 텍스트' },
            ],
          },
          {
            group: 'Fill',
            tokens: [
              { name: 'fillPrimary', light: '#6366f1', dark: '#818cf8', desc: '주요 채움' },
              { name: 'fillHovered', light: 'rgba(0,0,0,0.04)', dark: 'rgba(255,255,255,0.06)', desc: '호버 상태' },
              { name: 'fillPressed', light: 'rgba(0,0,0,0.08)', dark: 'rgba(255,255,255,0.1)', desc: '프레스 상태' },
            ],
          },
          {
            group: 'Background & Surface',
            tokens: [
              { name: 'backgroundPrimary', light: '#ffffff', dark: '#0f172a', desc: '기본 배경' },
              { name: 'surfaceContainer', light: '#f8fafc', dark: '#1e293b', desc: '컨테이너 배경' },
            ],
          },
          {
            group: 'Status Colors',
            tokens: [
              { name: 'positivePrimary', light: '#22c55e', dark: '#4ade80', desc: '성공/긍정' },
              { name: 'negativePrimary', light: '#ef4444', dark: '#f87171', desc: '오류/부정' },
              { name: 'warningPrimary', light: '#f59e0b', dark: '#fbbf24', desc: '경고' },
            ],
          },
        ].map((group, i) => (
          <div key={i} style={{
            padding: '20px', borderRadius: '12px', border: '1px solid #e2e8f0',
          }}>
            <div style={{ fontSize: '13px', fontWeight: '700', color: '#1e293b', marginBottom: '12px' }}>{group.group}</div>
            <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
              {group.tokens.map((t, j) => (
                <div key={j} style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                  <div style={{ display: 'flex', gap: '2px', flexShrink: 0 }}>
                    <div style={{ width: '16px', height: '16px', borderRadius: '4px', background: t.light, border: '1px solid #e2e8f0' }} />
                    <div style={{ width: '16px', height: '16px', borderRadius: '4px', background: t.dark, border: '1px solid #334155' }} />
                  </div>
                  <code style={{ fontSize: '11px', color: '#6366f1', fontFamily: '"JetBrains Mono", monospace', flexShrink: 0 }}>{t.name}</code>
                  <span style={{ fontSize: '11px', color: '#94a3b8' }}>{t.desc}</span>
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* Usage Examples */}
    <section style={{ marginBottom: '40px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>실전 사용 예시</h2>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            Dynamic Theme Toggle
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`const [mode, setMode] = useState('light')

<EclipseProvider mode={mode}>
  <button onClick={() => 
    setMode(m => m === 'light' ? 'dark' : 'light')
  }>
    Toggle Theme
  </button>
  <App />
</EclipseProvider>`}
          </pre>
        </div>
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            Token 기반 커스텀 스타일
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { vars } from '@heejun-com/theme-eclipse/token'

const CustomCard = () => (
  <div style={{
    background: vars.sem.color.surfaceContainer,
    color: vars.sem.color.foregroundPrimary,
    border: \`1px solid \${vars.sem.color.borderPrimary}\`,
    borderRadius: '12px',
    padding: '24px',
  }}>
    테마에 자동 반응하는 카드
  </div>
)`}
          </pre>
        </div>
      </div>
    </section>

    {/* Runtime Theme Change */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>런타임 테마 변경</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px' }}>
        <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>EclipseProvider</code>의 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>mode</code> prop을 상태로 관리하면 런타임에 즉시 테마를 전환할 수 있습니다.
        CSS 변수가 자동으로 교체되므로 별도의 리렌더링 최적화 없이도 부드럽게 전환됩니다.
      </p>

      <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden', marginBottom: '16px' }}>
        <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
          런타임 테마 전환 패턴 — useLocalStorage 활용
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`// app/providers.tsx
'use client'
import { useState, useEffect } from 'react'
import { EclipseProvider } from '@heejun-com/theme-eclipse'

export function Providers({ children }: { children: React.ReactNode }) {
  const [mode, setMode] = useState<'light' | 'dark'>('light')

  // 시스템 다크모드 감지 + 로컬스토리지 동기화
  useEffect(() => {
    const saved = localStorage.getItem('theme') as 'light' | 'dark' | null
    if (saved) {
      setMode(saved)
    } else if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
      setMode('dark')
    }
  }, [])

  const toggleTheme = () => {
    const next = mode === 'light' ? 'dark' : 'light'
    setMode(next)
    localStorage.setItem('theme', next)
  }

  return (
    <EclipseProvider mode={mode}>
      {/* toggleTheme을 Context로 하위에 전달 */}
      {children}
    </EclipseProvider>
  )
}`}
        </pre>
      </div>

      <div style={{
        padding: '16px 20px', borderRadius: '10px',
        background: 'linear-gradient(90deg, rgba(99,102,241,0.04) 0%, transparent 100%)',
        borderLeft: '3px solid #6366f1',
      }}>
        <p style={{ fontSize: '14px', color: '#475569', margin: 0, lineHeight: '1.6' }}>
          <strong style={{ color: '#1e293b' }}>팁:</strong>{' '}
          <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px' }}>window.matchMedia('(prefers-color-scheme: dark)')</code>로 시스템 설정을 감지하고,
          <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px' }}>localStorage</code>로 사용자 선택을 유지합니다.
        </p>
      </div>
    </section>

    {/* CSS Variable Direct Override */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>CSS 변수 직접 Override</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px' }}>
        Orbit UI의 모든 시맨틱 토큰은 CSS 사용자 정의 속성(Custom Properties)으로 노출됩니다.
        <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>:root</code>에서 직접 override하면 빌드 없이 즉시 적용됩니다.
      </p>

      <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden', marginBottom: '16px' }}>
        <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
          CSS 변수 네이밍 규칙
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* 네이밍 패턴: --{tier}-eclipse-{category}-{name} */

/* 시맨틱 토큰 (Semantic) */
--sem-eclipse-color-fillPrimary
--sem-eclipse-color-foregroundPrimary
--sem-eclipse-color-backgroundPrimary
--sem-eclipse-color-surfaceContainer
--sem-eclipse-color-borderPrimary

/* 컴포넌트 토큰 (Component) */
--comp-eclipse-solidButton-borderRadius
--comp-eclipse-solidButton-paddingHorizontal
--comp-eclipse-textField-borderRadius`}
        </pre>
      </div>

      <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
        <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
          다크모드 커스터마이징 예시
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* globals.css */

/* 라이트모드: 브랜드 컬러를 파랑 계열로 교체 */
:root {
  --sem-eclipse-color-fillPrimary: #0ea5e9;
  --sem-eclipse-color-fillSecondary: #e0f2fe;
}

/* 다크모드: 더 밝은 블루 계열 사용 */
[data-theme="dark"] {
  --sem-eclipse-color-fillPrimary: #38bdf8;
  --sem-eclipse-color-fillSecondary: #0c4a6e;
  --sem-eclipse-color-backgroundPrimary: #020617;
  --sem-eclipse-color-surfaceContainer: #0f172a;
}

/* 컴포넌트 단위 커스터마이징 */
:root {
  --comp-eclipse-solidButton-borderRadius: 6px;
}

/* 모바일: 더 큰 탭 타겟 */
@media (max-width: 768px) {
  :root {
    --comp-eclipse-solidButton-paddingVertical: 14px;
  }
}`}
        </pre>
      </div>
    </section>

    {/* Brand Color Step-by-Step */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>브랜드 컬러 교체 Step-by-Step</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px' }}>
        프로젝트 브랜드 컬러로 교체하는 단계별 가이드입니다. 예시는 Indigo(기본) → Sky Blue(커스텀) 교체입니다.
      </p>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
        {[
          {
            step: '1',
            title: '브랜드 팔레트 결정',
            desc: '5단계 이상의 명도 팔레트를 준비합니다. (50, 100, 200, 300, 400, 500, 600, 700, 800, 900)',
            code: `/* brand-palette.css */
:root {
  --brand-50:  #f0f9ff;
  --brand-100: #e0f2fe;
  --brand-200: #bae6fd;
  --brand-400: #38bdf8;
  --brand-500: #0ea5e9;
  --brand-600: #0284c7;
  --brand-700: #0369a1;
  --brand-900: #0c4a6e;
}`,
          },
          {
            step: '2',
            title: '시맨틱 토큰에 팔레트 연결',
            desc: '브랜드 컬러를 Orbit UI 시맨틱 토큰에 매핑합니다.',
            code: `/* custom-theme.css */
:root {
  /* Primary fill — 버튼, 링크, 강조에 사용 */
  --sem-eclipse-color-fillPrimary: var(--brand-500);

  /* Secondary fill — 호버 배경, 선택 영역 */
  --sem-eclipse-color-fillSecondary: var(--brand-100);

  /* On-fill — Primary 위의 텍스트 색 (항상 대비 확보) */
  --sem-eclipse-color-onFillPrimary: #ffffff;
}

[data-theme="dark"] {
  --sem-eclipse-color-fillPrimary: var(--brand-400);
  --sem-eclipse-color-fillSecondary: var(--brand-900);
}`,
          },
          {
            step: '3',
            title: 'Next.js에 CSS 파일 로드',
            desc: 'app/layout.tsx에서 순서대로 임포트합니다. theme-eclipse 스타일 다음에 override를 로드해야 합니다.',
            code: `// app/layout.tsx
import '@heejun-com/theme-eclipse/style.css'
import './brand-palette.css'   // 1. 팔레트 변수 정의
import './custom-theme.css'    // 2. 시맨틱 토큰 override`,
          },
          {
            step: '4',
            title: '접근성 대비 검증',
            desc: 'WCAG 2.1 AA 기준: 일반 텍스트 4.5:1, 대형 텍스트 3:1. 브랜드 컬러 교체 후 반드시 검증합니다.',
            code: `/* 빠른 대비 검증: 브라우저 DevTools에서 */
/* Accessibility > Color Contrast 탭 확인 */

/* 또는 CSS calc()로 수동 확인 */
/* Lighter 배경(#f0f9ff)에 Darker 텍스트(#0c4a6e) */
/* 대비비: ~12:1 — WCAG AAA 충족 */`,
          },
        ].map(({ step, title, desc, code }) => (
          <div key={step} style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
            <div style={{
              padding: '12px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0',
              display: 'flex', alignItems: 'center', gap: '10px',
            }}>
              <div style={{
                width: '24px', height: '24px', borderRadius: '50%',
                background: '#6366f1', color: '#fff',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontSize: '12px', fontWeight: '700', flexShrink: 0,
              }}>
                {step}
              </div>
              <div>
                <div style={{ fontSize: '13px', fontWeight: '700', color: '#1e293b' }}>{title}</div>
                <div style={{ fontSize: '12px', color: '#94a3b8', marginTop: '2px' }}>{desc}</div>
              </div>
            </div>
            <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
              {code}
            </pre>
          </div>
        ))}
      </div>
    </section>

    {/* TypeScript Theme Type Extension */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>TypeScript로 테마 타입 확장</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px' }}>
        vanilla-extract의 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>createTheme</code>과 TypeScript를 함께 사용하면
        완전한 타입 안전성을 갖춘 커스텀 테마를 만들 수 있습니다. IDE 자동완성과 타입 에러 검출이 모두 동작합니다.
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            커스텀 테마 생성 (my-theme.css.ts)
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`// my-theme.css.ts
import { createTheme } from '@vanilla-extract/css'
import { vars } from '@heejun-com/theme-eclipse/vars'

// vars의 타입을 그대로 상속 → 빠진 토큰은 타입 에러
export const myThemeClass = createTheme(vars, {
  sem: {
    color: {
      fillPrimary: '#0ea5e9',
      fillSecondary: '#e0f2fe',
      foregroundPrimary: '#0c4a6e',
      backgroundPrimary: '#f0f9ff',
      surfaceContainer: '#e0f2fe',
      borderPrimary: '#bae6fd',
      // ...나머지 토큰은 기본값 상속
    },
  },
})`}
          </pre>
        </div>

        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            테마 타입 확장 (theme-types.d.ts)
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`// theme-types.d.ts — 프로젝트 전용 테마 타입 선언
import type { lightThemeClass } from '@heejun-com/theme-eclipse'

// 허용된 테마 클래스 목록 타입 정의
export type ThemeClass =
  | typeof lightThemeClass    // Eclipse 기본 라이트
  | 'my-theme'                // 커스텀 브랜드 테마
  | 'high-contrast-theme'     // 고대비 접근성 테마

// EclipseProvider themeClass prop 확장
declare module '@heejun-com/theme-eclipse' {
  interface EclipseProviderProps {
    themeClass?: ThemeClass
  }
}

// 사용 예:
// <EclipseProvider themeClass={myThemeClass}>
//   — IDE에서 ThemeClass 타입 자동완성 제공
// </EclipseProvider>`}
          </pre>
        </div>

        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden', gridColumn: '1 / -1' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            다중 테마 런타임 전환 (TypeScript 완전 타입 안전)
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`// providers.tsx
import { EclipseProvider } from '@heejun-com/theme-eclipse'
import { myThemeClass } from './my-theme.css'
import { highContrastClass } from './high-contrast.css'

type ThemeMode = 'eclipse' | 'brand' | 'high-contrast'

const themeClassMap: Record<ThemeMode, string | undefined> = {
  eclipse: undefined,          // 기본 Eclipse 테마 (themeClass 없음)
  brand: myThemeClass,         // 커스텀 브랜드 테마
  'high-contrast': highContrastClass,  // 고대비 테마
}

export function AppProviders({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<ThemeMode>('eclipse')
  const [mode, setMode] = useState<'light' | 'dark'>('light')

  return (
    <EclipseProvider
      mode={mode}
      themeClass={themeClassMap[theme]}
    >
      {children}
    </EclipseProvider>
  )
}`}
          </pre>
        </div>
      </div>

      <div style={{
        marginTop: '16px',
        padding: '16px 20px', borderRadius: '10px',
        background: 'linear-gradient(90deg, rgba(99, 102, 241, 0.04) 0%, transparent 100%)',
        borderLeft: '3px solid #6366f1',
      }}>
        <p style={{ fontSize: '14px', color: '#475569', margin: 0, lineHeight: '1.6' }}>
          <strong style={{ color: '#1e293b' }}>vanilla-extract 장점:</strong>{' '}
          빌드 타임에 CSS가 생성되므로 런타임 오버헤드가 없습니다.
          <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px' }}>createTheme</code>은
          CSS 변수 클래스만 교체하므로 테마 전환 시 리렌더링이 최소화됩니다.
        </p>
      </div>
    </section>

    {/* Theme Extension */}
    <section style={{ marginBottom: '40px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>테마 확장 가이드</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '24px' }}>
        shadcn/ui의 CSS 변수 override 방식과 유사하게, Orbit UI도 CSS 변수를 직접 override하여 프로젝트별 커스텀 테마를 만들 수 있습니다.
        <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>EclipseProvider</code> 없이도 CSS 레이어에서 직접 토큰을 재정의할 수 있습니다.
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}>
        {/* CSS override */}
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            방법 1: CSS 변수 직접 Override (글로벌 CSS)
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* globals.css or custom-theme.css */
:root {
  /* fillPrimary를 브랜드 컬러로 교체 */
  --sem-eclipse-color-fillPrimary: #0ea5e9;

  /* 버튼 반경 조정 */
  --comp-eclipse-solidButton-borderRadius: 4px;

  /* 배경 커스텀 */
  --sem-eclipse-color-backgroundPrimary: #fafafa;
  --sem-eclipse-color-surfaceContainer: #f4f4f5;
}

/* 다크모드 override */
[data-theme="dark"] {
  --sem-eclipse-color-fillPrimary: #38bdf8;
}`}
          </pre>
        </div>

        {/* Token override via theme prop */}
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            방법 2: theme prop으로 컴포넌트 토큰 Override
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { SolidButton } from '@heejun-com/theme-eclipse'

// 컴포넌트 단위 토큰 override
<SolidButton
  theme={{
    '--comp-fill': '#0ea5e9',
    '--comp-fill-hovered': '#0284c7',
    '--comp-fill-pressed': '#0369a1',
    '--comp-on-fill': '#ffffff',
    '--comp-border-radius': '4px',
  }}
  color="primary"
  size="medium"
>
  <SolidButton.Center>커스텀 버튼</SolidButton.Center>
</SolidButton>`}
          </pre>
        </div>

        {/* vanilla-extract extend */}
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            방법 3: vanilla-extract로 새 테마 클래스 생성
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`// my-brand-theme.css.ts
import { createTheme } from '@vanilla-extract/css'
import { vars } from '@heejun-com/theme-eclipse/vars'

// Eclipse vars contract를 기반으로 새 테마 생성
export const brandThemeClass = createTheme(vars, {
  sem: {
    color: {
      fillPrimary: '#0ea5e9',
      fillSecondary: '#e0f2fe',
      foregroundPrimary: '#0c4a6e',
      backgroundPrimary: '#f0f9ff',
    },
  },
})

// 사용 시:
// <EclipseProvider themeClass={brandThemeClass}>
//   <App />
// </EclipseProvider>`}
          </pre>
        </div>

        {/* Next.js integration */}
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            방법 4: Next.js App Router 설정
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`// app/layout.tsx
import '@heejun-com/theme-eclipse/style.css'
import './globals.css'  // CSS 변수 override 파일
import { EclipseProvider } from '@heejun-com/theme-eclipse'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ko">
      <body>
        <EclipseProvider mode="light" platform="pc">
          {children}
        </EclipseProvider>
      </body>
    </html>
  )
}`}
          </pre>
        </div>
      </div>

      <div style={{
        padding: '16px 20px', borderRadius: '10px',
        background: 'linear-gradient(90deg, rgba(16, 185, 129, 0.04) 0%, transparent 100%)',
        borderLeft: '3px solid #10b981',
      }}>
        <p style={{ fontSize: '14px', color: '#475569', margin: 0, lineHeight: '1.6' }}>
          <strong style={{ color: '#1e293b' }}>권장 순서:</strong>{' '}
          단순 브랜드 색상 변경은 <strong>방법 1 (CSS 변수 override)</strong>,
          컴포넌트별 미세 조정은 <strong>방법 2 (theme prop)</strong>,
          완전히 다른 테마가 필요하면 <strong>방법 3 (vanilla-extract createTheme)</strong>을 사용하세요.
        </p>
      </div>
    </section>

    {/* Tailwind UI + Chakra UI 테마 벤치마크 */}
    <section style={{ marginBottom: '64px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>
        테마 시스템 벤치마크
      </h2>
      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '24px' }}>
        Tailwind UI와 Chakra UI의 테마 접근법과 Orbit UI의 차별점을 비교합니다. (Cycle 43 벤치마크)
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '16px', marginBottom: '28px' }}>
        {[
          {
            name: 'Tailwind UI',
            color: '#06b6d4',
            approach: 'Config 기반',
            pros: ['tailwind.config.js 단일 진입점', '유틸리티 클래스 조합', 'JIT 컴파일 최적화'],
            cons: ['런타임 테마 변경 불가', '타입 안전성 미흡', 'CSS 클래스 문자열 의존'],
          },
          {
            name: 'Chakra UI',
            color: '#319795',
            approach: 'Recipe 기반',
            pros: ['ColorSchemeProvider로 다크모드', 'extendTheme() 간편 확장', 'semantic tokens 지원'],
            cons: ['번들 크기 큼', 'Emotion 의존성', '커스텀 컴포넌트 복잡'],
          },
          {
            name: 'Orbit UI',
            color: '#6366f1',
            approach: 'vanilla-extract',
            pros: ['빌드 타임 타입 안전', '3단계 토큰 계층', 'Figma 변수 직접 연동'],
            cons: ['초기 설정 필요', '빌드 플러그인 의존', '학습 곡선 존재'],
          },
        ].map(({ name, color, approach, pros, cons }) => (
          <div key={name} style={{ padding: '20px', borderRadius: '14px', border: `2px solid ${color}25`, background: `${color}08` }}>
            <div style={{ fontSize: '14px', fontWeight: '800', color, marginBottom: '4px' }}>{name}</div>
            <div style={{ fontSize: '11px', fontWeight: '700', color: '#94a3b8', textTransform: 'uppercase', letterSpacing: '0.07em', marginBottom: '14px' }}>
              {approach}
            </div>
            <div style={{ marginBottom: '12px' }}>
              <div style={{ fontSize: '11px', fontWeight: '700', color: '#10b981', marginBottom: '6px' }}>장점</div>
              {pros.map((p) => (
                <div key={p} style={{ display: 'flex', gap: '6px', alignItems: 'flex-start', marginBottom: '4px' }}>
                  <span style={{ color: '#10b981', fontSize: '10px', marginTop: '2px' }}>✓</span>
                  <span style={{ fontSize: '12px', color: '#374151' }}>{p}</span>
                </div>
              ))}
            </div>
            <div>
              <div style={{ fontSize: '11px', fontWeight: '700', color: '#f59e0b', marginBottom: '6px' }}>고려사항</div>
              {cons.map((c) => (
                <div key={c} style={{ display: 'flex', gap: '6px', alignItems: 'flex-start', marginBottom: '4px' }}>
                  <span style={{ color: '#f59e0b', fontSize: '10px', marginTop: '2px' }}>!</span>
                  <span style={{ fontSize: '12px', color: '#64748b' }}>{c}</span>
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>

      <div style={{ padding: '20px 24px', borderRadius: '12px', background: '#f0fdf4', border: '1.5px solid #bbf7d0' }}>
        <div style={{ fontSize: '13px', fontWeight: '700', color: '#15803d', marginBottom: '8px' }}>
          Orbit UI의 핵심 차별점
        </div>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '12px' }}>
          {[
            { title: 'TypeScript 완전 지원', desc: 'theme prop에서 토큰 키 자동완성 및 타입 오류 감지' },
            { title: '제로 런타임 CSS', desc: 'vanilla-extract로 빌드 타임 CSS 생성, 런타임 오버헤드 없음' },
            { title: 'Figma 토큰 동기화', desc: 'Reference → Semantic → Component 3단계로 Figma 변수 직접 매핑' },
            { title: '컴포넌트별 격리', desc: 'theme prop으로 단일 인스턴스만 변경, 전역 영향 없음' },
          ].map(({ title, desc }) => (
            <div key={title} style={{ display: 'flex', gap: '10px', alignItems: 'flex-start' }}>
              <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#10b981', marginTop: '5px', flexShrink: 0 }} />
              <div>
                <div style={{ fontSize: '13px', fontWeight: '600', color: '#1e293b', marginBottom: '2px' }}>{title}</div>
                <div style={{ fontSize: '12px', color: '#64748b' }}>{desc}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    {/* Notion + Apple HIG 벤치마크 섹션 */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>
        Notion Design + Apple HIG 벤치마크
      </h2>
      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '28px' }}>
        Notion의 컨텍스트 기반 테마와 Apple HIG의 플랫폼 적응형 색상 시스템에서 배우는 Orbit UI 적용 패턴입니다. (Cycle 52 벤치마크)
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '20px', marginBottom: '28px' }}>
        {/* Notion */}
        <div style={{ borderRadius: '14px', border: '2px solid #0000001a', background: '#fff', overflow: 'hidden' }}>
          <div style={{ padding: '16px 20px', background: '#0f0f0f', display: 'flex', alignItems: 'center', gap: '10px' }}>
            <div style={{ width: '20px', height: '20px', borderRadius: '4px', background: '#fff', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '11px', fontWeight: '800', color: '#0f0f0f' }}>N</div>
            <span style={{ fontSize: '13px', fontWeight: '700', color: '#fff' }}>Notion Design System</span>
          </div>
          <div style={{ padding: '20px' }}>
            <div style={{ fontSize: '11px', fontWeight: '700', color: '#94a3b8', textTransform: 'uppercase', letterSpacing: '0.07em', marginBottom: '12px' }}>핵심 패턴</div>
            {[
              { pattern: '컨텍스트 기반 색상', desc: '페이지 유형(문서/DB/보드)에 따라 동적으로 테마 적용' },
              { pattern: '인라인 편집 토글', desc: '클릭 시 input 활성화, blur 시 저장 — 최소 UI 원칙' },
              { pattern: '계층형 Breadcrumb', desc: '페이지 경로를 실시간 반영, 클릭으로 이동 가능' },
              { pattern: '속성 패널 레이아웃', desc: '라벨 120px 고정 + 값 영역 가변 2-column 그리드' },
            ].map(({ pattern, desc }) => (
              <div key={pattern} style={{ marginBottom: '10px', padding: '10px 12px', borderRadius: '8px', background: '#f8fafc', border: '1px solid #f1f5f9' }}>
                <div style={{ fontSize: '12px', fontWeight: '700', color: '#1e293b', marginBottom: '2px' }}>{pattern}</div>
                <div style={{ fontSize: '11px', color: '#64748b' }}>{desc}</div>
              </div>
            ))}
          </div>
          <div style={{ padding: '12px 20px', background: '#f8fafc', borderTop: '1px solid #f1f5f9' }}>
            <div style={{ fontSize: '11px', fontWeight: '700', color: '#64748b', marginBottom: '6px' }}>Orbit UI 매핑</div>
            <code style={{ fontSize: '11px', color: '#6366f1', background: '#f0f1ff', padding: '4px 8px', borderRadius: '4px', display: 'block' }}>
              {'TextField onChange + onBlur → 인라인 편집'}
            </code>
          </div>
        </div>

        {/* Apple HIG */}
        <div style={{ borderRadius: '14px', border: '2px solid #0000001a', background: '#fff', overflow: 'hidden' }}>
          <div style={{ padding: '16px 20px', background: '#1c1c1e', display: 'flex', alignItems: 'center', gap: '10px' }}>
            <div style={{ width: '20px', height: '20px', borderRadius: '50%', background: 'linear-gradient(135deg, #e8e8e8, #b0b0b0)', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '11px', fontWeight: '800', color: '#1c1c1e' }}>A</div>
            <span style={{ fontSize: '13px', fontWeight: '700', color: '#fff' }}>Apple HIG</span>
          </div>
          <div style={{ padding: '20px' }}>
            <div style={{ fontSize: '11px', fontWeight: '700', color: '#94a3b8', textTransform: 'uppercase', letterSpacing: '0.07em', marginBottom: '12px' }}>핵심 패턴</div>
            {[
              { pattern: '플랫폼 적응형 색상', desc: '#007aff (iOS Blue) 시스템 색상, 다크모드 자동 전환' },
              { pattern: 'Clear 버튼 패턴', desc: 'Search Field에 입력값 있을 때만 표시되는 x 버튼' },
              { pattern: '드릴다운 네비게이션', desc: '< Back 버튼 + Breadcrumb 병행, 계층 탐색 명확화' },
              { pattern: '섹션 그룹 리스트', desc: 'rounded-2xl 카드 내 divider로 항목 구분, 끝은 rounded' },
            ].map(({ pattern, desc }) => (
              <div key={pattern} style={{ marginBottom: '10px', padding: '10px 12px', borderRadius: '8px', background: '#f8fafc', border: '1px solid #f1f5f9' }}>
                <div style={{ fontSize: '12px', fontWeight: '700', color: '#1e293b', marginBottom: '2px' }}>{pattern}</div>
                <div style={{ fontSize: '11px', color: '#64748b' }}>{desc}</div>
              </div>
            ))}
          </div>
          <div style={{ padding: '12px 20px', background: '#f8fafc', borderTop: '1px solid #f1f5f9' }}>
            <div style={{ fontSize: '11px', fontWeight: '700', color: '#64748b', marginBottom: '6px' }}>Orbit UI 매핑</div>
            <code style={{ fontSize: '11px', color: '#6366f1', background: '#f0f1ff', padding: '4px 8px', borderRadius: '4px', display: 'block' }}>
              {'Breadcrumb + TextField + SolidButton → 계층 탐색'}
            </code>
          </div>
        </div>
      </div>

      {/* 비교표 */}
      <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
        <div style={{ display: 'grid', gridTemplateColumns: '1.5fr 1fr 1fr 1fr', background: '#f8fafc', padding: '10px 16px', borderBottom: '1px solid #e2e8f0', fontSize: '11px', fontWeight: '700', color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
          <span>패턴</span>
          <span>Notion</span>
          <span>Apple HIG</span>
          <span>Orbit UI</span>
        </div>
        {[
          { pattern: '색상 철학', notion: '페이지 컨텍스트 기반', apple: '시스템 semantic 색상', orbit: '3단계 토큰 계층' },
          { pattern: '텍스트 입력', notion: '클릭-편집 인라인', apple: 'SF Symbol 아이콘 + Clear', orbit: 'TextField + onBlur 저장' },
          { pattern: '탐색 패턴', notion: 'Breadcrumb 경로 추적', apple: '< Back + Breadcrumb', orbit: 'Breadcrumb 컴포넌트' },
          { pattern: '다크 모드', notion: 'CSS 변수 자동 전환', apple: 'UIColor semantic 자동', orbit: 'EclipseProvider 토글' },
          { pattern: '상태 피드백', notion: '인라인 상태 도트', apple: 'SF Symbol 상태 아이콘', orbit: 'LabelBadge + CounterBadge' },
        ].map(({ pattern, notion, apple, orbit }, i) => (
          <div key={pattern} style={{ display: 'grid', gridTemplateColumns: '1.5fr 1fr 1fr 1fr', padding: '10px 16px', borderBottom: i < 4 ? '1px solid #f1f5f9' : 'none', fontSize: '12px', background: i % 2 === 0 ? '#fff' : '#fafafa' }}>
            <span style={{ fontWeight: '600', color: '#1e293b' }}>{pattern}</span>
            <span style={{ color: '#64748b' }}>{notion}</span>
            <span style={{ color: '#64748b' }}>{apple}</span>
            <span style={{ color: '#6366f1', fontWeight: '600' }}>{orbit}</span>
          </div>
        ))}
      </div>
    </section>

    {/* Tailwind UI + Arco Design 섹션 */}
    <section style={{ marginBottom: '64px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '24px' }}>
        <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', margin: 0 }}>Tailwind UI + Arco Design 테마 패턴 분석</h2>
        <span style={{ padding: '3px 10px', background: '#f1f5f9', borderRadius: '20px', fontSize: '12px', fontWeight: '600', color: '#6366f1' }}>Cycle 56</span>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '20px', marginBottom: '28px' }}>
        <div style={{ padding: '24px', borderRadius: '14px', background: '#0f172a', border: '1px solid #1e293b' }}>
          <div style={{ fontSize: '11px', fontWeight: '700', color: '#94a3b8', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Tailwind UI</div>
          <div style={{ fontSize: '18px', fontWeight: '700', color: '#fff', marginBottom: '10px' }}>Utility-First Layout System</div>
          <p style={{ fontSize: '13px', color: '#94a3b8', lineHeight: '1.7', margin: '0 0 16px' }}>
            Tailwind UI는 Tailwind CSS 유틸리티 클래스만으로 완성도 높은 레이아웃 패턴을 제공합니다. Application Shell, Stacked Layout, Sidebar with Header 등 실무 레이아웃 청사진을 제공해 빠른 구현이 가능합니다.
          </p>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', fontSize: '12px' }}>
            {[
              ['Stacked Layout', 'SectionTitle + 탭 네비게이션으로 페이지 헤더 구성'],
              ['Table Overlay', 'position: absolute 오버레이로 기존 테이블 구조 유지하며 로딩'],
              ['Filter Pattern', '검색 입력 + 상태 필터 버튼의 SectionTitle 하단 배치'],
            ].map(([k, v]) => (
              <div key={k} style={{ padding: '8px 12px', background: '#1e293b', borderRadius: '8px', border: '1px solid #334155' }}>
                <span style={{ color: '#6366f1', fontWeight: '700' }}>{k}: </span>
                <span style={{ color: '#94a3b8' }}>{v}</span>
              </div>
            ))}
          </div>
        </div>

        <div style={{ padding: '24px', borderRadius: '14px', background: '#001d35', border: '1px solid #003a6b' }}>
          <div style={{ fontSize: '11px', fontWeight: '700', color: '#5aaff5', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Arco Design</div>
          <div style={{ fontSize: '18px', fontWeight: '700', color: '#fff', marginBottom: '10px' }}>Enterprise Data Experience</div>
          <p style={{ fontSize: '13px', color: '#8eb9e0', lineHeight: '1.7', margin: '0 0 16px' }}>
            Arco Design은 아시아권 엔터프라이즈 제품에 특화된 데이터 시각화와 복잡한 폼 UX에 강점을 보입니다. Steps + Spin 조합, 낙관적 업데이트 피드백, Descriptions 다단계 설정 등이 특징입니다.
          </p>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', fontSize: '12px' }}>
            {[
              ['Steps + Spin', '다단계 처리를 타임라인으로 시각화, 각 단계에 로딩 스피너 삽입'],
              ['Optimistic Update', '즉각 UI 반영 후 서버 동기화 중 인라인 Loading 표시'],
              ['Collapse Settings', 'SectionTitle을 아코디언 헤더로 활용한 설정 그룹 패턴'],
            ].map(([k, v]) => (
              <div key={k} style={{ padding: '8px 12px', background: '#003a6b', borderRadius: '8px', border: '1px solid #005799' }}>
                <span style={{ color: '#5aaff5', fontWeight: '700' }}>{k}: </span>
                <span style={{ color: '#8eb9e0' }}>{v}</span>
              </div>
            ))}
          </div>
        </div>
      </div>

      <div style={{ overflowX: 'auto', borderRadius: '12px', border: '1.5px solid #e2e8f0', marginBottom: '24px' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '13px' }}>
          <thead>
            <tr style={{ background: '#f8fafc' }}>
              {['기준', 'Tailwind UI', 'Arco Design', 'Orbit UI 반영'].map((col, i) => (
                <th key={col} style={{ padding: '12px 16px', textAlign: 'left', fontWeight: 700, color: i === 3 ? '#6366f1' : '#475569', borderBottom: '1.5px solid #e2e8f0', whiteSpace: 'nowrap' }}>{col}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            {[
              ['레이아웃', 'Stacked/Sidebar Shell', '독립 레이아웃 미제공', 'Templates 패턴으로 제공'],
              ['로딩 UI', 'Table Overlay 반투명', 'Steps+Spin 다단계', 'Loading 오버레이·파이프라인·낙관적업데이트'],
              ['섹션 헤더', '검색+필터 상단 배치', 'Collapse 아코디언', 'SectionTitle 탭·아코디언·필터 3스토리'],
              ['상태 피드백', '인라인 텍스트 표시', '즉각 반영+동기화 표시', '낙관적 업데이트 + Loading 인라인'],
              ['데이터 시각화', '미제공', 'Charts 통합 제공', '추후 Chart 통합 계획'],
            ].map((row, i) => (
              <tr key={i} style={{ background: i % 2 === 0 ? '#fff' : '#fafafa' }}>
                {row.map((cell, j) => (
                  <td key={j} style={{ padding: '11px 16px', color: j === 3 ? '#4f46e5' : '#334155', borderBottom: '1px solid #f1f5f9', fontWeight: j === 0 ? 600 : 400 }}>{cell}</td>
                ))}
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <div style={{ padding: '20px', borderRadius: '12px', background: 'linear-gradient(135deg, #f0fdf4, #ecfdf5)', border: '2px solid #bbf7d0' }}>
        <div style={{ fontSize: '13px', fontWeight: '700', color: '#065f46', marginBottom: '12px' }}>Orbit UI 반영 결과</div>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '12px', fontSize: '12px', color: '#374151', lineHeight: '1.7' }}>
          <div>
            <div style={{ fontWeight: '700', color: '#059669', marginBottom: '4px' }}>SectionTitle 고도화</div>
            Tailwind UI 탭 섹션 네비게이션, Arco 아코디언 설정 그룹, Tailwind UI 필터링 섹션 헤더 3개 스토리 추가.
          </div>
          <div>
            <div style={{ fontWeight: '700', color: '#059669', marginBottom: '4px' }}>Loading 고도화</div>
            Tailwind UI 테이블 오버레이, Arco 다단계 파이프라인, Arco 낙관적 업데이트 인라인 로딩 3개 스토리 추가.
          </div>
        </div>
      </div>
    </section>

  </div>
</Unstyled>

{/* ========== CYCLE 61: shadcn/ui + Vercel Design ========== */}
<section style={{ marginBottom: '48px' }}>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '20px' }}>
    <div style={{ width: '36px', height: '36px', borderRadius: '10px', background: 'linear-gradient(135deg, #09090b, #18181b)', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#fff', fontWeight: '800', fontSize: '15px' }}>S</div>
    <div>
      <div style={{ fontSize: '22px', fontWeight: '800', color: '#09090b', letterSpacing: '-0.5px' }}>Cycle 61 — shadcn/ui + Vercel Design 벤치마크</div>
      <div style={{ fontSize: '13px', color: '#71717a', marginTop: '2px' }}>다크 모드 퍼스트 디자인과 컴팩트 인터랙션 패턴 분석</div>
    </div>
  </div>

  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '16px', marginBottom: '24px' }}>
    <div style={{ padding: '20px', borderRadius: '12px', background: '#09090b', border: '1px solid #27272a' }}>
      <div style={{ fontSize: '14px', fontWeight: '700', color: '#f4f4f5', marginBottom: '12px' }}>shadcn/ui 핵심 패턴</div>
      <ul style={{ margin: 0, padding: '0 0 0 16px', fontSize: '13px', color: '#a1a1aa', lineHeight: '1.8' }}>
        <li>Radix UI 기반 비스타일 프리미티브</li>
        <li>copy-paste 설치 방식 (소유권 이전)</li>
        <li>CSS 변수 기반 다크 모드 토글</li>
        <li>Tooltip.Provider delayDuration 전역 설정</li>
        <li>write/preview 탭 에디터 분리 패턴</li>
      </ul>
    </div>
    <div style={{ padding: '20px', borderRadius: '12px', background: '#09090b', border: '1px solid #27272a' }}>
      <div style={{ fontSize: '14px', fontWeight: '700', color: '#f4f4f5', marginBottom: '12px' }}>Vercel Design 핵심 패턴</div>
      <ul style={{ margin: 0, padding: '0 0 0 16px', fontSize: '13px', color: '#a1a1aa', lineHeight: '1.8' }}>
        <li>정보 밀도 최적화 — 컴팩트 액션 힌트</li>
        <li>kbd 단축키 레이블로 전문가 UX 강화</li>
        <li>배포 상태 실시간 파이프라인 시각화</li>
        <li>환경변수 멀티라인 편집기 패턴</li>
        <li>dark #09090b 배경 + zinc 계열 팔레트</li>
      </ul>
    </div>
  </div>

  <div style={{ overflowX: 'auto', borderRadius: '12px', border: '1.5px solid #e4e4e7', marginBottom: '24px' }}>
    <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '13px' }}>
      <thead>
        <tr style={{ background: '#fafafa' }}>
          {['기준', 'shadcn/ui', 'Vercel Design', 'Orbit UI 반영'].map((col, i) => (
            <th key={col} style={{ padding: '12px 16px', textAlign: 'left', fontWeight: 700, color: i === 3 ? '#6366f1' : '#09090b', borderBottom: '1.5px solid #e4e4e7', whiteSpace: 'nowrap' }}>{col}</th>
          ))}
        </tr>
      </thead>
      <tbody>
        {[
          ['색상 모드', 'CSS 변수 즉시 스위치', 'dark 퍼스트 단일 팔레트', 'Tooltip·TextArea 다크 테마 스토리'],
          ['툴팁', 'Provider delayDuration 전역', '컴팩트 액션힌트 + kbd', 'Shadcn 차트·Vercel 힌트·Linear 단축키'],
          ['텍스트 에디터', 'write/preview 탭 분리', '멀티라인 ENV 편집', 'Comment·EnvVar·AutoSave 3패턴'],
          ['상태 표시', '남은글자·제출 애니메이션', '파싱 결과 유효성 표시', '실시간 카운터 + debounce 저장'],
          ['제품 카탈로그', '사이드바 필터 + 그리드', '컴팩트 리스트 뷰', 'Slider 가격·Toggle 세일·Chip 카테고리'],
        ].map((row, i) => (
          <tr key={i} style={{ background: i % 2 === 0 ? '#fff' : '#fafafa' }}>
            {row.map((cell, j) => (
              <td key={j} style={{ padding: '11px 16px', color: j === 3 ? '#4f46e5' : '#09090b', borderBottom: '1px solid #f4f4f5', fontWeight: j === 0 ? 600 : 400 }}>{cell}</td>
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  </div>

  <div style={{ padding: '20px', borderRadius: '12px', background: 'linear-gradient(135deg, #faf5ff, #f0f9ff)', border: '2px solid #e9d5ff' }}>
    <div style={{ fontSize: '13px', fontWeight: '700', color: '#6d28d9', marginBottom: '12px' }}>Orbit UI 반영 결과 (Cycle 61)</div>
    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '12px', fontSize: '12px', color: '#374151', lineHeight: '1.7' }}>
      <div>
        <div style={{ fontWeight: '700', color: '#7c3aed', marginBottom: '4px' }}>Tooltip 고도화</div>
        shadcn 차트 데이터 레이블, Vercel 컴팩트 액션 힌트(kbd), Linear 단축키 레퍼런스 카드 3개 스토리 추가.
      </div>
      <div>
        <div style={{ fontWeight: '700', color: '#7c3aed', marginBottom: '4px' }}>TextArea 고도화</div>
        shadcn 코멘트 에디터(write/preview), Vercel 환경변수 편집기(parseEnv), Linear 자동저장(debounce) 3개 스토리 추가.
      </div>
      <div>
        <div style={{ fontWeight: '700', color: '#7c3aed', marginBottom: '4px' }}>Template 51: ProductCatalog</div>
        사이드바 카테고리 필터, 가격 Slider, 세일 Toggle, 3열 상품 그리드, LabelBadge 태그, 찜하기 토글.
      </div>
      <div>
        <div style={{ fontWeight: '700', color: '#7c3aed', marginBottom: '4px' }}>다크 모드 패턴 확립</div>
        #09090b 배경 + zinc 팔레트 일관 적용. kbd 단축키 UI, 멀티라인 파서, debounce 자동저장 레시피 정착.
      </div>
    </div>
  </div>
</section>

<section style={{ marginTop: '48px', padding: '32px', background: 'linear-gradient(135deg, #fff1f2 0%, #fff7ed 100%)', borderRadius: '16px', border: '1px solid #fecdd3' }}>
  <h2 style={{ fontSize: '22px', fontWeight: '800', color: '#881337', marginBottom: '8px' }}>Cycle 72 — MUI + Figma Plugin UI 패턴</h2>
  <p style={{ color: '#be123c', fontSize: '14px', marginBottom: '24px', fontWeight: '600' }}>Switch FormControlLabel + CounterBadge 오버레이 + 고밀도 컴팩트 UI 패턴</p>

  <h3 style={{ fontSize: '17px', fontWeight: '700', color: '#334155', marginBottom: '16px' }}>MUI에서 배운 것</h3>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fecdd3' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#be123c', marginBottom: '8px' }}>FormControlLabel 패턴</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        MUI는 Switch + 라벨 + description을 하나의 <code>FormControlLabel</code>로 묶어 전체 행에서 클릭 가능하게 합니다. Orbit UI에서는 <code>label</code> + <code>htmlFor</code> + <code>Switch id</code> 조합으로 동일한 접근성을 구현합니다.
      </p>
    </div>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fecdd3' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#be123c', marginBottom: '8px' }}>AppBar Badge 오버레이</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        MUI Badge는 버튼/아이콘 우상단에 <code>position: absolute</code>로 CounterBadge를 오버레이합니다. 0일 때 숨김, 99+ 오버플로우, 색상 변형(error/warning/default)을 지원합니다.
      </p>
    </div>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fecdd3' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#be123c', marginBottom: '8px' }}>Switch FormGroup 레이아웃</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        MUI FormGroup은 Switch 목록을 수직으로 쌓고 Divider로 구분합니다. 각 항목은 라벨(굵음), 설명(얇음), Switch(우측)로 구성되며 8px 그리드 기반 패딩을 사용합니다.
      </p>
    </div>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fecdd3' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#be123c', marginBottom: '8px' }}>Tabs + Badge 조합</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        MUI Tab에 Badge를 조합하면 탭별 미확인 항목 수를 표시할 수 있습니다. 0인 탭은 배지를 숨기고, 활성 탭 배지는 primary 색상으로 강조합니다.
      </p>
    </div>
  </div>

  <h3 style={{ fontSize: '17px', fontWeight: '700', color: '#334155', marginBottom: '16px' }}>Figma Plugin UI에서 배운 것</h3>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fed7aa' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#c2410c', marginBottom: '8px' }}>다크 배경 고밀도 패널</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        Figma 플러그인 UI는 <code>#2c2c2c</code> 배경에 30px 행 높이로 최대한 많은 정보를 표시합니다. 아이콘 컬러 + 텍스트 색상 변화로 활성 상태를 구분하며 Switch를 우측에 배치합니다.
      </p>
    </div>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fed7aa' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#c2410c', marginBottom: '8px' }}>레이어 패널 카운트 배지</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        Figma 레이어 패널은 그룹/프레임마다 내부 오브젝트 수를 우측에 배치합니다. 색상 점으로 레이어 타입을 구분하고, 선택된 항목은 왼쪽 컬러 보더로 강조합니다.
      </p>
    </div>
  </div>

  <h3 style={{ fontSize: '17px', fontWeight: '700', color: '#334155', marginBottom: '12px' }}>이번 사이클 적용 결과</h3>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
    <div style={{ padding: '12px 16px', borderRadius: '8px', background: '#f8fafc', border: '1px solid #e2e8f0', fontSize: '13px' }}>
      <span style={{ fontWeight: '700', color: '#be123c' }}>Switch +3:</span>
      <span style={{ color: '#475569', marginLeft: '8px' }}>MUI FormControlLabel 알림 설정, Figma 플러그인 뷰 옵션 패널, MUI+Figma 컬럼 가시성 토글</span>
    </div>
    <div style={{ padding: '12px 16px', borderRadius: '8px', background: '#f8fafc', border: '1px solid #e2e8f0', fontSize: '13px' }}>
      <span style={{ fontWeight: '700', color: '#c2410c' }}>CounterBadge +3:</span>
      <span style={{ color: '#475569', marginLeft: '8px' }}>MUI 탭 배지 패턴, Figma 레이어 패널 카운트, MUI+Figma 장바구니 배지 시스템</span>
    </div>
    <div style={{ padding: '12px 16px', borderRadius: '8px', background: '#f8fafc', border: '1px solid #e2e8f0', fontSize: '13px' }}>
      <span style={{ fontWeight: '700', color: '#059669' }}>Template #62:</span>
      <span style={{ color: '#475569', marginLeft: '8px' }}>TeamSettings — 팀 설정 대시보드 (멤버 역할 배지, Switch 알림 토글, 설정 그룹 카운트)</span>
    </div>
  </div>
</section>
