import { Meta, Unstyled } from '@storybook/blocks'

<Meta title='Intro/테마 가이드' />

<Unstyled>
  <style>{`
    [data-orbit-doc] .orbit-code {
      margin: 0;
      border: 1px solid var(--orbit-line);
      border-radius: 12px;
      overflow: hidden;
      background: var(--orbit-surface);
    }
    [data-orbit-doc] .orbit-code-head {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: 16px;
      padding: 9px 16px;
      border-bottom: 1px solid var(--orbit-line);
      background: var(--orbit-surface-sunken);
      font-family: var(--orbit-mono);
      font-size: 11px;
      color: var(--orbit-ink-3);
    }
    [data-orbit-doc] .orbit-code-head .tag {
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      font-size: 10.5px;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-code pre {
      margin: 0;
      padding: 18px 20px;
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      line-height: 1.7;
      color: var(--orbit-ink);
      background: transparent;
      overflow-x: auto;
      white-space: pre;
    }
    [data-orbit-doc] .orbit-twocode {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
      margin-top: 24px;
    }

    [data-orbit-doc] .orbit-mode-preview {
      margin-top: 28px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
    }
    [data-orbit-doc] .orbit-mode-preview .pane {
      border-radius: 14px;
      border: 1px solid var(--orbit-line);
      overflow: hidden;
    }
    [data-orbit-doc] .orbit-mode-preview .pane .head {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 11px 18px;
      border-bottom: 1px solid currentColor;
      font-family: var(--orbit-mono);
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-mode-preview .pane .head .dot {
      width: 14px;
      height: 14px;
      border-radius: 50%;
      border: 1px solid currentColor;
    }
    [data-orbit-doc] .orbit-mode-preview .pane.light {
      background: rgb(243, 245, 247);
      color: rgb(24, 26, 28);
    }
    [data-orbit-doc] .orbit-mode-preview .pane.light .head { border-bottom-color: rgba(24, 26, 28, 0.08); background: rgb(255, 255, 255); color: rgba(24, 26, 28, 0.78); }
    [data-orbit-doc] .orbit-mode-preview .pane.light .head .dot { background: rgb(255, 255, 255); border-color: rgba(24, 26, 28, 0.18); }

    [data-orbit-doc] .orbit-mode-preview .pane.dark {
      background: rgb(21, 23, 25);
      color: rgb(247, 248, 250);
    }
    [data-orbit-doc] .orbit-mode-preview .pane.dark .head { border-bottom-color: rgba(255, 255, 255, 0.08); background: rgb(28, 31, 35); color: rgba(247, 248, 250, 0.78); }
    [data-orbit-doc] .orbit-mode-preview .pane.dark .head .dot { background: rgb(21, 23, 25); border-color: rgba(255, 255, 255, 0.4); }

    [data-orbit-doc] .orbit-mode-preview .stage {
      padding: 22px;
      display: flex;
      flex-direction: column;
      gap: 14px;
    }
    [data-orbit-doc] .orbit-mode-preview .surface {
      padding: 14px 16px;
      border-radius: 10px;
      border: 1px solid currentColor;
    }
    [data-orbit-doc] .orbit-mode-preview .pane.light .surface { background: rgb(255, 255, 255); border-color: rgba(24, 26, 28, 0.08); }
    [data-orbit-doc] .orbit-mode-preview .pane.dark .surface { background: rgb(28, 31, 35); border-color: rgba(255, 255, 255, 0.08); }
    [data-orbit-doc] .orbit-mode-preview .surface .title {
      font-size: 13.5px;
      font-weight: 660;
      letter-spacing: -0.012em;
      margin-bottom: 3px;
    }
    [data-orbit-doc] .orbit-mode-preview .surface .body {
      font-size: 12px;
      opacity: 0.62;
    }
    [data-orbit-doc] .orbit-mode-preview .btns {
      display: flex;
      gap: 8px;
    }
    [data-orbit-doc] .orbit-mode-preview .pane.light .btns .primary {
      flex: 1;
      padding: 10px 14px;
      border-radius: 8px;
      background: rgb(37, 99, 235);
      color: rgb(255, 255, 255);
      font-size: 12.5px;
      font-weight: 660;
      text-align: center;
      letter-spacing: -0.012em;
    }
    [data-orbit-doc] .orbit-mode-preview .pane.dark .btns .primary {
      flex: 1;
      padding: 10px 14px;
      border-radius: 8px;
      background: rgb(96, 165, 250);
      color: rgb(15, 17, 19);
      font-size: 12.5px;
      font-weight: 660;
      text-align: center;
      letter-spacing: -0.012em;
    }
    [data-orbit-doc] .orbit-mode-preview .pane.light .btns .outline {
      flex: 1;
      padding: 10px 14px;
      border-radius: 8px;
      background: transparent;
      color: rgb(24, 26, 28);
      border: 1px solid rgba(24, 26, 28, 0.18);
      font-size: 12.5px;
      font-weight: 660;
      text-align: center;
      letter-spacing: -0.012em;
    }
    [data-orbit-doc] .orbit-mode-preview .pane.dark .btns .outline {
      flex: 1;
      padding: 10px 14px;
      border-radius: 8px;
      background: transparent;
      color: rgb(247, 248, 250);
      border: 1px solid rgba(255, 255, 255, 0.22);
      font-size: 12.5px;
      font-weight: 660;
      text-align: center;
      letter-spacing: -0.012em;
    }
    [data-orbit-doc] .orbit-mode-preview .scale {
      display: flex;
      gap: 6px;
    }
    [data-orbit-doc] .orbit-mode-preview .scale span {
      height: 8px;
      border-radius: 2px;
    }
    [data-orbit-doc] .orbit-mode-preview .pane.light .scale span:nth-child(1) { flex: 3; background: rgb(24, 26, 28); }
    [data-orbit-doc] .orbit-mode-preview .pane.light .scale span:nth-child(2) { flex: 2; background: rgba(24, 26, 28, 0.56); }
    [data-orbit-doc] .orbit-mode-preview .pane.light .scale span:nth-child(3) { flex: 1; background: rgba(24, 26, 28, 0.18); }
    [data-orbit-doc] .orbit-mode-preview .pane.dark .scale span:nth-child(1) { flex: 3; background: rgb(247, 248, 250); }
    [data-orbit-doc] .orbit-mode-preview .pane.dark .scale span:nth-child(2) { flex: 2; background: rgba(247, 248, 250, 0.56); }
    [data-orbit-doc] .orbit-mode-preview .pane.dark .scale span:nth-child(3) { flex: 1; background: rgba(255, 255, 255, 0.18); }

    [data-orbit-doc] .orbit-props {
      margin-top: 20px;
      width: 100%;
      border-collapse: separate;
      border-spacing: 0;
      font-size: 13px;
    }
    [data-orbit-doc] .orbit-props thead th {
      padding: 12px 16px;
      border-top: 1px solid var(--orbit-line-2);
      border-bottom: 1px solid var(--orbit-line-2);
      font-family: var(--orbit-mono);
      font-size: 11px;
      font-weight: 600;
      color: var(--orbit-ink-4);
      text-align: left;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-props tbody td {
      padding: 14px 16px;
      border-bottom: 1px solid var(--orbit-line);
      vertical-align: top;
      color: var(--orbit-ink-2);
      line-height: 1.55;
    }
    [data-orbit-doc] .orbit-props tbody tr:last-child td {
      border-bottom: 1px solid var(--orbit-line-2);
    }
    [data-orbit-doc] .orbit-props code {
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-props .prop code {
      font-weight: 600;
      color: var(--orbit-accent);
    }
    [data-orbit-doc] .orbit-props .vals {
      font-family: var(--orbit-mono);
      font-size: 12px;
      color: var(--orbit-ink-3);
    }
    [data-orbit-doc] .orbit-props .def {
      font-family: var(--orbit-mono);
      font-size: 12px;
      color: var(--orbit-ink-4);
    }

    [data-orbit-doc] .orbit-note {
      margin: 16px 0 0;
      padding: 14px 18px;
      border: 1px solid var(--orbit-line);
      border-radius: 10px;
      background: var(--orbit-surface);
      font-size: 13.5px;
      line-height: 1.6;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-note strong {
      display: block;
      margin-bottom: 4px;
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      font-weight: 700;
      color: var(--orbit-ink-3);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-note code {
      padding: 1px 6px;
      border-radius: 5px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink-2);
    }

    [data-orbit-doc] .orbit-cards {
      margin-top: 24px;
      display: grid;
      grid-template-columns: 200px minmax(0, 1fr);
      gap: 24px;
      row-gap: 0;
    }
    [data-orbit-doc] .orbit-cards .row {
      display: contents;
    }
    [data-orbit-doc] .orbit-cards .key {
      padding: 18px 0;
      border-top: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 11.5px;
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      letter-spacing: 0.06em;
      letter-spacing: -0.005em;
    }
    [data-orbit-doc] .orbit-cards .key strong {
      display: block;
      margin-top: 4px;
      font-family: var(--orbit-display);
      font-size: 16px;
      font-weight: 660;
      color: var(--orbit-ink);
      letter-spacing: -0.012em;
    }
    [data-orbit-doc] .orbit-cards .val {
      padding: 18px 0;
      border-top: 1px solid var(--orbit-line);
      font-size: 14px;
      color: var(--orbit-ink-2);
      line-height: 1.6;
    }
    [data-orbit-doc] .orbit-cards .val code {
      padding: 1px 6px;
      border-radius: 5px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink-2);
    }

    @media (max-width: 760px) {
      [data-orbit-doc] .orbit-mode-preview { grid-template-columns: 1fr; }
      [data-orbit-doc] .orbit-twocode { grid-template-columns: 1fr; }
      [data-orbit-doc] .orbit-cards { grid-template-columns: 1fr; gap: 4px 0; }
      [data-orbit-doc] .orbit-cards .key { padding: 14px 0 4px; }
      [data-orbit-doc] .orbit-cards .val { padding: 0 0 14px; border-top: 0; }
      [data-orbit-doc] .orbit-props thead th:nth-child(3),
      [data-orbit-doc] .orbit-props tbody td:nth-child(3) { display: none; }
    }
  `}</style>

  <div data-orbit-doc>
    <div className='orbit-wrap'>

      {/* HEADER */}
      <header className='orbit-page-head'>
        <div>
          <div className='orbit-eyebrow'><span className='num'>01</span>Theme Guide</div>
          <h1 className='orbit-h1'>테마 가이드</h1>
          <p className='orbit-lede'>
            Light·Dark 두 모드는 동등하게 발행됩니다. <code style={{ fontFamily: 'var(--orbit-mono)', fontSize: '0.9em', padding: '1px 6px', borderRadius: 5, background: 'var(--orbit-surface-sunken)', border: '1px solid var(--orbit-line)' }}>EclipseProvider</code>가 토큰 클래스, Portal, 모션 컨텍스트를 한 번에 켭니다.
          </p>
        </div>
        <div className='orbit-page-meta'>
          <div><strong>2</strong> modes</div>
          <div>SSR · CSR</div>
          <div>token override</div>
        </div>
      </header>

      {/* MODE PREVIEW */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>02</span>Light & Dark</div>
            <h2>두 모드, 같은 vocabulary.</h2>
            <p>같은 컴포넌트, 같은 토큰 이름. 매핑만 모드에 따라 달라집니다. Storybook 툴바에서 즉시 전환.</p>
          </div>
        </div>

        <div className='orbit-mode-preview'>
          <div className='pane light'>
            <div className='head'>
              <span>Light</span>
              <span className='dot' />
            </div>
            <div className='stage'>
              <div className='surface'>
                <div className='title'>Surface Container</div>
                <div className='body'>카드·패널의 기본 elevated 배경</div>
              </div>
              <div className='btns'>
                <div className='primary'>Primary</div>
                <div className='outline'>Outline</div>
              </div>
              <div className='scale'>
                <span /><span /><span />
              </div>
            </div>
          </div>
          <div className='pane dark'>
            <div className='head'>
              <span>Dark</span>
              <span className='dot' />
            </div>
            <div className='stage'>
              <div className='surface'>
                <div className='title'>Surface Container</div>
                <div className='body'>카드·패널의 기본 elevated 배경</div>
              </div>
              <div className='btns'>
                <div className='primary'>Primary</div>
                <div className='outline'>Outline</div>
              </div>
              <div className='scale'>
                <span /><span /><span />
              </div>
            </div>
          </div>
        </div>

        <aside className='orbit-note'>
          <strong>How tokens flip</strong>
          <code>systemMainPrimary</code>은 light에서 <code>primary5 (#2563EB)</code>로, dark에서 <code>primary4 (#60A5FA)</code>로 매핑됩니다.
          소비자가 작성하는 코드는 한 줄도 바뀌지 않습니다.
        </aside>
      </section>

      {/* PROVIDER */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>03</span>Provider</div>
            <h2>EclipseProvider 한 줄.</h2>
            <p>ThemeProvider, Portal, LottieProvider, ReusableSVG가 내부에서 합쳐집니다.</p>
          </div>
        </div>

        <div style={{ marginTop: 24 }}>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>TSX</span>
              <span>App.tsx</span>
            </div>
            <pre>{`import { EclipseProvider } from '@heejun-com/theme-eclipse'
import '@heejun-com/theme-eclipse/style.css'

export function App() {
  return (
    <EclipseProvider mode='light' platform='pc' baseTextSize='medium'>
      <YourApp />
    </EclipseProvider>
  )
}`}</pre>
          </div>
        </div>

        <table className='orbit-props'>
          <thead>
            <tr>
              <th style={{ width: '18%' }}>Prop</th>
              <th style={{ width: '34%' }}>Type</th>
              <th style={{ width: '14%' }}>Default</th>
              <th>설명</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td className='prop'><code>mode</code></td>
              <td className='vals'>'light' | 'dark'</td>
              <td className='def'>'light'</td>
              <td>전체 컬러 토큰 매핑을 결정. 외부 상태에서 토글하면 즉시 반영.</td>
            </tr>
            <tr>
              <td className='prop'><code>platform</code></td>
              <td className='vals'>'mobile' | 'pc'</td>
              <td className='def'>'mobile'</td>
              <td>일부 컴포넌트의 hover·touch 분기와 밀도 조정에 영향.</td>
            </tr>
            <tr>
              <td className='prop'><code>baseTextSize</code></td>
              <td className='vals'>'small' | 'medium' | 'large'</td>
              <td className='def'>'medium'</td>
              <td>전체 타이포 스케일 기준. 모든 텍스트가 일관 비율로 조정.</td>
            </tr>
          </tbody>
        </table>
      </section>

      {/* RUNTIME SWITCHING */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>04</span>Runtime Switching</div>
            <h2>모드는 외부 상태에 묶습니다.</h2>
            <p>useState·zustand·redux 어디든 OK. mode 프롭에 상태값을 그대로 흘려보내면 끝.</p>
          </div>
        </div>

        <div className='orbit-twocode'>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>useState</span>
              <span>basic toggle</span>
            </div>
            <pre>{`import { useState } from 'react'
import { EclipseProvider } from '@heejun-com/theme-eclipse'

export function App() {
  const [mode, setMode] = useState<'light' | 'dark'>('light')

  return (
    <EclipseProvider mode={mode}>
      <YourApp onToggleTheme={() =>
        setMode((m) => m === 'light' ? 'dark' : 'light')
      } />
    </EclipseProvider>
  )
}`}</pre>
          </div>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>matchMedia</span>
              <span>system preference</span>
            </div>
            <pre>{`import { useEffect, useState } from 'react'

export function useSystemTheme() {
  const [mode, setMode] = useState<'light' | 'dark'>(() =>
    matchMedia('(prefers-color-scheme: dark)').matches
      ? 'dark' : 'light'
  )

  useEffect(() => {
    const mq = matchMedia('(prefers-color-scheme: dark)')
    const handler = (e: MediaQueryListEvent) =>
      setMode(e.matches ? 'dark' : 'light')
    mq.addEventListener('change', handler)
    return () => mq.removeEventListener('change', handler)
  }, [])

  return mode
}`}</pre>
          </div>
        </div>
      </section>

      {/* SSR */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>05</span>SSR</div>
            <h2>첫 페인트 깜빡임 방지.</h2>
            <p><code>getTheme()</code>이 서버에서 클래스 문자열을 반환합니다. <code>html</code>에 박아두면 하이드레이션 전부터 토큰이 적용.</p>
          </div>
        </div>

        <div className='orbit-twocode'>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>Next.js Pages Router</span>
              <span>_document.tsx</span>
            </div>
            <pre>{`import { getTheme } from '@heejun-com/theme-eclipse/server'

export default function Document() {
  return (
    <Html lang='ko'>
      <Head />
      <body className={getTheme()}>
        <Main />
        <NextScript />
      </body>
    </Html>
  )
}`}</pre>
          </div>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>Next.js App Router</span>
              <span>app/layout.tsx</span>
            </div>
            <pre>{`import { getTheme } from '@heejun-com/theme-eclipse/server'
import '@heejun-com/theme-eclipse/style.css'

export default function RootLayout({
  children,
}: { children: React.ReactNode }) {
  return (
    <html lang='ko'>
      <body className={getTheme()}>
        {children}
      </body>
    </html>
  )
}`}</pre>
          </div>
        </div>

        <aside className='orbit-note'>
          <strong>Why this matters</strong>
          하이드레이션 전에 테마 클래스가 박혀 있어야 토큰이 즉시 적용됩니다. 그렇지 않으면 첫 페인트에서 light로 그렸다가
          dark로 점프하는 깜빡임이 발생합니다.
        </aside>
      </section>

      {/* THEME PROP */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>06</span>theme Prop</div>
            <h2>컴포넌트 단위 surgical 오버라이드.</h2>
            <p>CSS 작성 없이 TypeScript로 단일 컴포넌트의 토큰만 갈아끼웁니다.</p>
          </div>
        </div>

        <div style={{ marginTop: 24 }}>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>TSX</span>
              <span>특정 페이지·CTA에 한정된 오버라이드</span>
            </div>
            <pre>{`import { SolidButton } from '@heejun-com/theme-eclipse'

// 결제 완료 페이지의 단일 CTA만 success green으로
<SolidButton
  color='primary'
  size='large'
  theme={{
    enabledFillColor: 'var(--ref-color-green5)',
    hoveredFillColor: 'var(--ref-color-green6)',
    focusedBorderColor: 'var(--ref-color-green6)',
  }}
>
  <SolidButton.Center>예약하기</SolidButton.Center>
</SolidButton>`}</pre>
          </div>
        </div>

        <aside className='orbit-note'>
          <strong>When to reach for theme prop</strong>
          One Voice Rule의 예외 케이스 (예: 마케팅 페이지의 단일 강조 액션). 전체 디자인 시스템 컬러를 바꾸고 싶다면
          글로벌 CSS 변수 오버라이드를 사용하세요.
        </aside>
      </section>

      {/* GLOBAL OVERRIDE */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>07</span>Global Token Override</div>
            <h2>CSS 변수로 시스템 전체 톤 조정.</h2>
            <p>소비자 브랜드 컬러로 갈아끼우고 싶다면 reference·semantic 변수를 한 번 덮어쓰면 모든 컴포넌트가 따라옵니다.</p>
          </div>
        </div>

        <div className='orbit-cards'>
          <div className='row'>
            <div className='key'>Reference<strong>--ref-color-*</strong></div>
            <div className='val'>
              원시 컬러·간격·라운드·그림자 스케일. 통째로 갈아끼우는 가장 강력한 진입점.
              브랜드 컬러를 시스템의 primary로 매핑할 때 사용. <code>--ref-color-primary5</code>를 덮으면
              모든 primary 액션 컬러가 갱신됩니다.
            </div>
          </div>
          <div className='row'>
            <div className='key'>Semantic<strong>--sem-eclipse-color-*</strong></div>
            <div className='val'>
              역할 기반 토큰. <code>systemMainPrimary</code>, <code>foregroundPrimary</code> 같은 의미 이름.
              특정 역할만 분리해서 바꾸고 싶을 때 (e.g. <code>positivePrimary</code>만 다른 green으로) 이 레이어에서.
            </div>
          </div>
          <div className='row'>
            <div className='key'>Component<strong>--com-eclipse-*</strong></div>
            <div className='val'>
              컴포넌트 specific 토큰. <code>--com-eclipse-button-enabledFillColor</code>처럼 한 컴포넌트만 바꿀 때.
              전체 시스템에 영향 없이 단일 컴포넌트의 디폴트만 조정.
            </div>
          </div>
        </div>

        <div style={{ marginTop: 24 }}>
          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>CSS</span>
              <span>app.css</span>
            </div>
            <pre>{`/* 브랜드 컬러로 primary 토큰 덮어쓰기 */
.eclipse-light {
  --ref-color-primary5: #FF4500;  /* 브랜드 오렌지 */
  --ref-color-primary6: #DC3F00;  /* hover */
  --ref-color-primary1: #FFE5D6;  /* tint */
}

.eclipse-dark {
  --ref-color-primary5: #FF7A40;
  --ref-color-primary6: #FF9468;
  --ref-color-primary1: #3D1F10;
}

/* 또는 의미 레이어에서 한 번에 */
.eclipse-light {
  --sem-eclipse-color-systemMainPrimary: var(--my-brand-color);
}`}</pre>
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer className='orbit-foot'>
        <div>2 modes · 3 token tiers · SSR-safe · theme prop</div>
        <div>
          <a href='?path=/docs/intro-디자인토큰--docs'>← 디자인 토큰</a>
          <span style={{ marginLeft: 16 }} />
          <a href='?path=/docs/intro-접근성--docs'>접근성 →</a>
        </div>
      </footer>

    </div>
  </div>
</Unstyled>
