import { Meta, Unstyled } from '@storybook/blocks'

<Meta title="Intro/커스터마이제이션 가이드" />

<Unstyled>
  <div style={{
    maxWidth: '960px',
    margin: '0 auto',
    fontFamily: '"Pretendard Variable", "Pretendard", -apple-system, BlinkMacSystemFont, system-ui, sans-serif',
  }}>

    {/* Header */}
    <div style={{ marginBottom: '48px' }}>
      <div style={{ fontSize: '12px', fontWeight: '700', color: '#6366f1', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Customization Guide</div>
      <h1 style={{ fontSize: '36px', fontWeight: '800', color: '#1e293b', letterSpacing: '-0.03em', margin: '0 0 12px' }}>커스터마이제이션 가이드</h1>
      <p style={{ fontSize: '16px', color: '#64748b', lineHeight: '1.7', margin: 0 }}>
        MUI의 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '14px' }}>createTheme</code>처럼,
        Orbit UI도 다양한 방법으로 브랜드 색상과 토큰을 커스터마이즈할 수 있습니다.
        프로젝트 규모와 요구사항에 맞는 방법을 선택하세요.
      </p>
    </div>

    {/* MUI vs Orbit UI 비교 */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>MUI createTheme vs Orbit UI 커스텀</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px' }}>
        MUI는 JS 객체 기반의 단일 진입점(createTheme)을 제공하는 반면, Orbit UI는 CSS 변수 기반의 계층적 토큰 시스템을 사용합니다.
        두 방식 모두 런타임 테마 전환을 지원하지만 구현 방식이 다릅니다.
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}>
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '12px 20px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', gap: '8px' }}>
            <span style={{ fontSize: '13px', fontWeight: '700', color: '#475569' }}>MUI createTheme</span>
            <span style={{ fontSize: '11px', color: '#94a3b8', background: '#f1f5f9', padding: '2px 6px', borderRadius: '4px' }}>JS 객체 기반</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { createTheme, ThemeProvider } from '@mui/material'

const brandTheme = createTheme({
  palette: {
    primary: {
      main: '#0ea5e9',
      light: '#38bdf8',
      dark: '#0284c7',
    },
    background: {
      default: '#fafafa',
      paper: '#ffffff',
    },
  },
  shape: {
    borderRadius: 8,
  },
  typography: {
    fontFamily: 'Pretendard',
  },
})

// 사용:
<ThemeProvider theme={brandTheme}>
  <App />
</ThemeProvider>`}
          </pre>
        </div>

        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '12px 20px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', gap: '8px' }}>
            <span style={{ fontSize: '13px', fontWeight: '700', color: '#475569' }}>Orbit UI CSS 변수 방식</span>
            <span style={{ fontSize: '11px', color: '#6366f1', background: 'rgba(99,102,241,0.08)', padding: '2px 6px', borderRadius: '4px' }}>CSS 변수 기반</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* globals.css */
:root {
  --sem-eclipse-color-fillPrimary: #0ea5e9;
  --sem-eclipse-color-fillPrimaryHovered: #38bdf8;
  --sem-eclipse-color-fillPrimaryPressed: #0284c7;
  --sem-eclipse-color-backgroundPrimary: #fafafa;
  --sem-eclipse-color-surfaceContainer: #f0f9ff;
}

[data-theme="dark"] {
  --sem-eclipse-color-fillPrimary: #38bdf8;
  --sem-eclipse-color-backgroundPrimary: #0c1a2e;
}

// 사용:
import '@heejun-com/theme-eclipse/style.css'
import './globals.css'
<EclipseProvider>
  <App />
</EclipseProvider>`}
          </pre>
        </div>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px' }}>
        {[
          { label: 'MUI', pros: ['단일 진입점 (createTheme)', 'TypeScript 자동완성 지원', 'sx prop으로 인라인 오버라이드'], cons: ['JS 번들 크기 증가', 'SSR 하이드레이션 불일치 주의', 'CSS specificity 충돌 가능'] },
          { label: 'Orbit UI', pros: ['CSS 변수 기반으로 SSR 안전', '런타임 JS 없이 테마 전환 가능', 'Tailwind config와 자연스럽게 통합'], cons: ['CSS 변수명을 별도로 파악해야 함', 'TypeScript 자동완성 없음 (CSS 파일)', 'DevTools에서 변수 추적 필요'] },
        ].map((item) => (
          <div key={item.label} style={{ padding: '16px 20px', borderRadius: '12px', border: '1px solid #e2e8f0' }}>
            <div style={{ fontSize: '13px', fontWeight: '700', color: '#1e293b', marginBottom: '10px' }}>{item.label}</div>
            <div style={{ marginBottom: '8px' }}>
              <div style={{ fontSize: '11px', fontWeight: '600', color: '#10b981', marginBottom: '4px', textTransform: 'uppercase', letterSpacing: '0.05em' }}>장점</div>
              {item.pros.map((p, i) => (
                <div key={i} style={{ fontSize: '12px', color: '#475569', padding: '2px 0', display: 'flex', gap: '6px' }}>
                  <span style={{ color: '#10b981' }}>+</span> {p}
                </div>
              ))}
            </div>
            <div>
              <div style={{ fontSize: '11px', fontWeight: '600', color: '#ef4444', marginBottom: '4px', textTransform: 'uppercase', letterSpacing: '0.05em' }}>단점</div>
              {item.cons.map((c, i) => (
                <div key={i} style={{ fontSize: '12px', color: '#475569', padding: '2px 0', display: 'flex', gap: '6px' }}>
                  <span style={{ color: '#ef4444' }}>-</span> {c}
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* 방법 1: CSS 변수 Override */}
    <section style={{ marginBottom: '56px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '8px' }}>
        <div style={{ width: '28px', height: '28px', borderRadius: '8px', background: 'linear-gradient(135deg, #6366f1, #8b5cf6)', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#fff', fontSize: '13px', fontWeight: '800', flexShrink: 0 }}>1</div>
        <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', margin: 0 }}>CSS 변수 직접 Override</h2>
        <span style={{ fontSize: '11px', color: '#10b981', background: 'rgba(16,185,129,0.08)', border: '1px solid rgba(16,185,129,0.2)', padding: '2px 8px', borderRadius: '100px', fontWeight: 600 }}>가장 간단</span>
      </div>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px', marginLeft: '40px' }}>
        글로벌 CSS 파일에서 Orbit UI의 CSS 변수를 직접 재정의합니다.
        빌드 과정 없이 즉시 적용되며, 다크모드도 자연스럽게 지원됩니다.
      </p>

      <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden', marginBottom: '16px' }}>
        <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
          globals.css 또는 custom-brand.css
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* 브랜드 색상 (Sky Blue 계열) */
:root {
  /* Primary 색상 */
  --sem-eclipse-color-fillPrimary: #0ea5e9;
  --sem-eclipse-color-fillPrimaryHovered: #38bdf8;
  --sem-eclipse-color-fillPrimaryPressed: #0284c7;

  /* 배경 및 서피스 */
  --sem-eclipse-color-backgroundPrimary: #f0f9ff;
  --sem-eclipse-color-surfaceContainer: #e0f2fe;
  --sem-eclipse-color-surfaceContainerHigh: #bae6fd;

  /* 버튼 반경 (더 각진 스타일) */
  --comp-eclipse-solidButton-borderRadius: 6px;
  --comp-eclipse-outlineButton-borderRadius: 6px;
}

/* 다크모드 override */
[data-theme="dark"] {
  --sem-eclipse-color-fillPrimary: #38bdf8;
  --sem-eclipse-color-backgroundPrimary: #0c1a2e;
  --sem-eclipse-color-surfaceContainer: #0f2744;
}`}
        </pre>
      </div>

      <div style={{ padding: '14px 18px', borderRadius: '10px', background: 'rgba(99,102,241,0.04)', border: '1px solid rgba(99,102,241,0.15)' }}>
        <p style={{ margin: 0, fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
          <strong style={{ color: '#1e293b' }}>토큰 네임스페이스:</strong>
          {' '}시맨틱 토큰은 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '1px 5px', borderRadius: '4px', fontSize: '12px' }}>{'--sem-eclipse-color-*'}</code>,
          컴포넌트 토큰은 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '1px 5px', borderRadius: '4px', fontSize: '12px' }}>{'--comp-eclipse-*'}</code> 패턴을 따릅니다.
          DevTools의 Computed 탭에서 사용 중인 변수를 확인하세요.
        </p>
      </div>
    </section>

    {/* 방법 2: EclipseProvider theme prop */}
    <section style={{ marginBottom: '56px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '8px' }}>
        <div style={{ width: '28px', height: '28px', borderRadius: '8px', background: 'linear-gradient(135deg, #8b5cf6, #ec4899)', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#fff', fontSize: '13px', fontWeight: '800', flexShrink: 0 }}>2</div>
        <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', margin: 0 }}>theme prop으로 컴포넌트 토큰 Override</h2>
        <span style={{ fontSize: '11px', color: '#f59e0b', background: 'rgba(245,158,11,0.08)', border: '1px solid rgba(245,158,11,0.2)', padding: '2px 8px', borderRadius: '100px', fontWeight: 600 }}>컴포넌트 단위</span>
      </div>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px', marginLeft: '40px' }}>
        특정 컴포넌트의 특정 토큰만 오버라이드할 때 사용합니다. 글로벌 변경 없이 일부 컴포넌트만 다른 스타일을 적용할 수 있습니다.
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            SolidButton 토큰 Override
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { SolidButton } from '@heejun-com/theme-eclipse'

// 브랜드 Sky Blue 버튼
<SolidButton
  color="primary"
  size="medium"
  theme={{
    '--comp-fill': '#0ea5e9',
    '--comp-fill-hovered': '#38bdf8',
    '--comp-fill-pressed': '#0284c7',
    '--comp-on-fill': '#ffffff',
    '--comp-border-radius': '6px',
  }}
>
  <SolidButton.Center>브랜드 버튼</SolidButton.Center>
</SolidButton>`}
          </pre>
        </div>
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            FloatingTextField 토큰 Override
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { FloatingTextField } from '@heejun-com/theme-eclipse'

// 브랜드 색상 입력 필드
<FloatingTextField
  placeholder="이메일 주소"
  theme={{
    '--comp-border-focused': '#0ea5e9',
    '--comp-label-focused': '#0ea5e9',
    '--comp-border-radius': '6px',
  }}
/>

// 재사용 가능한 브랜드 컴포넌트 추출
const BrandTextField = (props) => (
  <FloatingTextField
    {...props}
    theme={{
      '--comp-border-focused': '#0ea5e9',
      '--comp-label-focused': '#0ea5e9',
    }}
  />
)`}
          </pre>
        </div>
      </div>
    </section>

    {/* 방법 3: vanilla-extract createTheme */}
    <section style={{ marginBottom: '56px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '8px' }}>
        <div style={{ width: '28px', height: '28px', borderRadius: '8px', background: 'linear-gradient(135deg, #10b981, #059669)', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#fff', fontSize: '13px', fontWeight: '800', flexShrink: 0 }}>3</div>
        <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', margin: 0 }}>vanilla-extract createTheme</h2>
        <span style={{ fontSize: '11px', color: '#6366f1', background: 'rgba(99,102,241,0.08)', border: '1px solid rgba(99,102,241,0.2)', padding: '2px 8px', borderRadius: '100px', fontWeight: 600 }}>완전한 테마</span>
      </div>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px', marginLeft: '40px' }}>
        Eclipse vars contract를 기반으로 완전히 새로운 테마 클래스를 생성합니다.
        MUI의 createTheme과 가장 유사한 방식으로, 빌드 시점에 타입 안전하게 테마를 정의합니다.
      </p>

      <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden', marginBottom: '16px' }}>
        <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
          my-brand-theme.css.ts
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`// my-brand-theme.css.ts
import { createTheme } from '@vanilla-extract/css'
import { vars } from '@heejun-com/theme-eclipse/vars'

// Eclipse vars contract를 기반으로 Sky Blue 브랜드 테마 생성
export const skyBlueLightTheme = createTheme(vars, {
  sem: {
    color: {
      fillPrimary: '#0ea5e9',
      fillPrimaryHovered: '#38bdf8',
      fillPrimaryPressed: '#0284c7',
      backgroundPrimary: '#f0f9ff',
      surfaceContainer: '#e0f2fe',
      foregroundPrimary: '#0c4a6e',
      foregroundSecondary: '#0369a1',
    },
  },
})

export const skyBlueDarkTheme = createTheme(vars, {
  sem: {
    color: {
      fillPrimary: '#38bdf8',
      fillPrimaryHovered: '#7dd3fc',
      fillPrimaryPressed: '#0ea5e9',
      backgroundPrimary: '#0c1a2e',
      surfaceContainer: '#0f2744',
      foregroundPrimary: '#e0f2fe',
      foregroundSecondary: '#bae6fd',
    },
  },
})

// 사용:
// <EclipseProvider themeClass={skyBlueLightTheme}>
//   <App />
// </EclipseProvider>`}
        </pre>
      </div>
    </section>

    {/* 방법 4: Tailwind Config 통합 */}
    <section style={{ marginBottom: '56px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '8px' }}>
        <div style={{ width: '28px', height: '28px', borderRadius: '8px', background: 'linear-gradient(135deg, #f59e0b, #d97706)', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#fff', fontSize: '13px', fontWeight: '800', flexShrink: 0 }}>4</div>
        <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', margin: 0 }}>Tailwind Config 통합</h2>
        <span style={{ fontSize: '11px', color: '#f59e0b', background: 'rgba(245,158,11,0.08)', border: '1px solid rgba(245,158,11,0.2)', padding: '2px 8px', borderRadius: '100px', fontWeight: 600 }}>유틸리티 클래스</span>
      </div>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '20px', marginLeft: '40px' }}>
        Orbit UI의 CSS 변수를 Tailwind CSS 설정에 연결하면, 유틸리티 클래스에서도 동일한 브랜드 토큰을 사용할 수 있습니다.
        vanilla-extract 없이 Tailwind만으로 일관된 색상 시스템을 구축합니다.
      </p>

      <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
        <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
          tailwind.config.ts
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '12px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`// tailwind.config.ts
import type { Config } from 'tailwindcss'

const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        // Orbit UI 시맨틱 토큰과 연결
        brand: {
          primary: 'var(--sem-eclipse-color-fillPrimary)',
          hovered: 'var(--sem-eclipse-color-fillPrimaryHovered)',
          pressed: 'var(--sem-eclipse-color-fillPrimaryPressed)',
        },
        surface: {
          DEFAULT: 'var(--sem-eclipse-color-backgroundPrimary)',
          container: 'var(--sem-eclipse-color-surfaceContainer)',
        },
        fg: {
          primary: 'var(--sem-eclipse-color-foregroundPrimary)',
          secondary: 'var(--sem-eclipse-color-foregroundSecondary)',
          muted: 'var(--sem-eclipse-color-foregroundTertiary)',
        },
        border: {
          DEFAULT: 'var(--sem-eclipse-color-borderPrimary)',
        },
      },
    },
  },
}

export default config

// 사용 예:
// <div className="bg-surface-container text-fg-primary border border-border">
//   <button className="bg-brand-primary hover:bg-brand-hovered text-white">
//     브랜드 버튼
//   </button>
// </div>`}
        </pre>
      </div>
    </section>

    {/* 실전 예제: 블루 기반 브랜드 테마 */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>실전 예제: Sky Blue 브랜드 테마</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '24px' }}>
        위의 방법 1(CSS 변수 override)을 사용한 Sky Blue 브랜드 테마의 완전한 설정 예제입니다.
        Next.js 13+ App Router 기준으로 작성되었습니다.
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>
        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            app/brand-theme.css
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`/* Sky Blue 브랜드 테마 */
:root {
  /* Primary */
  --sem-eclipse-color-fillPrimary: #0ea5e9;
  --sem-eclipse-color-fillPrimaryHovered: #38bdf8;
  --sem-eclipse-color-fillPrimaryPressed: #0284c7;
  --sem-eclipse-color-fillPrimarySubtle: #e0f2fe;

  /* Text */
  --sem-eclipse-color-foregroundPrimary: #0c4a6e;
  --sem-eclipse-color-foregroundSecondary: #0369a1;

  /* Background */
  --sem-eclipse-color-backgroundPrimary: #f0f9ff;
  --sem-eclipse-color-surfaceContainer: #e0f2fe;

  /* Borders */
  --sem-eclipse-color-borderPrimary: #bae6fd;

  /* Button shapes */
  --comp-eclipse-solidButton-borderRadius: 8px;
  --comp-eclipse-outlineButton-borderRadius: 8px;
  --comp-eclipse-ghostButton-borderRadius: 8px;
}

[data-theme="dark"] {
  --sem-eclipse-color-fillPrimary: #38bdf8;
  --sem-eclipse-color-backgroundPrimary: #0c1a2e;
  --sem-eclipse-color-surfaceContainer: #0f2744;
  --sem-eclipse-color-borderPrimary: #1e3a5f;
}`}
          </pre>
        </div>

        <div style={{ borderRadius: '16px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', fontSize: '12px', fontWeight: '600', color: '#475569' }}>
            app/layout.tsx
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import '@heejun-com/theme-eclipse/style.css'
import './brand-theme.css'  // 브랜드 override
import { EclipseProvider } from '@heejun-com/theme-eclipse'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ko">
      <body>
        <EclipseProvider mode="light" platform="pc">
          {children}
        </EclipseProvider>
      </body>
    </html>
  )
}

// 결과:
// - 모든 SolidButton이 Sky Blue로 변경
// - FloatingTextField focus 시 Sky Blue 테두리
// - Progress, Loading 등 모든 primary 요소 반영`}
          </pre>
        </div>
      </div>
    </section>

    {/* 권장 사항 */}
    <section style={{ marginBottom: '40px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '20px' }}>언제 어떤 방법을 선택할까?</h2>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
        {[
          {
            scenario: '브랜드 색상 1-2가지만 변경',
            method: '방법 1 (CSS 변수 Override)',
            color: '#10b981',
            detail: '글로벌 CSS에 --sem-eclipse-color-fillPrimary만 바꿔도 버튼, 포커스, 프로그레스 등 전체 반영',
          },
          {
            scenario: '특정 페이지에서만 다른 스타일',
            method: '방법 2 (theme prop)',
            color: '#f59e0b',
            detail: '해당 컴포넌트 인스턴스에만 theme prop 전달. 글로벌 영향 없음',
          },
          {
            scenario: '완전히 다른 두 가지 테마 (e.g. 관리자 / 고객)',
            method: '방법 3 (vanilla-extract createTheme)',
            color: '#6366f1',
            detail: 'createTheme으로 두 테마 클래스 생성 후 EclipseProvider.themeClass로 동적 전환',
          },
          {
            scenario: 'Tailwind 프로젝트에서 일관된 색상 사용',
            method: '방법 4 (Tailwind Config 통합)',
            color: '#f97316',
            detail: 'CSS 변수를 Tailwind 색상에 연결. text-brand-primary, bg-surface-container 등 유틸리티 클래스로 활용',
          },
        ].map((item) => (
          <div key={item.scenario} style={{
            padding: '16px 20px', borderRadius: '12px', border: '1px solid #e2e8f0',
            display: 'flex', gap: '16px', alignItems: 'flex-start',
          }}>
            <div style={{
              width: '8px', height: '8px', borderRadius: '50%',
              background: item.color, flexShrink: 0, marginTop: '6px',
            }} />
            <div style={{ flex: 1 }}>
              <div style={{ fontSize: '14px', fontWeight: '600', color: '#1e293b', marginBottom: '4px' }}>{item.scenario}</div>
              <div style={{ fontSize: '12px', color: item.color, fontWeight: '600', marginBottom: '4px' }}>{item.method}</div>
              <div style={{ fontSize: '12px', color: '#64748b' }}>{item.detail}</div>
            </div>
          </div>
        ))}
      </div>
    </section>

    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>Tailwind UI & Apple HIG 벤치마크 패턴</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.6', marginBottom: '24px' }}>Tailwind UI의 컴포넌트 조합 방식과 Apple HIG의 모바일 UX 원칙에서 학습한 커스터마이징 패턴을 정리합니다.</p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '20px', marginBottom: '24px' }}>
        <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '12px 16px', background: 'linear-gradient(135deg, #0369a1, #0284c7)', color: '#fff' }}>
            <div style={{ fontSize: '14px', fontWeight: '800' }}>Tailwind UI 핵심 패턴</div>
          </div>
          <div style={{ padding: '16px', display: 'flex', flexDirection: 'column', gap: '12px' }}>
            <div>
              <div style={{ fontSize: '12px', fontWeight: '700', color: '#0369a1', marginBottom: '4px' }}>1. 프리셋 + 캘린더 조합</div>
              <div style={{ fontSize: '12px', color: '#374151', lineHeight: '1.7' }}>
                DatePicker 우측 캘린더에 좌측 프리셋 사이드바를 추가. "오늘", "이번 달 말일" 등 빠른 선택을 제공합니다.
                Orbit UI의 Calendar + useState로 동일하게 구현 가능합니다.
              </div>
            </div>
            <div>
              <div style={{ fontSize: '12px', fontWeight: '700', color: '#0369a1', marginBottom: '4px' }}>2. 언더라인 탭 + 카운트 뱃지</div>
              <div style={{ fontSize: '12px', color: '#374151', lineHeight: '1.7' }}>
                탭 선택 표시를 하단 2px 언더라인으로 표현. 카운트 뱃지는 활성 탭에서 브랜드 색상, 비활성은 회색 처리.
              </div>
            </div>
            <div>
              <div style={{ fontSize: '12px', fontWeight: '700', color: '#0369a1', marginBottom: '4px' }}>3. 2단계 예약 UX</div>
              <div style={{ fontSize: '12px', color: '#374151', lineHeight: '1.7' }}>
                날짜 선택 → 시간 슬롯 선택 → 확정 버튼의 순차적 단계. 각 단계 완료 전까지 다음 단계 CTA 비활성화.
              </div>
            </div>
          </div>
        </div>

        <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '12px 16px', background: 'linear-gradient(135deg, #374151, #1f2937)', color: '#fff' }}>
            <div style={{ fontSize: '14px', fontWeight: '800' }}>Apple HIG 핵심 원칙</div>
          </div>
          <div style={{ padding: '16px', display: 'flex', flexDirection: 'column', gap: '12px' }}>
            <div>
              <div style={{ fontSize: '12px', fontWeight: '700', color: '#374151', marginBottom: '4px' }}>1. UISegmentedControl 패턴</div>
              <div style={{ fontSize: '12px', color: '#374151', lineHeight: '1.7' }}>
                활성 세그먼트가 흰 카드로 부상. 회색 컨테이너 내부에서 150-200ms cubic-bezier 트랜지션으로 자연스럽게 전환됩니다.
              </div>
            </div>
            <div>
              <div style={{ fontSize: '12px', fontWeight: '700', color: '#374151', marginBottom: '4px' }}>2. 인라인 입력 + 시각적 확인</div>
              <div style={{ fontSize: '12px', color: '#374151', lineHeight: '1.7' }}>
                텍스트 입력과 캘린더를 동시에 표시. 입력 포맷(YYYY-MM-DD)을 hint로 제공, blur 시 유효성 검사.
              </div>
            </div>
            <div>
              <div style={{ fontSize: '12px', fontWeight: '700', color: '#374151', marginBottom: '4px' }}>3. 진행 단계 표시</div>
              <div style={{ fontSize: '12px', color: '#374151', lineHeight: '1.7' }}>
                체크아웃처럼 순차적 UX는 스텝 인디케이터로 현재 위치를 명확히 표시. 완료 단계는 체크마크로 전환.
              </div>
            </div>
          </div>
        </div>
      </div>

      <div style={{ padding: '20px', borderRadius: '12px', background: '#f8fafc', border: '1px solid #e2e8f0' }}>
        <div style={{ fontSize: '13px', fontWeight: '700', color: '#1e293b', marginBottom: '12px' }}>Orbit UI 컴포넌트 대응 조합</div>
        <div style={{ overflowX: 'auto' }}>
          <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '12px' }}>
            <thead>
              <tr style={{ background: '#f1f5f9' }}>
                <th style={{ padding: '8px 12px', textAlign: 'left', color: '#475569', fontWeight: '700', borderBottom: '1px solid #e2e8f0' }}>패턴</th>
                <th style={{ padding: '8px 12px', textAlign: 'left', color: '#475569', fontWeight: '700', borderBottom: '1px solid #e2e8f0' }}>벤치마크 출처</th>
                <th style={{ padding: '8px 12px', textAlign: 'left', color: '#6366f1', fontWeight: '700', borderBottom: '1px solid #e2e8f0' }}>Orbit UI 구현</th>
              </tr>
            </thead>
            <tbody>
              {[
                { pattern: '프리셋 날짜 선택', source: 'Tailwind UI', impl: 'Calendar + useState(프리셋 key) + button 사이드바' },
                { pattern: 'Segmented Tab', source: 'Apple HIG', impl: 'Tab 컴포넌트 또는 button role=tab + CSS boxShadow 부상 효과' },
                { pattern: '2단계 예약', source: 'Tailwind UI + Apple HIG', impl: 'Calendar (날짜) + button 그리드 (시간) + SolidButton (확정)' },
                { pattern: '생일 입력 검증', source: 'Apple HIG', impl: 'input + Calendar + useState(error) + disabled={{ after: today }}' },
                { pattern: '모바일 체크아웃', source: 'Apple HIG', impl: 'step state + SolidButton CTA + Progress 또는 커스텀 스텝 인디케이터' },
              ].map((row, i) => (
                <tr key={row.pattern} style={{ background: i % 2 === 0 ? '#fff' : '#fafafa' }}>
                  <td style={{ padding: '8px 12px', color: '#1e293b', fontWeight: '600', borderBottom: '1px solid #f1f5f9' }}>{row.pattern}</td>
                  <td style={{ padding: '8px 12px', color: '#6366f1', borderBottom: '1px solid #f1f5f9' }}>{row.source}</td>
                  <td style={{ padding: '8px 12px', color: '#374151', fontFamily: 'monospace', fontSize: '11px', borderBottom: '1px solid #f1f5f9' }}>{row.impl}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    </section>

    {/* Chakra UI + Google Material 3 테마 패턴 비교 */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>Chakra UI + Google Material 3 테마 패턴</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '24px' }}>
        Chakra UI의 <code style={{ background: '#f1f5f9', color: '#319795', padding: '2px 6px', borderRadius: '4px', fontSize: '13px' }}>extendTheme</code>과
        Material 3의 <code style={{ background: '#f1f5f9', color: '#6750a4', padding: '2px 6px', borderRadius: '4px', fontSize: '13px' }}>colorScheme</code> 기반 접근 방식을 Orbit UI 3-tier 토큰 시스템으로 매핑합니다.
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '28px' }}>
        {/* Chakra UI */}
        <div style={{ borderRadius: '16px', border: '1px solid #b2f5ea', overflow: 'hidden' }}>
          <div style={{ padding: '12px 20px', background: '#e6fffa', borderBottom: '1px solid #b2f5ea', display: 'flex', alignItems: 'center', gap: '8px' }}>
            <span style={{ fontSize: '13px', fontWeight: '700', color: '#234e52' }}>Chakra UI — extendTheme</span>
            <span style={{ fontSize: '11px', color: '#319795', background: '#c6f6f0', padding: '2px 6px', borderRadius: '4px' }}>시맨틱 토큰</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { extendTheme } from '@chakra-ui/react'

const brandTheme = extendTheme({
  colors: {
    brand: {
      50:  '#eff6ff',
      500: '#6366f1',
      900: '#1e1b4b',
    },
  },
  semanticTokens: {
    colors: {
      'chakra-body-bg': {
        _light: 'white',
        _dark: 'gray.900',
      },
    },
  },
})

// Orbit UI 대응:
// CSS 변수로 동일한 패턴 구현
// --color-fill-primary: var(--ref-color-indigo500);`}
          </pre>
        </div>

        {/* Google Material 3 */}
        <div style={{ borderRadius: '16px', border: '1px solid #d8b4fe', overflow: 'hidden' }}>
          <div style={{ padding: '12px 20px', background: '#faf5ff', borderBottom: '1px solid #d8b4fe', display: 'flex', alignItems: 'center', gap: '8px' }}>
            <span style={{ fontSize: '13px', fontWeight: '700', color: '#44337a' }}>Google Material 3 — colorScheme</span>
            <span style={{ fontSize: '11px', color: '#6750a4', background: '#ede9fe', padding: '2px 6px', borderRadius: '4px' }}>Dynamic Color</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`// Material 3 Dynamic Color 시스템
// Source Color → Tonal Palette → Color Scheme

const m3Scheme = {
  primary:          '#6750A4',
  onPrimary:        '#FFFFFF',
  primaryContainer: '#EADDFF',
  secondary:        '#625B71',
  surface:          '#FEF7FF',
  onSurface:        '#1D1B20',
}

// Orbit UI 대응:
// Semantic token과 1:1 매핑
// fillPrimary   → primary
// textOnPrimary → onPrimary`}
          </pre>
        </div>
      </div>

      {/* 토큰 매핑 테이블 */}
      <h3 style={{ fontSize: '18px', fontWeight: '700', color: '#1e293b', marginBottom: '12px' }}>토큰 매핑 비교</h3>
      <div style={{ overflowX: 'auto', borderRadius: '12px', border: '1px solid #e2e8f0' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '13px' }}>
          <thead>
            <tr style={{ background: '#f8fafc' }}>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontWeight: '700', color: '#475569', borderBottom: '1px solid #e2e8f0' }}>용도</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontWeight: '700', color: '#319795', borderBottom: '1px solid #e2e8f0' }}>Chakra UI</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontWeight: '700', color: '#6750a4', borderBottom: '1px solid #e2e8f0' }}>Material 3</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontWeight: '700', color: '#6366f1', borderBottom: '1px solid #e2e8f0' }}>Orbit UI</th>
            </tr>
          </thead>
          <tbody>
            {[
              { purpose: '주 브랜드 색상', chakra: 'brand.500', m3: 'primary', orbit: 'fillPrimary' },
              { purpose: '주 배경', chakra: 'chakra-body-bg', m3: 'surface', orbit: 'backgroundBase' },
              { purpose: '텍스트 색상', chakra: 'chakra-body-text', m3: 'onSurface', orbit: 'textPrimary' },
              { purpose: '경계선', chakra: 'gray.200', m3: 'outlineVariant', orbit: 'borderDefault' },
              { purpose: '포커스 링', chakra: 'blue.500 (focusRingColor)', m3: 'primary (focus-indicator)', orbit: 'borderFocused' },
              { purpose: '에러 색상', chakra: 'red.500', m3: 'error', orbit: 'fillError' },
            ].map((row, i) => (
              <tr key={row.purpose} style={{ background: i % 2 === 0 ? '#fff' : '#fafafa' }}>
                <td style={{ padding: '8px 16px', color: '#374151', fontWeight: '500', borderBottom: '1px solid #f1f5f9' }}>{row.purpose}</td>
                <td style={{ padding: '8px 16px', color: '#319795', fontFamily: 'monospace', fontSize: '12px', borderBottom: '1px solid #f1f5f9' }}>{row.chakra}</td>
                <td style={{ padding: '8px 16px', color: '#6750a4', fontFamily: 'monospace', fontSize: '12px', borderBottom: '1px solid #f1f5f9' }}>{row.m3}</td>
                <td style={{ padding: '8px 16px', color: '#6366f1', fontFamily: 'monospace', fontSize: '12px', borderBottom: '1px solid #f1f5f9' }}>{row.orbit}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      {/* Orbit UI 장점 */}
      <div style={{ marginTop: '24px', padding: '20px', background: '#eff6ff', borderRadius: '12px', border: '1px solid #bfdbfe' }}>
        <h4 style={{ fontSize: '15px', fontWeight: '700', color: '#1e40af', margin: '0 0 12px' }}>Orbit UI 3-tier 토큰 시스템의 강점</h4>
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: '12px' }}>
          {[
            { title: 'CSS 변수 기반', desc: 'JS 런타임 없이 순수 CSS 변수로 테마 전환. SSR 환경에서 FOUC 없음' },
            { title: '계층적 구조', desc: 'Reference → Semantic → Component 3단계로 변경 범위를 최소화' },
            { title: 'Scope 전환', desc: 'data-theme 속성 하나로 컴포넌트 단위 다크/라이트 독립 전환 가능' },
          ].map((item) => (
            <div key={item.title} style={{ background: '#fff', borderRadius: '10px', padding: '14px', border: '1px solid #dbeafe' }}>
              <div style={{ fontSize: '13px', fontWeight: '700', color: '#1e40af', marginBottom: '6px' }}>{item.title}</div>
              <div style={{ fontSize: '12px', color: '#3b82f6', lineHeight: '1.6' }}>{item.desc}</div>
            </div>
          ))}
        </div>
      </div>
    </section>

    {/* Google M3 + Figma Plugin UI 커스터마이징 섹션 */}
    <section style={{ marginBottom: '64px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '24px' }}>
        <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', margin: 0 }}>Google M3 + Figma Plugin UI 커스터마이징 패턴</h2>
        <span style={{ padding: '3px 10px', background: '#f1f5f9', borderRadius: '20px', fontSize: '12px', fontWeight: '600', color: '#6366f1' }}>Cycle 57</span>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '20px', marginBottom: '28px' }}>
        <div style={{ padding: '24px', borderRadius: '14px', background: '#fff8f1', border: '1.5px solid #fed7aa' }}>
          <div style={{ fontSize: '11px', fontWeight: '700', color: '#ea580c', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Google Material 3</div>
          <div style={{ fontSize: '18px', fontWeight: '700', color: '#9a3412', marginBottom: '10px' }}>Dynamic Color System</div>
          <p style={{ fontSize: '13px', color: '#c2410c', lineHeight: '1.7', margin: '0 0 16px' }}>
            M3의 동적 색상은 하나의 "seed color"에서 전체 색상 팔레트를 자동 생성합니다. Primary, Secondary, Tertiary, Error의 4개 역할과 각각의 Container, On- 색상으로 체계적인 색상 커스터마이징을 제공합니다.
          </p>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', fontSize: '12px' }}>
            {[
              ['Icon Button Variant', 'Filled / Tonal / Outlined / Standard 4가지 역할 → OutlineIconButton color + style 조합으로 구현'],
              ['Supporting Text', '입력 필드 하단 조건 체크리스트 → PasswordField error prop + 조건 피드백 UI'],
              ['Extended FAB', '스크롤 반응 확장/축소 FAB → max-width + opacity 트랜지션 패턴'],
            ].map(([k, v]) => (
              <div key={k} style={{ padding: '8px 12px', background: '#fff', borderRadius: '8px', border: '1px solid #fed7aa' }}>
                <div style={{ color: '#ea580c', fontWeight: '700', marginBottom: '2px' }}>{k}</div>
                <div style={{ color: '#9a3412' }}>{v}</div>
              </div>
            ))}
          </div>
        </div>

        <div style={{ padding: '24px', borderRadius: '14px', background: '#1e1e1e', border: '1px solid #3f3f46' }}>
          <div style={{ fontSize: '11px', fontWeight: '700', color: '#71717a', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Figma Plugin UI</div>
          <div style={{ fontSize: '18px', fontWeight: '700', color: '#e4e4e7', marginBottom: '10px' }}>Compact Dark Canvas</div>
          <p style={{ fontSize: '13px', color: '#a1a1aa', lineHeight: '1.7', margin: '0 0 16px' }}>
            Figma 플러그인은 240~320px 너비의 컴팩트 다크 패널에 최대 밀도로 컨트롤을 배치합니다. #1e1e1e 배경, 9-11px 레이블, 4px 간격의 아이콘 그룹이 핵심 패턴입니다.
          </p>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', fontSize: '12px' }}>
            {[
              ['도구 팔레트', '그룹별 OutlineIconButton + 활성 도구 border+color 강조, 9px 레이블'],
              ['API 키 마스킹', 'PasswordField + 부분 마스킹(앞6자+뒤4자) + 클립보드 복사'],
              ['색상 팔레트', '#1e1e1e / #27272a / #3f3f46 3단계 배경 계층'],
            ].map(([k, v]) => (
              <div key={k} style={{ padding: '8px 12px', background: '#27272a', borderRadius: '8px', border: '1px solid #3f3f46' }}>
                <div style={{ color: '#a5b4fc', fontWeight: '700', marginBottom: '2px' }}>{k}</div>
                <div style={{ color: '#71717a' }}>{v}</div>
              </div>
            ))}
          </div>
        </div>
      </div>

      <div style={{ overflowX: 'auto', borderRadius: '12px', border: '1.5px solid #e2e8f0', marginBottom: '24px' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '13px' }}>
          <thead>
            <tr style={{ background: '#f8fafc' }}>
              {['커스터마이징 기준', 'Google M3', 'Figma Plugin UI', 'Orbit UI 반영'].map((col, i) => (
                <th key={col} style={{ padding: '12px 16px', textAlign: 'left', fontWeight: 700, color: i === 3 ? '#6366f1' : '#475569', borderBottom: '1.5px solid #e2e8f0', whiteSpace: 'nowrap' }}>{col}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            {[
              ['아이콘 버튼 역할', '4가지 variant 체계', '단일 컴팩트 스타일', 'color+size+style 조합으로 구현'],
              ['비밀번호 입력', '조건 체크리스트 Supporting Text', 'API 키 마스킹 패널', 'PasswordField error+강도지표'],
              ['다크 테마', 'Dynamic Color 자동생성', '수동 #1e1e1e 팔레트', 'EclipseProvider mode="dark"'],
              ['밀도', 'Touch-friendly 48dp 최소', '8-10px 컴팩트', 'size="small" prop'],
              ['상호작용', 'Ripple 효과', '즉각 상태 변경', 'transition 0.15s 일관 적용'],
            ].map((row, i) => (
              <tr key={i} style={{ background: i % 2 === 0 ? '#fff' : '#fafafa' }}>
                {row.map((cell, j) => (
                  <td key={j} style={{ padding: '11px 16px', color: j === 3 ? '#4f46e5' : '#334155', borderBottom: '1px solid #f1f5f9', fontWeight: j === 0 ? 600 : 400 }}>{cell}</td>
                ))}
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <div style={{ padding: '20px', borderRadius: '12px', background: 'linear-gradient(135deg, #f0fdf4, #ecfdf5)', border: '2px solid #bbf7d0' }}>
        <div style={{ fontSize: '13px', fontWeight: '700', color: '#065f46', marginBottom: '12px' }}>Orbit UI 반영 결과</div>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '12px', fontSize: '12px', color: '#374151', lineHeight: '1.7' }}>
          <div>
            <div style={{ fontWeight: '700', color: '#059669', marginBottom: '4px' }}>OutlineIconButton 고도화</div>
            M3 Tonal 토글 그룹, Figma 컴팩트 도구 팔레트, M3 Extended FAB 스크롤 반응 3개 스토리 추가.
          </div>
          <div>
            <div style={{ fontWeight: '700', color: '#059669', marginBottom: '4px' }}>PasswordField 고도화</div>
            M3 비밀번호 강도 체크리스트, Figma API 키 마스킹 패널, M3 비밀번호 재설정 단계폼 3개 스토리 추가.
          </div>
        </div>
      </div>
    </section>

  </div>


<section style={{ marginTop: '48px', padding: '32px', background: 'linear-gradient(135deg, #fff7ed 0%, #fef3c7 100%)', borderRadius: '16px', border: '1px solid #fed7aa' }}>
  <h2 style={{ fontSize: '22px', fontWeight: '800', color: '#7c2d12', marginBottom: '8px' }}>Cycle 70 — Tailwind UI + Ant Design 커스텀 패턴</h2>
  <p style={{ color: '#c2410c', fontSize: '14px', marginBottom: '24px', fontWeight: '600' }}>AlertDialog 위험 작업 UX + Carousel 콘텐츠 슬라이더 + 알림 센터 패턴</p>

  <h3 style={{ fontSize: '17px', fontWeight: '700', color: '#334155', marginBottom: '16px' }}>Tailwind UI에서 배운 것</h3>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fed7aa' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#ea580c', marginBottom: '8px' }}>파괴적 액션 확인 패턴</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        단순한 "확인 / 취소"가 아닌, 삭제 대상 이름을 직접 입력하게 해서 실수를 원천 차단. GitHub 레포 삭제 등 고위험 작업에서 필수 UX 패턴.
      </p>
    </div>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fed7aa' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#ea580c', marginBottom: '8px' }}>히어로 슬라이더 구성</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        랜딩 페이지 히어로에 그라디언트 배경 + 텍스트 오버레이 슬라이드 패턴. 3-4초 자동 전환, 하단 pill 도트 인디케이터, smooth transition.
      </p>
    </div>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fed7aa' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#ea580c', marginBottom: '8px' }}>팀원 소개 카드 캐러셀</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        About 페이지 팀원 소개를 카드 캐러셀로 구성. 아바타 이니셜 + 직함 + 스킬 태그 조합. 모바일 반응형 핵심 패턴.
      </p>
    </div>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fed7aa' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#ea580c', marginBottom: '8px' }}>알림 센터 레이아웃</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        카테고리 탭 필터 + 읽음/안읽음 배지 카운트 + 일괄 읽음 처리. 알림 항목은 타입별 색상 코딩(info/success/warning/mention).
      </p>
    </div>
  </div>

  <h3 style={{ fontSize: '17px', fontWeight: '700', color: '#334155', marginBottom: '16px' }}>Ant Design에서 배운 것</h3>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fed7aa' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#92400e', marginBottom: '8px' }}>폼 이탈 경고 3선택지</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        "저장 후 이동 / 저장하지 않고 이동 / 계속 편집" 세 가지 명시적 선택지. 단순 Yes/No보다 사용자 의도를 정확히 반영하여 데이터 손실 방지.
      </p>
    </div>
    <div style={{ padding: '16px', borderRadius: '10px', background: '#fff', border: '1px solid #fed7aa' }}>
      <h4 style={{ fontSize: '14px', fontWeight: '700', color: '#92400e', marginBottom: '8px' }}>공지 배너 타입별 색상</h4>
      <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6' }}>
        info(blue)/warning(amber)/success(green) 타입별 색상 시스템 + 배지. 자동 슬라이더로 여러 공지를 컴팩트하게 순환 노출하는 대시보드 패턴.
      </p>
    </div>
  </div>

  <h3 style={{ fontSize: '17px', fontWeight: '700', color: '#334155', marginBottom: '12px' }}>이번 사이클 적용 결과</h3>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
    <div style={{ padding: '12px 16px', borderRadius: '8px', background: '#f8fafc', border: '1px solid #e2e8f0', fontSize: '13px' }}>
      <span style={{ fontWeight: '700', color: '#ea580c' }}>AlertDialog +3:</span>
      <span style={{ color: '#475569', marginLeft: '8px' }}>Tailwind 구독 해지 확인, Tailwind 파일 영구 삭제(입력 검증), Ant 폼 이탈 경고(3선택지)</span>
    </div>
    <div style={{ padding: '12px 16px', borderRadius: '8px', background: '#f8fafc', border: '1px solid #e2e8f0', fontSize: '13px' }}>
      <span style={{ fontWeight: '700', color: '#0369a1' }}>Carousel +3:</span>
      <span style={{ color: '#475569', marginLeft: '8px' }}>Tailwind 히어로 슬라이더, Tailwind 팀원 소개 캐러셀, Ant 공지 배너 슬라이더</span>
    </div>
    <div style={{ padding: '12px 16px', borderRadius: '8px', background: '#f8fafc', border: '1px solid #e2e8f0', fontSize: '13px' }}>
      <span style={{ fontWeight: '700', color: '#059669' }}>Template #60:</span>
      <span style={{ color: '#475569', marginLeft: '8px' }}>TailwindNotificationCenter — 알림 센터, 카테고리 탭, 읽음 관리, Progress 진행률 시각화</span>
    </div>
  </div>
</section>
</Unstyled>
