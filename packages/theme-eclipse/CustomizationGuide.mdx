import { Meta, Unstyled } from '@storybook/blocks'

<Meta title='Intro/커스터마이제이션 가이드' />

<Unstyled>
  <style>{`
    [data-orbit-doc] .orbit-code {
      margin: 0;
      border: 1px solid var(--orbit-line);
      border-radius: 12px;
      overflow: hidden;
      background: var(--orbit-surface);
    }
    [data-orbit-doc] .orbit-code-head {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: 16px;
      padding: 9px 16px;
      border-bottom: 1px solid var(--orbit-line);
      background: var(--orbit-surface-sunken);
      font-family: var(--orbit-mono);
      font-size: 11px;
      color: var(--orbit-ink-3);
    }
    [data-orbit-doc] .orbit-code-head .tag {
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      font-size: 10.5px;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-code pre {
      margin: 0;
      padding: 18px 20px;
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      line-height: 1.7;
      color: var(--orbit-ink);
      background: transparent;
      overflow-x: auto;
      white-space: pre;
    }

    [data-orbit-doc] .orbit-decision {
      margin-top: 24px;
      width: 100%;
      border-collapse: separate;
      border-spacing: 0;
      font-size: 13px;
    }
    [data-orbit-doc] .orbit-decision thead th {
      padding: 12px 16px;
      border-top: 1px solid var(--orbit-line-2);
      border-bottom: 1px solid var(--orbit-line-2);
      font-family: var(--orbit-mono);
      font-size: 11px;
      font-weight: 600;
      color: var(--orbit-ink-4);
      text-align: left;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-decision tbody td {
      padding: 14px 16px;
      border-bottom: 1px solid var(--orbit-line);
      vertical-align: top;
      color: var(--orbit-ink-2);
      line-height: 1.55;
    }
    [data-orbit-doc] .orbit-decision tbody tr:last-child td {
      border-bottom: 1px solid var(--orbit-line-2);
    }
    [data-orbit-doc] .orbit-decision code {
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-decision .level {
      font-weight: 660;
      color: var(--orbit-ink);
      letter-spacing: -0.012em;
    }
    [data-orbit-doc] .orbit-decision .scope {
      font-family: var(--orbit-mono);
      font-size: 11.5px;
      color: var(--orbit-ink-3);
    }

    [data-orbit-doc] .orbit-level {
      display: grid;
      grid-template-columns: 200px minmax(0, 1fr);
      gap: 32px;
      padding: 36px 0;
      border-bottom: 1px solid var(--orbit-line);
      align-items: start;
    }
    [data-orbit-doc] .orbit-level:last-of-type { border-bottom: 0; }
    [data-orbit-doc] .orbit-level .meta {
      font-family: var(--orbit-mono);
      font-size: 11px;
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-level .meta strong {
      display: block;
      margin-top: 4px;
      font-family: var(--orbit-display);
      font-size: 18px;
      font-weight: 680;
      letter-spacing: -0.018em;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-level .meta .scope-tag {
      display: inline-block;
      margin-top: 12px;
      padding: 3px 10px;
      border-radius: 999px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      color: var(--orbit-ink-3);
      letter-spacing: 0.04em;
    }
    [data-orbit-doc] .orbit-level h3 {
      margin: 0 0 8px;
      font-size: 18px;
      font-weight: 660;
      letter-spacing: -0.015em;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-level p {
      margin: 0 0 16px;
      max-width: 62ch;
      font-size: 14.5px;
      line-height: 1.62;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-level p code {
      padding: 1px 6px;
      border-radius: 5px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-level .use {
      margin: 8px 0 16px;
      padding-left: 16px;
      list-style: none;
    }
    [data-orbit-doc] .orbit-level .use li {
      position: relative;
      padding: 4px 0;
      font-size: 13.5px;
      line-height: 1.55;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-level .use li::before {
      content: '→';
      position: absolute;
      left: -16px;
      color: var(--orbit-ink-4);
      font-family: var(--orbit-mono);
    }

    [data-orbit-doc] .orbit-note {
      margin-top: 20px;
      padding: 14px 18px;
      border: 1px solid var(--orbit-line);
      border-radius: 10px;
      background: var(--orbit-surface);
      font-size: 13.5px;
      line-height: 1.6;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-note strong {
      display: block;
      margin-bottom: 4px;
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      font-weight: 700;
      color: var(--orbit-ink-3);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-note code {
      padding: 1px 6px;
      border-radius: 5px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink-2);
    }

    @media (max-width: 760px) {
      [data-orbit-doc] .orbit-level { grid-template-columns: 1fr; gap: 12px; padding: 24px 0; }
      [data-orbit-doc] .orbit-decision thead th:nth-child(4),
      [data-orbit-doc] .orbit-decision tbody td:nth-child(4) { display: none; }
    }
  `}</style>

  <div data-orbit-doc>
    <div className='orbit-wrap'>

      {/* HEADER */}
      <header className='orbit-page-head'>
        <div>
          <div className='orbit-eyebrow'><span className='num'>01</span>Customization</div>
          <h1 className='orbit-h1'>커스터마이제이션</h1>
          <p className='orbit-lede'>
            세 가지 수준으로 시스템을 커스터마이즈할 수 있습니다. 단일 컴포넌트만 바꿀지, 토큰 한 레이어를 갈아끼울지,
            완전히 새 테마를 만들지를 먼저 정하고 출발하세요.
          </p>
        </div>
        <div className='orbit-page-meta'>
          <div><strong>3</strong> levels</div>
          <div>theme prop</div>
          <div>CSS vars · custom theme</div>
        </div>
      </header>

      {/* DECISION TABLE */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>02</span>Pick a Level</div>
            <h2>어떤 수준이 필요한가.</h2>
            <p>변경 범위가 커질수록 작업량이 늘어납니다. 한 컴포넌트면 충분한지, 시스템 전체를 갈아끼우는지부터 결정.</p>
          </div>
        </div>

        <table className='orbit-decision'>
          <thead>
            <tr>
              <th style={{ width: '20%' }}>레벨</th>
              <th style={{ width: '22%' }}>도구</th>
              <th style={{ width: '26%' }}>적용 범위</th>
              <th>적합한 상황</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td className='level'>01 · theme prop</td>
              <td><code>theme={'{{...}}'}</code></td>
              <td className='scope'>단일 컴포넌트 인스턴스</td>
              <td>특정 페이지·CTA·이벤트 배너의 한 버튼만 다르게.</td>
            </tr>
            <tr>
              <td className='level'>02 · CSS Variable Override</td>
              <td><code>--ref-* / --sem-*</code></td>
              <td className='scope'>전역 또는 컨테이너 스코프</td>
              <td>브랜드 컬러 매핑, primary·neutral 톤 조정, 일부 컴포넌트 디폴트만 바꿀 때.</td>
            </tr>
            <tr>
              <td className='level'>03 · Custom Theme Package</td>
              <td><code>@heejun-com/core</code></td>
              <td className='scope'>전체 디자인 시스템 자체</td>
              <td>완전히 다른 시각 언어. 자체 토큰 체계 + 자체 브랜드를 풀-매뉴얼로 만들 때.</td>
            </tr>
          </tbody>
        </table>
      </section>

      {/* LEVEL 1 */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>03</span>Level 01</div>
            <h2>theme 프롭.</h2>
            <p>단일 컴포넌트 인스턴스에만 토큰을 surgical하게 덮어쓰는 방법. CSS 작성 없음, TypeScript로 타입 안전.</p>
          </div>
        </div>

        <div className='orbit-level'>
          <div className='meta'>Scope<strong>한 컴포넌트</strong><span className='scope-tag'>local</span></div>
          <div>
            <h3>인스턴스 단위 오버라이드</h3>
            <p>
              컴포넌트별 <code>ComponentToken</code> 타입의 partial 객체를 <code>theme</code> 프롭에 전달합니다.
              나머지 토큰은 글로벌 디폴트를 그대로 사용. One Voice Rule의 의도된 예외를 만들 때 사용합니다.
            </p>
            <ul className='use'>
              <li>이벤트 배너의 단일 강조 액션 (예: <code>green5</code>로 success-only)</li>
              <li>마케팅 페이지의 브랜드 컬러 일회성 노출</li>
              <li>A/B 테스트용 변형</li>
            </ul>

            <div className='orbit-code'>
              <div className='orbit-code-head'>
                <span className='tag'>TSX</span>
                <span>SolidButton instance override</span>
              </div>
              <pre>{`import { SolidButton } from '@heejun-com/theme-eclipse'

// 결제 완료 페이지의 단일 CTA만 성공 그린으로
<SolidButton
  color='primary'
  size='large'
  theme={{
    enabledFillColor: 'var(--ref-color-green5)',
    hoveredFillColor: 'var(--ref-color-green6)',
    focusedBorderColor: 'var(--ref-color-green6)',
  }}
>
  <SolidButton.Center>예약하기</SolidButton.Center>
</SolidButton>`}</pre>
            </div>

            <aside className='orbit-note'>
              <strong>Tip</strong>
              컴포넌트 토큰 이름은 <code>packages/theme-eclipse/src/styles/component-token.ts</code>에서 확인.
              TypeScript 자동완성으로 가능한 키를 모두 탐색할 수 있습니다.
            </aside>
          </div>
        </div>
      </section>

      {/* LEVEL 2 */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>04</span>Level 02</div>
            <h2>CSS 변수 오버라이드.</h2>
            <p>3-tier 토큰 시스템의 어느 한 레이어를 덮어쓰면 그 위 레이어 전체가 자동으로 따라옵니다.</p>
          </div>
        </div>

        <div className='orbit-level'>
          <div className='meta'>Tier<strong>Reference</strong><span className='scope-tag'>--ref-*</span></div>
          <div>
            <h3>원시 컬러 스케일 교체</h3>
            <p>
              가장 깊은 진입점. <code>--ref-color-primary*</code>를 한 번 덮으면 모든 primary 매핑이 갱신됩니다.
              브랜드 컬러를 시스템의 voice로 만들 때 사용.
            </p>

            <div className='orbit-code'>
              <div className='orbit-code-head'>
                <span className='tag'>CSS</span>
                <span>app.css</span>
              </div>
              <pre>{`/* 브랜드 오렌지를 primary로 매핑 */
.eclipse-light {
  --ref-color-primary1: #FFE5D6;
  --ref-color-primary4: #FF9468;
  --ref-color-primary5: #FF4500;
  --ref-color-primary6: #DC3F00;
}

.eclipse-dark {
  --ref-color-primary1: #3D1F10;
  --ref-color-primary4: #FF7A40;
  --ref-color-primary5: #FF9468;
  --ref-color-primary6: #FFB392;
}`}</pre>
            </div>
          </div>
        </div>

        <div className='orbit-level'>
          <div className='meta'>Tier<strong>Semantic</strong><span className='scope-tag'>--sem-eclipse-color-*</span></div>
          <div>
            <h3>역할 단위 재매핑</h3>
            <p>
              "primary action color" 같은 의미 토큰만 갈아끼웁니다. 다른 컴포넌트들은 자기 역할(예: positive, negative)을
              그대로 유지합니다. 부분 커스터마이즈에 적합.
            </p>

            <div className='orbit-code'>
              <div className='orbit-code-head'>
                <span className='tag'>CSS</span>
                <span>brand-overrides.css</span>
              </div>
              <pre>{`/* primary 액션만 브랜드 컬러로, 나머지는 기본 유지 */
.eclipse-light {
  --sem-eclipse-color-systemMainPrimary: var(--my-brand-color);
  --sem-eclipse-color-systemMainTertiary: var(--my-brand-tint);
}

/* positive·negative 등은 기본 토큰을 그대로 사용 */`}</pre>
            </div>
          </div>
        </div>

        <div className='orbit-level'>
          <div className='meta'>Tier<strong>Component</strong><span className='scope-tag'>--com-eclipse-*</span></div>
          <div>
            <h3>단일 컴포넌트 디폴트 조정</h3>
            <p>
              시스템 전체에 영향 없이 한 컴포넌트의 모든 인스턴스 디폴트만 바꿀 때. <code>theme</code> 프롭과의 차이는,
              컴포넌트 사용처마다 따로 지정할 필요 없이 *글로벌하게* 적용된다는 점.
            </p>

            <div className='orbit-code'>
              <div className='orbit-code-head'>
                <span className='tag'>CSS</span>
                <span>button-radius.css</span>
              </div>
              <pre>{`/* 시스템 전체 버튼을 pill 모양으로 (radius medium → rounded) */
:root {
  --com-eclipse-button-radius: var(--ref-radius-rounded);
  --com-eclipse-filledButton-radius: var(--ref-radius-rounded);
  --com-eclipse-outlineButton-radius: var(--ref-radius-rounded);
}`}</pre>
            </div>
          </div>
        </div>
      </section>

      {/* LEVEL 3 */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>05</span>Level 03</div>
            <h2>자체 테마 패키지.</h2>
            <p>Eclipse 대신 자체 디자인 언어의 시스템을 만들고 싶을 때. <code>@heejun-com/core</code>를 직접 래핑합니다.</p>
          </div>
        </div>

        <div className='orbit-level'>
          <div className='meta'>Build<strong>Custom Theme</strong><span className='scope-tag'>headless</span></div>
          <div>
            <h3>Headless Core 위에 자체 토큰 입히기</h3>
            <p>
              <code>@heejun-com/core</code>는 접근성·키보드·상태 로직만 담은 컴포넌트입니다. <code>theme</code> 프롭으로
              자체 토큰을 주입하여 완전히 다른 시각 시스템을 만들 수 있습니다. Eclipse가 이 방식으로 만들어졌습니다.
            </p>

            <div className='orbit-code'>
              <div className='orbit-code-head'>
                <span className='tag'>TSX</span>
                <span>packages/theme-mybrand/src/components/MyButton.tsx</span>
              </div>
              <pre>{`import { Button } from '@heejun-com/core'
import { brandTokens } from '../tokens'

export const MyButton = ({ variant, size, children, ...rest }) => (
  <Button
    theme={{
      ...brandTokens.button.base,
      ...brandTokens.button.variant[variant],
      ...brandTokens.button.size[size],
    }}
    {...rest}
  >
    {children}
  </Button>
)`}</pre>
            </div>

            <aside className='orbit-note'>
              <strong>When to go this deep</strong>
              브랜드의 시각 언어가 Eclipse의 "Quiet Foundation" 미학과 근본적으로 다를 때 (예: 마케팅 중심 brand register, drenched color, editorial 디스플레이 폰트).
              그 외엔 Level 02의 토큰 오버라이드로 충분합니다.
            </aside>
          </div>
        </div>
      </section>

      {/* WORKED EXAMPLE */}
      <section className='orbit-sect'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>06</span>Worked Example</div>
            <h2>브랜드 컬러 한 줄 주입.</h2>
            <p>가장 흔한 케이스: 회사 브랜드 컬러를 primary로 박는 풀 플로우.</p>
          </div>
        </div>

        <div className='orbit-code'>
          <div className='orbit-code-head'>
            <span className='tag'>CSS</span>
            <span>app.css · global brand injection</span>
          </div>
          <pre>{`/* 1. 브랜드 컬러 정의 */
:root {
  --my-brand-50: #EFF6FF;
  --my-brand-100: #DBEAFE;
  --my-brand-500: #3B82F6;
  --my-brand-600: #2563EB;
  --my-brand-700: #1D4ED8;
}

/* 2. Eclipse primary 스케일에 매핑 */
.eclipse-light {
  --ref-color-primary1: var(--my-brand-100);
  --ref-color-primary5: var(--my-brand-500);
  --ref-color-primary6: var(--my-brand-600);
  --ref-color-primary7: var(--my-brand-700);
}

.eclipse-dark {
  --ref-color-primary5: var(--my-brand-500);
  --ref-color-primary6: var(--my-brand-700);
}

/* 3. 포커스 링도 함께 맞춤 (선택) */
.eclipse-light {
  --sem-eclipse-focus-ring-color: rgba(59, 130, 246, 0.85);
}`}</pre>
        </div>

        <aside className='orbit-note'>
          <strong>이 한 번의 매핑으로 일어나는 일</strong>
          모든 primary 버튼, 포커스 링, 링크, 현재 선택 표시, in-bounds 강조가 브랜드 블루로 갱신됩니다.
          컴포넌트 import 코드는 한 줄도 바뀌지 않습니다. <code>SolidButton color='primary'</code>가 자동으로 브랜드 컬러를 따라옵니다.
        </aside>
      </section>

      {/* FOOTER */}
      <footer className='orbit-foot'>
        <div>3 levels · token-driven · type-safe</div>
        <div>
          <a href='?path=/docs/intro-디자인토큰--docs'>← 디자인 토큰</a>
          <span style={{ marginLeft: 16 }} />
          <a href='?path=/docs/intro-테마-가이드--docs'>테마 가이드 →</a>
        </div>
      </footer>

    </div>
  </div>
</Unstyled>
