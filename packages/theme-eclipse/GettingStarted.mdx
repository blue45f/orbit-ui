import { Meta, Unstyled } from '@storybook/blocks'

<Meta title='Intro/시작하기' />

<Unstyled>
  <div style={{
    maxWidth: '800px',
    margin: '0 auto',
    fontFamily: '"Pretendard Variable", "Pretendard", -apple-system, BlinkMacSystemFont, system-ui, sans-serif',
  }}>

    {/* Header */}
    <div style={{ marginBottom: '48px' }}>
      <div style={{ fontSize: '12px', fontWeight: '700', color: '#6366f1', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Getting Started</div>
      <h1 style={{ fontSize: '36px', fontWeight: '800', color: '#1e293b', letterSpacing: '-0.03em', margin: '0 0 12px' }}>시작하기</h1>
      <p style={{ fontSize: '16px', color: '#64748b', lineHeight: '1.6', margin: 0 }}>Orbit UI Eclipse 테마를 프로젝트에 적용하는 방법을 안내합니다.</p>
    </div>

    {/* Step 1: Installation */}
    <section style={{ marginBottom: '48px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '20px' }}>
        <div style={{
          width: '28px', height: '28px', borderRadius: '8px', background: '#6366f1',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          color: '#ffffff', fontSize: '14px', fontWeight: '700', flexShrink: 0,
        }}>1</div>
        <h2 style={{ fontSize: '20px', fontWeight: '700', color: '#1e293b', margin: 0 }}>패키지 설치</h2>
      </div>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
        <div style={{
          display: 'flex', alignItems: 'center', gap: '12px',
          padding: '12px 16px', borderRadius: '10px',
          background: '#0f172a', fontFamily: '"JetBrains Mono", monospace',
          border: '1px solid rgba(99, 102, 241, 0.3)',
        }}>
          <span style={{ fontSize: '11px', color: '#64748b', fontWeight: '600', minWidth: '36px', textTransform: 'uppercase' }}>pnpm</span>
          <code style={{ fontSize: '13px', color: '#e2e8f0' }}>pnpm add @heejun-com/theme-eclipse @heejun-com/icons</code>
          <span style={{ fontSize: '10px', color: '#6366f1', fontWeight: '600', marginLeft: 'auto', textTransform: 'uppercase', letterSpacing: '0.05em' }}>Recommended</span>
        </div>
        <div style={{
          display: 'flex', alignItems: 'center', gap: '12px',
          padding: '12px 16px', borderRadius: '10px',
          background: '#0f172a', fontFamily: '"JetBrains Mono", monospace',
          border: '1px solid #1e293b',
        }}>
          <span style={{ fontSize: '11px', color: '#64748b', fontWeight: '600', minWidth: '36px', textTransform: 'uppercase' }}>npm</span>
          <code style={{ fontSize: '13px', color: '#e2e8f0' }}>npm install @heejun-com/theme-eclipse @heejun-com/icons</code>
        </div>
        <div style={{
          display: 'flex', alignItems: 'center', gap: '12px',
          padding: '12px 16px', borderRadius: '10px',
          background: '#0f172a', fontFamily: '"JetBrains Mono", monospace',
          border: '1px solid #1e293b',
        }}>
          <span style={{ fontSize: '11px', color: '#64748b', fontWeight: '600', minWidth: '36px', textTransform: 'uppercase' }}>yarn</span>
          <code style={{ fontSize: '13px', color: '#e2e8f0' }}>yarn add @heejun-com/theme-eclipse @heejun-com/icons</code>
        </div>
      </div>
    </section>

    {/* Step 2: Setup */}
    <section style={{ marginBottom: '48px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '20px' }}>
        <div style={{
          width: '28px', height: '28px', borderRadius: '8px', background: '#6366f1',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          color: '#ffffff', fontSize: '14px', fontWeight: '700', flexShrink: 0,
        }}>2</div>
        <h2 style={{ fontSize: '20px', fontWeight: '700', color: '#1e293b', margin: 0 }}>환경 설정</h2>
      </div>

      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '16px', lineHeight: '1.6' }}>
        프로젝트 최상단에 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>EclipseProvider</code>를 적용하고, 스타일 CSS를 import 해주세요.
      </p>

      <div style={{
        borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden', marginBottom: '24px',
      }}>
        <div style={{ padding: '8px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', gap: '6px' }}>
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#f87171' }} />
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#fbbf24' }} />
          <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: '#34d399' }} />
          <span style={{ marginLeft: '8px', fontSize: '11px', color: '#94a3b8', fontWeight: '500' }}>App.tsx</span>
        </div>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '13px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { EclipseProvider } from '@heejun-com/theme-eclipse'
import '@heejun-com/theme-eclipse/style.css'

function MyApp() {
  return (
    <EclipseProvider mode="light">
      <AppComponents />
    </EclipseProvider>
  )
}`}
        </pre>
      </div>

      <div style={{ fontSize: '13px', fontWeight: '600', color: '#475569', marginBottom: '12px' }}>EclipseProvider Props</div>
      <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse', margin: 0 }}>
          <thead>
            <tr style={{ background: '#f8fafc' }}>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: '1px solid #e2e8f0' }}>Prop</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: '1px solid #e2e8f0' }}>Type</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: '1px solid #e2e8f0' }}>Default</th>
              <th style={{ padding: '10px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: '1px solid #e2e8f0' }}>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td style={{ padding: '10px 16px', borderBottom: '1px solid #f1f5f9' }}><code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '12px', fontWeight: '600' }}>mode</code></td>
              <td style={{ padding: '10px 16px', borderBottom: '1px solid #f1f5f9', fontSize: '13px', color: '#64748b' }}>'light' | 'dark'</td>
              <td style={{ padding: '10px 16px', borderBottom: '1px solid #f1f5f9', fontSize: '13px', color: '#64748b' }}>'light'</td>
              <td style={{ padding: '10px 16px', borderBottom: '1px solid #f1f5f9', fontSize: '13px', color: '#475569' }}>라이트/다크 모드 전환</td>
            </tr>
            <tr>
              <td style={{ padding: '10px 16px' }}><code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '12px', fontWeight: '600' }}>baseTextSize</code></td>
              <td style={{ padding: '10px 16px', fontSize: '13px', color: '#64748b' }}>'small' | 'medium' | 'large'</td>
              <td style={{ padding: '10px 16px', fontSize: '13px', color: '#64748b' }}>'medium'</td>
              <td style={{ padding: '10px 16px', fontSize: '13px', color: '#475569' }}>기본 텍스트 크기</td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

    {/* Step 3: SSR */}
    <section style={{ marginBottom: '48px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '20px' }}>
        <div style={{
          width: '28px', height: '28px', borderRadius: '8px', background: '#6366f1',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          color: '#ffffff', fontSize: '14px', fontWeight: '700', flexShrink: 0,
        }}>3</div>
        <h2 style={{ fontSize: '20px', fontWeight: '700', color: '#1e293b', margin: 0 }}>Server Side Rendering</h2>
      </div>

      <div style={{
        padding: '16px 20px', borderRadius: '10px', marginBottom: '16px',
        background: 'linear-gradient(90deg, rgba(99, 102, 241, 0.04) 0%, transparent 100%)',
        borderLeft: '3px solid #6366f1',
      }}>
        <p style={{ fontSize: '14px', color: '#475569', margin: 0, lineHeight: '1.6' }}>
          SSR 환경에서 초기 렌더링 시 깜빡임을 방지하려면 <code style={{ background: '#f1f5f9', color: '#6366f1', padding: '2px 6px', borderRadius: '4px', fontSize: '13px', fontWeight: '500' }}>getTheme()</code>을 사용해주세요.
        </p>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px' }}>
        <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '8px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
            <span style={{ fontSize: '12px', color: '#64748b', fontWeight: '600' }}>Pages Router</span>
            <span style={{ fontSize: '11px', color: '#94a3b8' }}>_document.tsx</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.7', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { getTheme } from '@heejun-com/theme-eclipse/server'

export default function Document() {
  return (
    <Html lang="ko">
      <Head />
      <body className={getTheme()}>
        <Main />
        <NextScript />
      </body>
    </Html>
  )
}`}
          </pre>
        </div>
        <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
          <div style={{ padding: '8px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
            <span style={{ fontSize: '12px', color: '#64748b', fontWeight: '600' }}>App Router</span>
            <span style={{ fontSize: '11px', color: '#94a3b8' }}>app/layout.tsx</span>
          </div>
          <pre style={{ margin: 0, padding: '16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.7', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { getTheme } from '@heejun-com/theme-eclipse/server'
import '@heejun-com/theme-eclipse/style.css'

export default function RootLayout({
  children,
}: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <body className={getTheme()}>
        {children}
      </body>
    </html>
  )
}`}
          </pre>
        </div>
      </div>
    </section>

    {/* Step 4: Components */}
    <section style={{ marginBottom: '48px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '20px' }}>
        <div style={{
          width: '28px', height: '28px', borderRadius: '8px', background: '#6366f1',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          color: '#ffffff', fontSize: '14px', fontWeight: '700', flexShrink: 0,
        }}>4</div>
        <h2 style={{ fontSize: '20px', fontWeight: '700', color: '#1e293b', margin: 0 }}>컴포넌트 사용</h2>
      </div>

      <div style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden', marginBottom: '24px' }}>
        <pre style={{ margin: 0, padding: '20px', background: '#0f172a', color: '#e2e8f0', fontSize: '13px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflow: 'auto' }}>
{`import { SolidButton, TextField, Toggle } from '@heejun-com/theme-eclipse'
import { BellIcon } from '@heejun-com/icons'

function MyForm() {
  return (
    <div>
      <TextField label="이름" placeholder="홍길동" />
      <Toggle label="알림 수신" />
      <SolidButton>
        <SolidButton.Leading><BellIcon /></SolidButton.Leading>
        <SolidButton.Center>저장하기</SolidButton.Center>
      </SolidButton>
    </div>
  )
}`}
        </pre>
      </div>

      <div style={{
        padding: '16px 20px', borderRadius: '10px',
        background: 'linear-gradient(90deg, rgba(99, 102, 241, 0.04) 0%, transparent 100%)',
        borderLeft: '3px solid #6366f1',
      }}>
        <p style={{ fontSize: '14px', color: '#475569', margin: 0, lineHeight: '1.6' }}>
          더 많은 컴포넌트와 사용 예시는 사이드바의 <strong>Eclipse</strong> 섹션에서 확인할 수 있습니다.
        </p>
      </div>
    </section>

    {/* MUI + Raycast 벤치마크 */}
    <section style={{ marginBottom: '48px' }}>
      <h2 style={{ fontSize: '20px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>
        마이그레이션 가이드
      </h2>
      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '20px' }}>
        MUI 또는 Raycast Extension UI에서 Orbit UI로 전환하는 핵심 패턴을 비교합니다. (Cycle 44 벤치마크)
      </p>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
        {[
          {
            from: 'MUI TextField + FormControl',
            to: 'Orbit UI TextField / PasswordField',
            code: `// MUI\n<FormControl error={hasError}>\n  <TextField label="이메일" variant="outlined" />\n  <FormHelperText>에러 메시지</FormHelperText>\n</FormControl>\n\n// Orbit UI\n<TextField\n  placeholder="이메일"\n  error={hasError}\n/>`,
            gain: '별도 FormControl 없이 error prop 단일 처리',
          },
          {
            from: 'MUI Snackbar + Alert',
            to: 'Orbit UI Toast (Sonner 기반)',
            code: `// MUI (Snackbar + Alert 조합)\n<Snackbar open={open} anchorOrigin={{ vertical: "bottom", horizontal: "center" }}>\n  <Alert severity="success">저장 완료</Alert>\n</Snackbar>\n\n// Orbit UI\ntoast.success("저장 완료", { description: "자동으로 닫힙니다" })`,
            gain: 'Provider 없이 toast() 함수 호출만으로 사용',
          },
          {
            from: 'Raycast Toast API',
            to: 'Orbit UI Toast action pattern',
            code: `// Raycast\nawait showToast({ title: "Done", style: Toast.Style.Success })\n\n// Orbit UI\ntoast.success("Done", {\n  action: { label: "실행취소", onClick: handleUndo }\n})`,
            gain: 'async/await 없이 즉시 호출, UNDO 액션 내장',
          },
        ].map(({ from, to, code, gain }) => (
          <div key={from} style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', borderBottom: '1px solid #f1f5f9' }}>
              <div style={{ padding: '10px 14px', background: '#fff5f5', borderRight: '1px solid #f1f5f9' }}>
                <div style={{ fontSize: '10px', fontWeight: 700, color: '#94a3b8', textTransform: 'uppercase', marginBottom: '2px' }}>Before</div>
                <div style={{ fontSize: '12px', color: '#ef4444', fontWeight: 600 }}>{from}</div>
              </div>
              <div style={{ padding: '10px 14px', background: '#f0fdf4' }}>
                <div style={{ fontSize: '10px', fontWeight: 700, color: '#94a3b8', textTransform: 'uppercase', marginBottom: '2px' }}>After</div>
                <div style={{ fontSize: '12px', color: '#10b981', fontWeight: 600 }}>{to}</div>
              </div>
            </div>
            <pre style={{ margin: 0, padding: '14px', background: '#0f172a', fontSize: '11px', color: '#e2e8f0', overflowX: 'auto', fontFamily: 'monospace', lineHeight: '1.6' }}>
              {code}
            </pre>
            <div style={{ padding: '8px 14px', background: '#f8fafc', fontSize: '11px', color: '#10b981', fontWeight: 600, borderTop: '1px solid #f1f5f9' }}>
              ✓ {gain}
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* MUI + Raycast 마이그레이션 가이드 */}
    <section style={{ marginBottom: '56px' }}>
      <h2 style={{ fontSize: '24px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>MUI / Raycast 패턴 → Orbit UI 전환</h2>
      <p style={{ fontSize: '14px', color: '#64748b', lineHeight: '1.7', marginBottom: '24px' }}>
        MUI의 Stepper, Accordion 패턴과 Raycast Extension의 커맨드 팔레트 패턴을 Orbit UI 컴포넌트로 구현하는 방법입니다.
      </p>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '16px', marginBottom: '28px' }}>
        {[
          {
            pattern: 'MUI Stepper',
            orbitComponents: 'PageIndicator + Progress',
            desc: '단계별 위자드 UI — PageIndicator가 현재 단계 표시, Progress가 완료율을 보여줍니다.',
            code: `// MUI
<Stepper activeStep={step}>
  <Step><StepLabel>정보 입력</StepLabel></Step>
  <Step><StepLabel>확인</StepLabel></Step>
</Stepper>

// Orbit UI
<PageIndicator currentPage={step} onPageChange={setStep}>
  {steps.map((_, i) => <span key={i} />)}
</PageIndicator>
<Progress value={(step / steps.length) * 100} />`,
          },
          {
            pattern: 'MUI Accordion + Summary',
            orbitComponents: 'Accordion.Item + Trigger',
            desc: '닫힌 상태에서 요약 정보를 표시하는 패턴 — Trigger 내부에 조건부로 요약 텍스트를 추가합니다.',
            code: `// MUI
<AccordionSummary expandIcon={<ExpandMoreIcon />}>
  <Typography>{title}</Typography>
  <Typography sx={{ color: 'text.secondary' }}>
    {isOpen ? '' : summary}
  </Typography>
</AccordionSummary>

// Orbit UI
<Accordion.Trigger>
  <div style={{ display: 'flex', justifyContent: 'space-between', width: '100%' }}>
    <span>{title}</span>
    {!isOpen && <span style={{ color: '#94a3b8' }}>{summary}</span>}
  </div>
</Accordion.Trigger>`,
          },
          {
            pattern: 'Raycast Section List',
            orbitComponents: 'Accordion (multiple) + LabelBadge',
            desc: '키보드 단축키가 있는 섹션별 커맨드 목록 — Accordion multiple 모드로 여러 섹션을 동시에 열 수 있습니다.',
            code: `// Raycast Extension
<List.Section title="최근 사용" subtitle={String(items.length)}>
  {items.map(item => (
    <List.Item title={item.title} subtitle={item.desc}
      actions={<Action.Push shortcut={{ modifiers: [], key: 'd' }} />}
    />
  ))}
</List.Section>

// Orbit UI
<Accordion type="multiple" value={openSections} onValueChange={setOpenSections}>
  {sections.map(section => (
    <Accordion.Item key={section.id} value={section.id}>
      <Accordion.Trigger>{section.title}</Accordion.Trigger>
      <Accordion.Content>
        {section.items.map(item => (
          <div key={item.label} style={{ display: 'flex', justifyContent: 'space-between' }}>
            <span>{item.label}</span>
            <kbd>{item.shortcut}</kbd>
          </div>
        ))}
      </Accordion.Content>
    </Accordion.Item>
  ))}
</Accordion>`,
          },
        ].map((item) => (
          <div key={item.pattern} style={{ borderRadius: '12px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
            <div style={{ padding: '12px 16px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0', display: 'flex', alignItems: 'center', gap: '10px' }}>
              <span style={{ fontSize: '13px', fontWeight: '700', color: '#0f172a' }}>{item.pattern}</span>
              <span style={{ fontSize: '11px', color: '#6366f1', background: '#eff6ff', padding: '2px 8px', borderRadius: '4px', fontWeight: '600' }}>
                → {item.orbitComponents}
              </span>
            </div>
            <div style={{ padding: '10px 16px', background: '#fffbf0', borderBottom: '1px solid #e2e8f0' }}>
              <p style={{ margin: 0, fontSize: '13px', color: '#64748b', lineHeight: '1.6' }}>{item.desc}</p>
            </div>
            <pre style={{ margin: 0, padding: '14px 16px', background: '#0f172a', color: '#e2e8f0', fontSize: '11px', lineHeight: '1.8', fontFamily: '"JetBrains Mono", monospace', overflowX: 'auto' }}>
              {item.code}
            </pre>
          </div>
        ))}
      </div>
    </section>

  </div>
</Unstyled>
