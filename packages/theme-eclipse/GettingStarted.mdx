import { Meta, Unstyled } from '@storybook/blocks'

<Meta title='Intro/시작하기' />

<Unstyled>
  <style>{`
    [data-orbit-doc] .orbit-step {
      display: grid;
      grid-template-columns: 84px minmax(0, 1fr);
      gap: 32px;
      padding: 40px 0;
      border-bottom: 1px solid var(--orbit-line);
      align-items: start;
    }
    [data-orbit-doc] .orbit-step:last-of-type { border-bottom: 0; }
    [data-orbit-doc] .orbit-step .step-mark {
      display: flex;
      flex-direction: column;
      gap: 4px;
      padding-top: 6px;
    }
    [data-orbit-doc] .orbit-step .step-num {
      font-family: var(--orbit-mono);
      font-size: 36px;
      font-weight: 480;
      letter-spacing: -0.02em;
      color: var(--orbit-ink);
      line-height: 1;
    }
    [data-orbit-doc] .orbit-step .step-tag {
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-step h2 {
      margin: 0 0 6px;
      font-size: 22px;
      font-weight: 680;
      letter-spacing: -0.018em;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-step .step-desc {
      margin: 0 0 20px;
      max-width: 58ch;
      font-size: 14.5px;
      line-height: 1.6;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-step .step-desc code,
    [data-orbit-doc] .orbit-step .step-note code {
      padding: 1px 6px;
      border-radius: 5px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink-2);
    }

    [data-orbit-doc] .orbit-pkgs {
      display: flex;
      flex-direction: column;
      border-top: 1px solid var(--orbit-line);
      border-bottom: 1px solid var(--orbit-line);
    }
    [data-orbit-doc] .orbit-pkgs .row {
      display: grid;
      grid-template-columns: 64px minmax(0, 1fr) auto;
      gap: 16px;
      align-items: center;
      padding: 14px 16px;
      border-top: 1px solid var(--orbit-line);
    }
    [data-orbit-doc] .orbit-pkgs .row:first-child { border-top: 0; }
    [data-orbit-doc] .orbit-pkgs .pm {
      font-family: var(--orbit-mono);
      font-size: 11px;
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      letter-spacing: 0.06em;
    }
    [data-orbit-doc] .orbit-pkgs code {
      font-family: var(--orbit-mono);
      font-size: 13px;
      color: var(--orbit-ink);
      background: transparent;
      border: 0;
      padding: 0;
      letter-spacing: -0.005em;
    }
    [data-orbit-doc] .orbit-pkgs .badge {
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      font-weight: 600;
      color: var(--orbit-accent);
      background: var(--orbit-accent-soft);
      padding: 2px 8px;
      border-radius: 999px;
      text-transform: uppercase;
      letter-spacing: 0.06em;
    }

    [data-orbit-doc] .orbit-code {
      margin: 0;
      border: 1px solid var(--orbit-line);
      border-radius: 12px;
      overflow: hidden;
      background: var(--orbit-surface);
    }
    [data-orbit-doc] .orbit-code-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 16px;
      padding: 9px 16px;
      border-bottom: 1px solid var(--orbit-line);
      background: var(--orbit-surface-sunken);
      font-family: var(--orbit-mono);
      font-size: 11px;
      color: var(--orbit-ink-3);
      letter-spacing: 0;
    }
    [data-orbit-doc] .orbit-code-head .tag {
      color: var(--orbit-ink-4);
      text-transform: uppercase;
      font-size: 10.5px;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-code pre {
      margin: 0;
      padding: 18px 20px;
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      line-height: 1.7;
      color: var(--orbit-ink);
      background: transparent;
      overflow-x: auto;
      white-space: pre;
    }

    [data-orbit-doc] .orbit-twocode {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
    }

    [data-orbit-doc] .orbit-props {
      margin-top: 20px;
      width: 100%;
      border-collapse: separate;
      border-spacing: 0;
      font-size: 13px;
    }
    [data-orbit-doc] .orbit-props thead th {
      padding: 12px 16px;
      border-top: 1px solid var(--orbit-line-2);
      border-bottom: 1px solid var(--orbit-line-2);
      font-family: var(--orbit-mono);
      font-size: 11px;
      font-weight: 600;
      color: var(--orbit-ink-4);
      text-align: left;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-props tbody td {
      padding: 14px 16px;
      border-bottom: 1px solid var(--orbit-line);
      vertical-align: top;
      color: var(--orbit-ink-2);
      line-height: 1.55;
    }
    [data-orbit-doc] .orbit-props tbody tr:last-child td {
      border-bottom: 1px solid var(--orbit-line-2);
    }
    [data-orbit-doc] .orbit-props code {
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-props .prop code {
      font-weight: 600;
      color: var(--orbit-accent);
    }
    [data-orbit-doc] .orbit-props .vals {
      font-family: var(--orbit-mono);
      font-size: 12px;
      color: var(--orbit-ink-3);
    }
    [data-orbit-doc] .orbit-props .def {
      font-family: var(--orbit-mono);
      font-size: 12px;
      color: var(--orbit-ink-4);
    }

    [data-orbit-doc] .orbit-note {
      margin: 16px 0 0;
      padding: 14px 18px;
      border: 1px solid var(--orbit-line);
      border-radius: 10px;
      background: var(--orbit-surface);
      font-size: 13.5px;
      line-height: 1.6;
      color: var(--orbit-ink-2);
      letter-spacing: -0.005em;
    }
    [data-orbit-doc] .orbit-note strong {
      display: block;
      margin-bottom: 4px;
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      font-weight: 700;
      color: var(--orbit-ink-3);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    @media (max-width: 760px) {
      [data-orbit-doc] .orbit-step { grid-template-columns: 1fr; gap: 12px; padding: 28px 0; }
      [data-orbit-doc] .orbit-step .step-mark { flex-direction: row; align-items: baseline; gap: 12px; padding-top: 0; }
      [data-orbit-doc] .orbit-step .step-num { font-size: 28px; }
      [data-orbit-doc] .orbit-pkgs .row { grid-template-columns: 56px 1fr; }
      [data-orbit-doc] .orbit-pkgs .row .badge { grid-column: 2; justify-self: start; }
      [data-orbit-doc] .orbit-twocode { grid-template-columns: 1fr; }
      [data-orbit-doc] .orbit-props thead th:nth-child(3),
      [data-orbit-doc] .orbit-props tbody td:nth-child(3) { display: none; }
    }
  `}</style>

  <div data-orbit-doc>
    <div className='orbit-wrap'>

      {/* HEADER */}
      <header className='orbit-page-head'>
        <div>
          <div className='orbit-eyebrow'><span className='num'>01</span>Getting Started</div>
          <h1 className='orbit-h1'>시작하기</h1>
          <p className='orbit-lede'>
            Eclipse 테마를 프로젝트에 적용하는 데 필요한 4단계. 설치 후 30초 안에 첫 컴포넌트를 렌더링할 수 있습니다.
          </p>
        </div>
        <div className='orbit-page-meta'>
          <div><strong>4</strong> steps</div>
          <div>~30s setup</div>
          <div>React 18·19</div>
        </div>
      </header>

      {/* STEP 1 */}
      <section className='orbit-step'>
        <div className='step-mark'>
          <div className='step-num'>01</div>
          <div className='step-tag'>Install</div>
        </div>
        <div>
          <h2>패키지 설치</h2>
          <p className='step-desc'>
            대부분의 프로젝트는 <code>theme-eclipse</code>와 <code>icons</code> 두 패키지면 충분합니다.
            헤드리스 layer가 필요하면 <code>@heejun-com/core</code>도 추가하세요.
          </p>

          <div className='orbit-pkgs'>
            <div className='row'>
              <span className='pm'>pnpm</span>
              <code>pnpm add @heejun-com/theme-eclipse @heejun-com/icons</code>
              <span className='badge'>recommended</span>
            </div>
            <div className='row'>
              <span className='pm'>npm</span>
              <code>npm install @heejun-com/theme-eclipse @heejun-com/icons</code>
              <span />
            </div>
            <div className='row'>
              <span className='pm'>yarn</span>
              <code>yarn add @heejun-com/theme-eclipse @heejun-com/icons</code>
              <span />
            </div>
          </div>
        </div>
      </section>

      {/* STEP 2 */}
      <section className='orbit-step'>
        <div className='step-mark'>
          <div className='step-num'>02</div>
          <div className='step-tag'>Provider</div>
        </div>
        <div>
          <h2>EclipseProvider 적용</h2>
          <p className='step-desc'>
            앱 루트에 <code>EclipseProvider</code>를 두고 글로벌 CSS를 import 합니다.
            테마 토큰, 폰트 스케일, Portal·Lottie 컨텍스트가 한 번에 주입됩니다.
          </p>

          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>TSX</span>
              <span>App.tsx</span>
            </div>
            <pre>{`import { EclipseProvider } from '@heejun-com/theme-eclipse'
import '@heejun-com/theme-eclipse/style.css'

function App() {
  return (
    <EclipseProvider mode='light'>
      <YourApp />
    </EclipseProvider>
  )
}`}</pre>
          </div>

          <table className='orbit-props'>
            <thead>
              <tr>
                <th style={{ width: '18%' }}>Prop</th>
                <th style={{ width: '34%' }}>Type</th>
                <th style={{ width: '14%' }}>Default</th>
                <th>설명</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td className='prop'><code>mode</code></td>
                <td className='vals'>'light' | 'dark'</td>
                <td className='def'>'light'</td>
                <td>라이트·다크 모드 전환. <code>prefers-color-scheme</code>와 연동 시 외부에서 제어.</td>
              </tr>
              <tr>
                <td className='prop'><code>platform</code></td>
                <td className='vals'>'mobile' | 'pc'</td>
                <td className='def'>'mobile'</td>
                <td>플랫폼 컨텍스트. 일부 컴포넌트의 hover·touch 분기에 영향.</td>
              </tr>
              <tr>
                <td className='prop'><code>baseTextSize</code></td>
                <td className='vals'>'small' | 'medium' | 'large'</td>
                <td className='def'>'medium'</td>
                <td>전체 타이포 스케일 기준. 모든 텍스트 스타일이 일관 비율로 조정.</td>
              </tr>
            </tbody>
          </table>
        </div>
      </section>

      {/* STEP 3 */}
      <section className='orbit-step'>
        <div className='step-mark'>
          <div className='step-num'>03</div>
          <div className='step-tag'>SSR</div>
        </div>
        <div>
          <h2>Server Side Rendering</h2>
          <p className='step-desc'>
            SSR 환경에서 초기 렌더 깜빡임을 방지하려면 <code>getTheme()</code>으로 클래스 이름을
            <code>html</code> 또는 <code>body</code>에 미리 박아 둡니다.
          </p>

          <div className='orbit-twocode'>
            <div className='orbit-code'>
              <div className='orbit-code-head'>
                <span className='tag'>Pages Router</span>
                <span>_document.tsx</span>
              </div>
              <pre>{`import { getTheme } from '@heejun-com/theme-eclipse/server'

export default function Document() {
  return (
    <Html lang='ko'>
      <Head />
      <body className={getTheme()}>
        <Main />
        <NextScript />
      </body>
    </Html>
  )
}`}</pre>
            </div>
            <div className='orbit-code'>
              <div className='orbit-code-head'>
                <span className='tag'>App Router</span>
                <span>app/layout.tsx</span>
              </div>
              <pre>{`import { getTheme } from '@heejun-com/theme-eclipse/server'
import '@heejun-com/theme-eclipse/style.css'

export default function RootLayout({
  children,
}: { children: React.ReactNode }) {
  return (
    <html lang='ko'>
      <body className={getTheme()}>
        {children}
      </body>
    </html>
  )
}`}</pre>
            </div>
          </div>

          <aside className='orbit-note'>
            <strong>Why this matters</strong>
            클라이언트 하이드레이션 전에 테마 클래스가 박혀 있어야 토큰이 즉시 적용됩니다. 그렇지 않으면 첫 페인트에서 light로 그렸다가 dark로 점프하는 깜빡임이 발생합니다.
          </aside>
        </div>
      </section>

      {/* STEP 4 */}
      <section className='orbit-step'>
        <div className='step-mark'>
          <div className='step-num'>04</div>
          <div className='step-tag'>Compose</div>
        </div>
        <div>
          <h2>첫 컴포넌트 조립</h2>
          <p className='step-desc'>
            슬롯 컴파운드 패턴으로 컴포넌트 내부를 구성합니다. <code>Leading</code> · <code>Center</code> · <code>Trailing</code>{' '}
            세 슬롯이 시각과 콘텐츠를 분리합니다.
          </p>

          <div className='orbit-code'>
            <div className='orbit-code-head'>
              <span className='tag'>TSX</span>
              <span>MyForm.tsx</span>
            </div>
            <pre>{`import { SolidButton, FloatingTextField, Switch } from '@heejun-com/theme-eclipse'
import { IconBell, IconPlus } from '@heejun-com/icons'

export function MyForm() {
  return (
    <form>
      <FloatingTextField label='이름' placeholder='홍길동' />
      <Switch label='알림 수신' />

      <SolidButton color='primary' size='medium'>
        <SolidButton.Leading>
          <IconPlus />
        </SolidButton.Leading>
        <SolidButton.Center>
          저장하기
        </SolidButton.Center>
      </SolidButton>
    </form>
  )
}`}</pre>
          </div>

          <aside className='orbit-note'>
            <strong>Next</strong>
            전체 컴포넌트 카탈로그는 사이드바의 <code>Eclipse</code> 섹션 또는 <a href='?path=/docs/intro-컴포넌트-개요--docs' style={{ color: 'var(--orbit-accent)', textDecoration: 'none' }}>컴포넌트 개요</a>에서 확인할 수 있습니다.
          </aside>
        </div>
      </section>

      {/* FOOTER */}
      <footer className='orbit-foot'>
        <div>4 steps · pnpm·npm·yarn · CSR + SSR</div>
        <div>
          <a href='?path=/docs/intro-컴포넌트-개요--docs'>컴포넌트 카탈로그 →</a>
        </div>
      </footer>

    </div>
  </div>
</Unstyled>
