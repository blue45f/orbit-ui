import { Meta, Unstyled } from '@storybook/blocks'

<Meta title='Intro/마이그레이션 가이드' />

<Unstyled>
  <style>{`
    [data-orbit-doc] .orbit-tabs {
      margin-top: 24px;
      display: flex;
      flex-wrap: wrap;
      gap: 2px;
      padding: 4px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      border-radius: 10px;
      width: fit-content;
    }
    [data-orbit-doc] .orbit-tabs a {
      padding: 6px 14px;
      border-radius: 7px;
      font-family: var(--orbit-display);
      font-size: 13px;
      font-weight: 600;
      letter-spacing: -0.012em;
      color: var(--orbit-ink-3);
      text-decoration: none !important;
      transition:
        background-color 160ms cubic-bezier(0.2, 0, 0, 1),
        color 160ms cubic-bezier(0.2, 0, 0, 1);
    }
    [data-orbit-doc] .orbit-tabs a:hover {
      color: var(--orbit-ink);
      background: var(--orbit-surface);
    }

    [data-orbit-doc] .orbit-map {
      margin-top: 24px;
      width: 100%;
      border-collapse: separate;
      border-spacing: 0;
      font-size: 13px;
    }
    [data-orbit-doc] .orbit-map thead th {
      padding: 12px 16px;
      border-top: 1px solid var(--orbit-line-2);
      border-bottom: 1px solid var(--orbit-line-2);
      font-family: var(--orbit-mono);
      font-size: 11px;
      font-weight: 600;
      color: var(--orbit-ink-4);
      text-align: left;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-map tbody td {
      padding: 12px 16px;
      border-bottom: 1px solid var(--orbit-line);
      vertical-align: top;
      color: var(--orbit-ink-2);
      line-height: 1.55;
    }
    [data-orbit-doc] .orbit-map tbody tr:last-child td {
      border-bottom: 1px solid var(--orbit-line-2);
    }
    [data-orbit-doc] .orbit-map .from {
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-ink);
      font-weight: 580;
    }
    [data-orbit-doc] .orbit-map .arrow {
      width: 24px;
      color: var(--orbit-ink-4);
      font-family: var(--orbit-mono);
      text-align: center;
    }
    [data-orbit-doc] .orbit-map .to {
      font-family: var(--orbit-mono);
      font-size: 12.5px;
      color: var(--orbit-accent);
      font-weight: 580;
    }
    [data-orbit-doc] .orbit-map .note {
      font-size: 12.5px;
      color: var(--orbit-ink-3);
    }

    [data-orbit-doc] .orbit-diff {
      margin-top: 24px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
    }
    [data-orbit-doc] .orbit-diff .pane {
      border: 1px solid var(--orbit-line);
      border-radius: 12px;
      overflow: hidden;
      background: var(--orbit-surface);
    }
    [data-orbit-doc] .orbit-diff .pane .head {
      padding: 9px 16px;
      border-bottom: 1px solid var(--orbit-line);
      background: var(--orbit-surface-sunken);
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: 12px;
      font-family: var(--orbit-mono);
      font-size: 11px;
      color: var(--orbit-ink-3);
    }
    [data-orbit-doc] .orbit-diff .pane .tag {
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-diff .pane.from .tag { color: var(--orbit-ink-4); }
    [data-orbit-doc] .orbit-diff .pane.to .tag { color: var(--orbit-accent); }
    [data-orbit-doc] .orbit-diff .pane pre {
      margin: 0;
      padding: 16px 18px;
      font-family: var(--orbit-mono);
      font-size: 12px;
      line-height: 1.7;
      color: var(--orbit-ink);
      background: transparent;
      overflow-x: auto;
      white-space: pre;
    }

    [data-orbit-doc] .orbit-source {
      margin-top: 40px;
      padding-bottom: 40px;
      border-bottom: 1px solid var(--orbit-line);
    }
    [data-orbit-doc] .orbit-source:last-of-type { border-bottom: 0; padding-bottom: 0; }
    [data-orbit-doc] .orbit-source h3 {
      margin: 0;
      font-size: 22px;
      font-weight: 680;
      letter-spacing: -0.02em;
      color: var(--orbit-ink);
    }
    [data-orbit-doc] .orbit-source .summary {
      margin: 8px 0 0;
      max-width: 60ch;
      font-size: 14px;
      line-height: 1.6;
      color: var(--orbit-ink-3);
    }

    [data-orbit-doc] .orbit-note {
      margin-top: 16px;
      padding: 12px 16px;
      border: 1px solid var(--orbit-line);
      border-radius: 10px;
      background: var(--orbit-surface);
      font-size: 13px;
      line-height: 1.55;
      color: var(--orbit-ink-2);
    }
    [data-orbit-doc] .orbit-note strong {
      display: block;
      margin-bottom: 4px;
      font-family: var(--orbit-mono);
      font-size: 10.5px;
      font-weight: 700;
      color: var(--orbit-ink-3);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    [data-orbit-doc] .orbit-note code {
      padding: 1px 6px;
      border-radius: 5px;
      background: var(--orbit-surface-sunken);
      border: 1px solid var(--orbit-line);
      font-family: var(--orbit-mono);
      font-size: 12px;
      color: var(--orbit-ink-2);
    }

    @media (max-width: 760px) {
      [data-orbit-doc] .orbit-diff { grid-template-columns: 1fr; }
      [data-orbit-doc] .orbit-map thead th:nth-child(4),
      [data-orbit-doc] .orbit-map tbody td:nth-child(4) { display: none; }
    }
  `}</style>

  <div data-orbit-doc>
    <div className='orbit-wrap'>

      {/* HEADER */}
      <header className='orbit-page-head'>
        <div>
          <div className='orbit-eyebrow'><span className='num'>01</span>Migration</div>
          <h1 className='orbit-h1'>마이그레이션 가이드</h1>
          <div className='orbit-lede'>
            널리 쓰이는 헤드리스·디자인 시스템에서 Orbit UI로 옮길 때의 컴포넌트 대응표와 코드 전환 예시.
            한 번에 다 바꿀 필요는 없습니다, 페이지 단위로 점진 마이그레이션이 권장됩니다.
          </div>
        </div>
        <div className='orbit-page-meta'>
          <div><strong>3</strong> sources</div>
          <div>incremental</div>
          <div>codemod-friendly</div>
        </div>
      </header>

      {/* QUICK NAV */}
      <nav className='orbit-tabs' aria-label='Sources'>
        <a href='#composable'>ComposableUI</a>
        <a href='#enterprise'>EnterpriseUI</a>
        <a href='#headless'>Headless Toolkit</a>
      </nav>

      {/* COMPOSABLE UI */}
      <section className='orbit-sect orbit-source' id='composable'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>02</span>Source 01</div>
            <h3>ComposableUI → Orbit UI</h3>
            <div className='summary'>FormControl 패턴 + variant prop 기반 라이브러리. Orbit UI에서는 variant가 별도 컴포넌트로 분리됩니다.</div>
          </div>
        </div>

        <table className='orbit-map'>
          <thead>
            <tr>
              <th style={{ width: '24%' }}>From</th>
              <th style={{ width: '4%' }}></th>
              <th style={{ width: '32%' }}>To (Orbit UI)</th>
              <th>설명</th>
            </tr>
          </thead>
          <tbody>
            <tr><td className='from'>Button</td><td className='arrow'>→</td><td className='to'>SolidButton · OutlineButton · GhostButton</td><td className='note'>variant prop 대신 별도 컴포넌트.</td></tr>
            <tr><td className='from'>Input</td><td className='arrow'>→</td><td className='to'>TextField · FloatingTextField</td><td className='note'>플로팅 라벨이 필요하면 FloatingTextField.</td></tr>
            <tr><td className='from'>Checkbox</td><td className='arrow'>→</td><td className='to'>Checkbox · BoxedCheckbox</td><td className='note'>박스형 변형은 BoxedCheckbox.</td></tr>
            <tr><td className='from'>Radio</td><td className='arrow'>→</td><td className='to'>RadioButton</td><td className='note'>Group 컴포넌트는 fieldset + legend 직접 구성.</td></tr>
            <tr><td className='from'>Switch</td><td className='arrow'>→</td><td className='to'>Switch</td><td className='note'>API 거의 동일.</td></tr>
            <tr><td className='from'>Modal</td><td className='arrow'>→</td><td className='to'>Modal · AlertDialog</td><td className='note'>경고·확인 다이얼로그는 AlertDialog.</td></tr>
            <tr><td className='from'>Tooltip</td><td className='arrow'>→</td><td className='to'>Tooltip · HoverCard</td><td className='note'>리치 콘텐츠는 HoverCard.</td></tr>
            <tr><td className='from'>FormControl, FormLabel, FormHelperText</td><td className='arrow'>→</td><td className='to'>없음</td><td className='note'>label + helper 텍스트를 직접 구성. <code>htmlFor</code>·<code>aria-describedby</code> 명시.</td></tr>
            <tr><td className='from'>Badge</td><td className='arrow'>→</td><td className='to'>LabelBadge · CounterBadge · AnimatedBadge</td><td className='note'>용도에 따라 분리.</td></tr>
          </tbody>
        </table>

        <div className='orbit-diff'>
          <div className='pane from'>
            <div className='head'>
              <span className='tag'>before</span>
              <span>ComposableUI</span>
            </div>
            <pre>{`<FormControl isInvalid={!!error}>
  <FormLabel>이메일</FormLabel>
  <Input
    variant='outlined'
    placeholder='you@example.com'
    value={email}
    onChange={(e) => setEmail(e.target.value)}
  />
  <FormHelperText>{error}</FormHelperText>
</FormControl>`}</pre>
          </div>
          <div className='pane to'>
            <div className='head'>
              <span className='tag'>after</span>
              <span>Orbit UI</span>
            </div>
            <pre>{`<div>
  <FloatingTextField
    label='이메일'
    placeholder='you@example.com'
    value={email}
    onChange={(e) => setEmail(e.target.value)}
    error={!!error}
    helperText={error}
  />
</div>`}</pre>
          </div>
        </div>
      </section>

      {/* ENTERPRISE UI */}
      <section className='orbit-sect orbit-source' id='enterprise'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>03</span>Source 02</div>
            <h3>EnterpriseUI → Orbit UI</h3>
            <div className='summary'>JS 객체 테마 + Snackbar·Alert 조합 기반 시스템. Orbit UI는 CSS 변수 토큰과 단일 토스트 API로 단순화.</div>
          </div>
        </div>

        <table className='orbit-map'>
          <thead>
            <tr>
              <th style={{ width: '24%' }}>From</th>
              <th style={{ width: '4%' }}></th>
              <th style={{ width: '32%' }}>To (Orbit UI)</th>
              <th>설명</th>
            </tr>
          </thead>
          <tbody>
            <tr><td className='from'>createTheme + ThemeProvider</td><td className='arrow'>→</td><td className='to'>CSS 변수 + EclipseProvider</td><td className='note'>객체 대신 <code>--ref-*</code> CSS 변수 오버라이드.</td></tr>
            <tr><td className='from'>Snackbar + Alert</td><td className='arrow'>→</td><td className='to'>toast.success · toast.error</td><td className='note'>Provider 없이 함수 호출.</td></tr>
            <tr><td className='from'>Stepper + Step + StepLabel</td><td className='arrow'>→</td><td className='to'>PageIndicator + Progress</td><td className='note'>인디케이터 + 진행률 분리.</td></tr>
            <tr><td className='from'>Accordion + AccordionSummary</td><td className='arrow'>→</td><td className='to'>Accordion.Item + Accordion.Trigger</td><td className='note'>슬롯 컴파운드.</td></tr>
            <tr><td className='from'>TextField variant='outlined'</td><td className='arrow'>→</td><td className='to'>FloatingTextField</td><td className='note'>error prop으로 단일 처리.</td></tr>
            <tr><td className='from'>IconButton</td><td className='arrow'>→</td><td className='to'>SolidIconButton · OutlineIconButton</td><td className='note'>variant에 맞춰 분리.</td></tr>
            <tr><td className='from'>Chip + onDelete</td><td className='arrow'>→</td><td className='to'>Chip + Chip.Trailing</td><td className='note'>슬롯에 IconClose.</td></tr>
            <tr><td className='from'>Skeleton + variant='text'</td><td className='arrow'>→</td><td className='to'>Skeleton</td><td className='note'>variant 없이 width·height 직접.</td></tr>
          </tbody>
        </table>

        <div className='orbit-diff'>
          <div className='pane from'>
            <div className='head'>
              <span className='tag'>before</span>
              <span>EnterpriseUI toast</span>
            </div>
            <pre>{`<Snackbar
  open={open}
  anchorOrigin={{
    vertical: 'bottom',
    horizontal: 'center',
  }}
>
  <Alert severity='success'>
    저장 완료
  </Alert>
</Snackbar>`}</pre>
          </div>
          <div className='pane to'>
            <div className='head'>
              <span className='tag'>after</span>
              <span>Orbit UI toast</span>
            </div>
            <pre>{`toast.success('저장 완료', {
  description: '자동으로 닫힙니다',
  action: {
    label: '실행취소',
    onClick: handleUndo,
  },
})`}</pre>
          </div>
        </div>

        <aside className='orbit-note'>
          <strong>토스트 마이그레이션 팁</strong>
          Orbit UI의 toast는 Sonner 기반으로 Provider 없이 호출 가능합니다. <code>toast.success</code>·<code>toast.error</code>·<code>toast.warning</code>·<code>toast.info</code>로 의미를 분리.
        </aside>
      </section>

      {/* HEADLESS TOOLKIT */}
      <section className='orbit-sect orbit-source' id='headless'>
        <div className='orbit-sect-head'>
          <div>
            <div className='orbit-eyebrow'><span className='num'>04</span>Source 03</div>
            <h3>Headless Toolkit → Orbit UI</h3>
            <div className='summary'>스타일 없는 헤드리스 프리미티브 라이브러리에서 옮길 때. ARIA·키보드는 거의 일대일 매핑.</div>
          </div>
        </div>

        <table className='orbit-map'>
          <thead>
            <tr>
              <th style={{ width: '24%' }}>From</th>
              <th style={{ width: '4%' }}></th>
              <th style={{ width: '32%' }}>To (Orbit UI)</th>
              <th>설명</th>
            </tr>
          </thead>
          <tbody>
            <tr><td className='from'>Dialog.Root + Trigger + Content</td><td className='arrow'>→</td><td className='to'>Modal + Modal.Trigger + Modal.Content</td><td className='note'>슬롯 패턴 동일.</td></tr>
            <tr><td className='from'>Popover.Root + Trigger + Content</td><td className='arrow'>→</td><td className='to'>Popover + Popover.Trigger + Popover.Content</td><td className='note'>API 동일.</td></tr>
            <tr><td className='from'>Tooltip.Provider + Tooltip + Trigger</td><td className='arrow'>→</td><td className='to'>Tooltip (Provider 없음)</td><td className='note'>EclipseProvider가 자동.</td></tr>
            <tr><td className='from'>DropdownMenu.Root + Content + Item</td><td className='arrow'>→</td><td className='to'>Dropdown + Dropdown.Item</td><td className='note'>API 거의 동일.</td></tr>
            <tr><td className='from'>Toggle / Switch</td><td className='arrow'>→</td><td className='to'>Switch</td><td className='note'>동일.</td></tr>
            <tr><td className='from'>Tabs.Root + List + Trigger + Content</td><td className='arrow'>→</td><td className='to'>TabGroup + TabItem</td><td className='note'>슬롯 명 약간 상이.</td></tr>
            <tr><td className='from'>Accordion.Root + Item + Trigger + Content</td><td className='arrow'>→</td><td className='to'>Accordion + Accordion.Item + Accordion.Trigger + Accordion.Content</td><td className='note'>API 동일.</td></tr>
            <tr><td className='from'>Slot</td><td className='arrow'>→</td><td className='to'><code>as</code> 프롭</td><td className='note'>asChild 패턴 대신 polymorphic as.</td></tr>
          </tbody>
        </table>

        <div className='orbit-diff'>
          <div className='pane from'>
            <div className='head'>
              <span className='tag'>before</span>
              <span>Headless toolkit (asChild)</span>
            </div>
            <pre>{`<Button asChild>
  <Link href='/dashboard'>
    대시보드로
  </Link>
</Button>`}</pre>
          </div>
          <div className='pane to'>
            <div className='head'>
              <span className='tag'>after</span>
              <span>Orbit UI (as prop)</span>
            </div>
            <pre>{`<SolidButton as={Link} href='/dashboard'>
  <SolidButton.Center>
    대시보드로
  </SolidButton.Center>
</SolidButton>`}</pre>
          </div>
        </div>

        <aside className='orbit-note'>
          <strong>점진 마이그레이션 전략</strong>
          기존 라이브러리와 Orbit UI는 한 페이지에 공존 가능합니다. 가장 자주 쓰는 컴포넌트 (Button, TextField) 먼저 옮기고
          페이지 단위로 점진 교체하세요. CSS 변수 충돌만 주의 (네임스페이스가 다르므로 일반적으로 안전).
        </aside>
      </section>

      {/* FOOTER */}
      <footer className='orbit-foot'>
        <div>3 sources · incremental · codemod-friendly</div>
        <div>
          <a href='?path=/docs/intro-시작하기--docs'>시작하기 →</a>
        </div>
      </footer>

    </div>
  </div>
</Unstyled>
