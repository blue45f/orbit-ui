import { Meta, Unstyled } from '@storybook/blocks'

<Meta title='Intro/컴포넌트 개요' />

<Unstyled>
  <div style={{
    maxWidth: '960px',
    margin: '0 auto',
    fontFamily: '"Pretendard Variable", "Pretendard", -apple-system, BlinkMacSystemFont, system-ui, sans-serif',
  }}>

    {/* Header */}
    <div style={{ marginBottom: '48px' }}>
      <div style={{ fontSize: '12px', fontWeight: '700', color: '#6366f1', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '8px' }}>Component Overview</div>
      <h1 style={{ fontSize: '36px', fontWeight: '800', color: '#1e293b', letterSpacing: '-0.03em', margin: '0 0 12px' }}>컴포넌트 개요</h1>
      <p style={{ fontSize: '16px', color: '#64748b', lineHeight: '1.6', margin: 0 }}>50개 이상의 프로덕션 레디 컴포넌트로 구성된 완전한 디자인 시스템</p>
    </div>

    {/* Design Principles */}
    <section style={{ marginBottom: '64px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Design Principles</h2>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '16px' }}>
        {[
          {
            title: 'Soft Elevation',
            desc: '빛의 물리적 특성을 고려한 다층적 그림자 시스템으로 자연스러운 깊이감을 제공합니다.',
            gradient: 'linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%)',
          },
          {
            title: 'Micro-Interactions',
            desc: '모든 상호작용에 물리 기반 애니메이션을 적용하여 생동감 있는 피드백을 전달합니다.',
            gradient: 'linear-gradient(135deg, #3b82f6 0%, #06b6d4 100%)',
          },
          {
            title: 'Glassmorphism',
            desc: '투명도와 블러 효과의 SurfaceGlass 토큰으로 배경과 콘텐츠의 깊이를 표현합니다.',
            gradient: 'linear-gradient(135deg, #8b5cf6 0%, #ec4899 100%)',
          },
        ].map((item, i) => (
          <div key={i} style={{
            borderRadius: '16px',
            overflow: 'hidden',
          }}>
            <div style={{
              height: '4px',
              background: item.gradient,
            }} />
            <div style={{
              padding: '24px',
              border: '1px solid #e2e8f0',
              borderTop: 'none',
              borderRadius: '0 0 16px 16px',
            }}>
              <h3 style={{ fontSize: '16px', fontWeight: '700', color: '#1e293b', margin: '0 0 8px' }}>{item.title}</h3>
              <p style={{ fontSize: '13px', color: '#64748b', lineHeight: '1.65', margin: 0 }}>{item.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* Component Map */}
    <section style={{ marginBottom: '64px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Component Map</h2>

      {[
        {
          category: 'Actions',
          color: '#6366f1',
          bg: 'rgba(99, 102, 241, 0.04)',
          components: [
            { name: 'SolidButton', desc: 'Primary CTA 버튼' },
            { name: 'OutlineButton', desc: '보조 액션 버튼' },
            { name: 'GhostButton', desc: '최소 스타일 버튼' },
            { name: 'SolidIconButton', desc: '아이콘 전용 버튼' },
            { name: 'Chip', desc: '필터/태그 칩' },
            { name: 'TabGroup', desc: '탭 네비게이션' },
            { name: 'SegmentedControl', desc: '분절 선택 컨트롤' },
          ],
        },
        {
          category: 'Inputs',
          color: '#8b5cf6',
          bg: 'rgba(139, 92, 246, 0.04)',
          components: [
            { name: 'TextField', desc: '텍스트 입력 필드' },
            { name: 'FloatingTextField', desc: '플로팅 라벨 필드' },
            { name: 'PasswordField', desc: '비밀번호 입력' },
            { name: 'SearchBar', desc: '검색 입력바' },
            { name: 'Checkbox', desc: '체크박스 선택' },
            { name: 'RadioButton', desc: '라디오 선택' },
            { name: 'Toggle / Switch', desc: '온오프 전환' },
            { name: 'Dropdown', desc: '드롭다운 선택' },
            { name: 'Slider', desc: '범위 슬라이더' },
            { name: 'Calendar', desc: '날짜 선택기' },
          ],
        },
        {
          category: 'Data Display',
          color: '#3b82f6',
          bg: 'rgba(59, 130, 246, 0.04)',
          components: [
            { name: 'DataTable', desc: '정렬/선택/페이지네이션 테이블' },
            { name: 'Avatar', desc: '사용자 아바타' },
            { name: 'LabelBadge', desc: '상태 라벨 배지' },
            { name: 'CounterBadge', desc: '수치 카운터 배지' },
            { name: 'AnimatedBadge', desc: '애니메이션 배지' },
            { name: 'ListTile', desc: '리스트 타일' },
            { name: 'Accordion', desc: '접기/펼치기' },
            { name: 'Carousel', desc: '캐러셀 슬라이더' },
          ],
        },
        {
          category: 'Feedback',
          color: '#f59e0b',
          bg: 'rgba(245, 158, 11, 0.04)',
          components: [
            { name: 'Modal', desc: '다이얼로그 모달' },
            { name: 'AlertDialog', desc: '확인/경고 다이얼로그' },
            { name: 'Toast', desc: '토스트 알림' },
            { name: 'Tooltip', desc: '툴팁 오버레이' },
            { name: 'HoverCard', desc: '호버 카드 팝업' },
            { name: 'Popover', desc: '팝오버 컨텐츠' },
            { name: 'Progress', desc: '진행률 표시' },
            { name: 'Skeleton', desc: '로딩 스켈레톤' },
            { name: 'Loading', desc: '로딩 스피너' },
          ],
        },
        {
          category: 'Navigation',
          color: '#10b981',
          bg: 'rgba(16, 185, 129, 0.04)',
          components: [
            { name: 'AppBar', desc: '앱 상단바' },
            { name: 'Breadcrumb', desc: '경로 탐색' },
            { name: 'ScrollableTabGroup', desc: '스크롤 탭' },
            { name: 'PageIndicator', desc: '페이지 인디케이터' },
            { name: 'PageNumber', desc: '페이지 번호' },
            { name: 'PageDots', desc: '페이지 도트' },
          ],
        },
      ].map((section, i) => (
        <div key={i} style={{ marginBottom: '24px' }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: '10px',
            marginBottom: '12px',
          }}>
            <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: section.color }} />
            <h3 style={{ fontSize: '16px', fontWeight: '700', color: '#1e293b', margin: 0 }}>{section.category}</h3>
            <span style={{ fontSize: '12px', color: '#94a3b8', fontWeight: '500' }}>{section.components.length} components</span>
          </div>

          <div style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))',
            gap: '8px',
          }}>
            {section.components.map((comp, j) => (
              <div key={j} style={{
                padding: '12px 16px',
                borderRadius: '8px',
                border: '1px solid #f1f5f9',
                background: section.bg,
              }}>
                <div style={{ fontSize: '13px', fontWeight: '600', color: '#1e293b' }}>{comp.name}</div>
                <div style={{ fontSize: '11px', color: '#94a3b8', marginTop: '2px' }}>{comp.desc}</div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </section>

    {/* Design Guidelines */}
    <section style={{ marginBottom: '40px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '24px' }}>Design Guidelines</h2>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '12px' }}>
        {[
          { title: '8px Grid', desc: '모든 간격과 크기는 8px 단위(0.5rem)로 설계', icon: '#' },
          { title: 'Type-safe', desc: 'TypeScript로 강력한 타입 추론과 자동완성 제공', icon: 'TS' },
          { title: 'Accessible', desc: 'WCAG AA 등급 준수, WAI-ARIA 패턴 적용', icon: 'A' },
        ].map((item, i) => (
          <div key={i} style={{
            padding: '24px',
            borderRadius: '12px',
            background: 'linear-gradient(135deg, rgba(99, 102, 241, 0.04), rgba(168, 85, 247, 0.04))',
            border: '1px solid rgba(99, 102, 241, 0.08)',
            textAlign: 'center',
          }}>
            <div style={{
              width: '36px', height: '36px', borderRadius: '8px',
              background: '#6366f1', color: '#ffffff',
              display: 'inline-flex', alignItems: 'center', justifyContent: 'center',
              fontSize: '13px', fontWeight: '800', marginBottom: '12px',
              fontFamily: '"JetBrains Mono", monospace',
            }}>{item.icon}</div>
            <div style={{ fontSize: '15px', fontWeight: '700', color: '#1e293b', marginBottom: '4px' }}>{item.title}</div>
            <div style={{ fontSize: '12px', color: '#64748b', lineHeight: '1.5' }}>{item.desc}</div>
          </div>
        ))}
      </div>
    </section>

    {/* Do / Don't */}
    <section style={{ marginBottom: '64px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>Do / Don't</h2>
      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '28px' }}>컴포넌트를 올바르게 사용하는 방법과 피해야 할 패턴을 안내합니다.</p>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '20px' }}>
        {[
          {
            topic: 'SolidButton — 컴파운드 패턴',
            do: 'Center, Leading, Trailing 서브컴포넌트를 사용해 버튼 내부 레이아웃을 구성합니다.',
            dont: '버튼 내부에 임의의 div나 span을 직접 삽입하면 스타일과 접근성이 깨질 수 있습니다.',
            doCode: `<SolidButton color="primary" size="medium">
  <SolidButton.Leading><PlusIcon /></SolidButton.Leading>
  <SolidButton.Center>항목 추가</SolidButton.Center>
</SolidButton>`,
            dontCode: `<SolidButton color="primary" size="medium">
  <div style={{ display: 'flex', gap: 8 }}>
    <PlusIcon />
    항목 추가
  </div>
</SolidButton>`,
          },
          {
            topic: 'Toast — 피드백 타입 분리',
            do: 'toast.success / toast.error / toast.warning / toast.info를 상황에 맞게 사용합니다.',
            dont: '모든 피드백에 기본 toast()를 사용하면 사용자가 결과를 구분하기 어렵습니다.',
            doCode: `// 저장 성공
toast.success('변경사항이 저장되었습니다.')

// 네트워크 오류
toast.error('요청에 실패했습니다.', {
  description: '잠시 후 다시 시도해 주세요.',
})`,
            dontCode: `// 타입 구분 없이 모두 동일
toast('저장되었습니다.')
toast('실패했습니다.')
toast('주의가 필요합니다.')`,
          },
          {
            topic: 'Toggle — 제어 vs 비제어',
            do: 'controlled(제어) 사용 시 checked + onCheckedChange를 함께 지정합니다.',
            dont: 'checked만 지정하고 onCheckedChange를 생략하면 상태가 변경되지 않습니다.',
            doCode: `const [enabled, setEnabled] = useState(false)

<Toggle
  checked={enabled}
  onCheckedChange={setEnabled}
/>`,
            dontCode: `// onCheckedChange 없으면 동작 안 함
<Toggle checked={true} />`,
          },
          {
            topic: 'API 일관성 — color / size / disabled',
            do: '모든 인터랙티브 컴포넌트는 동일한 color, size, disabled 인터페이스를 따릅니다.',
            dont: '컴포넌트마다 다른 prop 이름을 사용하거나 HTML 속성을 직접 조작하지 않습니다.',
            doCode: `// 통일된 인터페이스
<SolidButton color="primary" size="medium" disabled />
<OutlineButton color="black" size="large" disabled />
<GhostButton color="gray" size="small" disabled />`,
            dontCode: `// 임의로 색상을 인라인 스타일로 변경
<SolidButton style={{ background: '#ff0000' }}>
  버튼
</SolidButton>`,
          },
        ].map(({ topic, do: doText, dont, doCode, dontCode }, i) => (
          <div key={i} style={{ borderRadius: '14px', border: '1.5px solid #e2e8f0', overflow: 'hidden' }}>
            <div style={{ padding: '14px 20px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0' }}>
              <span style={{ fontSize: '13px', fontWeight: 700, color: '#475569' }}>{topic}</span>
            </div>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 0 }}>
              <div style={{ padding: '20px', borderRight: '1px solid #e2e8f0' }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: '6px', marginBottom: '10px' }}>
                  <div style={{ width: '20px', height: '20px', borderRadius: '50%', background: '#10b981', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '11px', fontWeight: 800, color: '#fff' }}>O</div>
                  <span style={{ fontSize: '12px', fontWeight: 700, color: '#10b981' }}>Do</span>
                </div>
                <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6', margin: '0 0 12px' }}>{doText}</p>
                <pre style={{ margin: 0, padding: '12px', borderRadius: '8px', background: '#f0fdf4', border: '1px solid #bbf7d0', fontSize: '11px', color: '#15803d', lineHeight: '1.6', overflowX: 'auto' }}>{doCode}</pre>
              </div>
              <div style={{ padding: '20px' }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: '6px', marginBottom: '10px' }}>
                  <div style={{ width: '20px', height: '20px', borderRadius: '50%', background: '#ef4444', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '11px', fontWeight: 800, color: '#fff' }}>X</div>
                  <span style={{ fontSize: '12px', fontWeight: 700, color: '#ef4444' }}>Don't</span>
                </div>
                <p style={{ fontSize: '13px', color: '#475569', lineHeight: '1.6', margin: '0 0 12px' }}>{dont}</p>
                <pre style={{ margin: 0, padding: '12px', borderRadius: '8px', background: '#fef2f2', border: '1px solid #fecaca', fontSize: '11px', color: '#b91c1c', lineHeight: '1.6', overflowX: 'auto' }}>{dontCode}</pre>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* Accessibility Checklist — Mantine style */}
    <section style={{ marginBottom: '64px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>접근성 체크리스트</h2>
      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '24px' }}>Orbit UI 컴포넌트를 사용하기 전에 아래 체크리스트를 확인하세요. WAI-ARIA 패턴을 기반으로 작성되었습니다.</p>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '16px' }}>
        {[
          {
            category: '키보드 내비게이션',
            color: '#6366f1',
            items: [
              { label: 'Tab 키로 포커스 이동 가능', required: true },
              { label: 'Enter / Space 키로 버튼·체크박스 활성화', required: true },
              { label: '화살표 키로 라디오 그룹 탐색', required: true },
              { label: 'Escape 키로 모달·팝오버 닫기', required: true },
              { label: '포커스 트랩: 모달 내부에서만 Tab 순환', required: false },
            ],
          },
          {
            category: 'ARIA 속성',
            color: '#3b82f6',
            items: [
              { label: 'aria-label 또는 aria-labelledby 지정', required: true },
              { label: 'aria-disabled로 비활성 상태 표현', required: true },
              { label: 'aria-expanded로 열림/닫힘 상태 표현', required: true },
              { label: 'aria-live로 동적 콘텐츠 변경 알림', required: false },
              { label: 'role="alert"로 에러 메시지 전달', required: false },
            ],
          },
          {
            category: '색상 및 대비',
            color: '#10b981',
            items: [
              { label: '텍스트 대비율 4.5:1 이상 (WCAG AA)', required: true },
              { label: '아이콘 대비율 3:1 이상', required: true },
              { label: '색상만으로 상태를 구분하지 않음', required: true },
              { label: '포커스 링 시각적으로 명확하게 표시', required: true },
              { label: '고대비 모드(forced-colors) 지원', required: false },
            ],
          },
          {
            category: '폼 컴포넌트',
            color: '#f59e0b',
            items: [
              { label: 'label 과 input이 htmlFor/id로 연결', required: true },
              { label: '에러 메시지가 aria-describedby로 연결', required: true },
              { label: '필수 항목에 aria-required 표시', required: true },
              { label: '라디오 그룹에 fieldset + legend 사용', required: true },
              { label: '체크박스 인터미디에이트에 aria-checked="mixed"', required: false },
            ],
          },
        ].map((section, i) => (
          <div key={i} style={{ borderRadius: '14px', border: '1.5px solid #e2e8f0', overflow: 'hidden' }}>
            <div style={{
              padding: '12px 18px', background: '#f8fafc', borderBottom: '1px solid #e2e8f0',
              display: 'flex', alignItems: 'center', gap: 8,
            }}>
              <div style={{ width: 10, height: 10, borderRadius: '50%', background: section.color, flexShrink: 0 }} />
              <span style={{ fontSize: '13px', fontWeight: 700, color: '#475569' }}>{section.category}</span>
            </div>
            <div style={{ padding: '14px 18px', display: 'flex', flexDirection: 'column', gap: 8 }}>
              {section.items.map((item, j) => (
                <div key={j} style={{ display: 'flex', alignItems: 'flex-start', gap: 10 }}>
                  <div style={{
                    width: 16, height: 16, borderRadius: 4, flexShrink: 0, marginTop: 1,
                    background: item.required ? section.color : 'transparent',
                    border: `2px solid ${item.required ? section.color : '#d1d5db'}`,
                    display: 'flex', alignItems: 'center', justifyContent: 'center',
                  }}>
                    {item.required && (
                      <svg width="9" height="9" viewBox="0 0 12 12" fill="none">
                        <polyline points="2,6 5,9 10,3" stroke="#fff" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
                      </svg>
                    )}
                  </div>
                  <div>
                    <span style={{ fontSize: '12px', color: '#374151', lineHeight: '1.5' }}>{item.label}</span>
                    {!item.required && (
                      <span style={{ marginLeft: 6, fontSize: '10px', color: '#94a3b8', fontWeight: 600, background: '#f1f5f9', padding: '1px 5px', borderRadius: 4 }}>선택</span>
                    )}
                  </div>
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* Component Composition Patterns — Tailwind UI style */}
    <section style={{ marginBottom: '64px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>컴포넌트 조합 패턴</h2>
      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '24px' }}>실무에서 자주 사용되는 컴포넌트 조합과 권장 패턴을 안내합니다.</p>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
        {[
          {
            pattern: '검색 + 필터 헤더',
            components: ['SearchBar', 'Dropdown', 'Chip', 'SolidButton'],
            description: '목록 페이지 상단에서 검색과 필터를 함께 제공하는 패턴.',
            useCase: '상품 목록, 사용자 관리, 데이터 탐색 화면',
            color: '#6366f1',
          },
          {
            pattern: '폼 입력 묶음',
            components: ['TextField', 'RadioButton', 'Checkbox', 'Toggle', 'SolidButton'],
            description: '회원가입, 설정 저장 등 데이터를 입력받는 폼 레이아웃.',
            useCase: '회원가입, 프로필 편집, 환경 설정',
            color: '#3b82f6',
          },
          {
            pattern: '알림 + 피드백',
            components: ['Toast', 'AlertDialog', 'Modal', 'Progress', 'Loading'],
            description: '사용자 액션에 대한 즉각적인 피드백과 결과 표시 패턴.',
            useCase: '파일 업로드, 결제 처리, 비동기 작업',
            color: '#f59e0b',
          },
          {
            pattern: '내비게이션 쉘',
            components: ['AppBar', 'Breadcrumb', 'TabGroup', 'Drawer', 'Avatar'],
            description: '앱 전체 레이아웃을 구성하는 내비게이션 컴포넌트 조합.',
            useCase: '대시보드, 관리자 패널, SaaS 앱',
            color: '#10b981',
          },
          {
            pattern: '데이터 테이블 도구모음',
            components: ['DataTable', 'Checkbox', 'Dropdown', 'Popover', 'CounterBadge'],
            description: '행 선택, 필터, 액션 메뉴가 결합된 고급 데이터 테이블 패턴.',
            useCase: '사용자 관리, 주문 목록, 컨텐츠 관리',
            color: '#8b5cf6',
          },
          {
            pattern: '컨텐츠 카드',
            components: ['Avatar', 'LabelBadge', 'HoverCard', 'GhostButton', 'Skeleton'],
            description: '사용자/컨텐츠 정보를 카드 형태로 표시하는 패턴.',
            useCase: '팀 멤버 목록, 게시물 피드, 프로필 페이지',
            color: '#ec4899',
          },
        ].map((item, i) => (
          <div key={i} style={{ padding: '18px 20px', borderRadius: '12px', border: '1.5px solid #e2e8f0', background: '#fff', display: 'flex', gap: 20, alignItems: 'flex-start' }}>
            <div style={{ width: 4, borderRadius: 99, background: item.color, alignSelf: 'stretch', flexShrink: 0 }} />
            <div style={{ flex: 1 }}>
              <div style={{ fontSize: '14px', fontWeight: 700, color: '#1e293b', marginBottom: 6 }}>{item.pattern}</div>
              <div style={{ display: 'flex', flexWrap: 'wrap', gap: 5, marginBottom: 8 }}>
                {item.components.map((comp) => (
                  <code key={comp} style={{ fontSize: '11px', fontWeight: 600, color: item.color, background: `${item.color}12`, padding: '2px 8px', borderRadius: 5, fontFamily: 'monospace' }}>{comp}</code>
                ))}
              </div>
              <div style={{ fontSize: '12px', color: '#64748b', lineHeight: '1.6', marginBottom: 4 }}>{item.description}</div>
              <div style={{ fontSize: '11px', color: '#94a3b8' }}>사용 예시: {item.useCase}</div>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* API Consistency */}
    <section style={{ marginBottom: '64px' }}>
      <h2 style={{ fontSize: '22px', fontWeight: '700', color: '#1e293b', letterSpacing: '-0.02em', marginBottom: '8px' }}>API 일관성</h2>
      <p style={{ fontSize: '14px', color: '#64748b', marginBottom: '24px' }}>Ant Design에서 영감을 받아 모든 컴포넌트가 예측 가능한 인터페이스를 따릅니다.</p>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '14px' }}>
        {[
          { prop: 'color', values: '"primary" | "black" | "gray" | "white"', desc: '시각적 색상 변형. 모든 버튼/입력 컴포넌트에 동일하게 적용.' },
          { prop: 'size', values: '"small" | "medium" | "large"', desc: '컴포넌트 크기. 패딩, 폰트, 아이콘 크기가 연동하여 변경.' },
          { prop: 'disabled', values: 'boolean', desc: '비활성화 상태. HTML aria-disabled와 cursor: not-allowed가 자동 적용.' },
          { prop: 'theme', values: 'ComponentToken 부분 오버라이드', desc: 'CSS 변수 없이 TypeScript로 토큰을 직접 오버라이드.' },
          { prop: 'ref', values: 'ForwardedRef', desc: '모든 루트 엘리먼트에 forwardRef가 적용되어 DOM 접근 가능.' },
          { prop: 'as', values: '"button" | "a" | ...', desc: '렌더링 엘리먼트를 변경. 링크 버튼 구현 시 사용.' },
        ].map(({ prop, values, desc }, i) => (
          <div key={i} style={{ padding: '18px', borderRadius: '10px', border: '1.5px solid #e2e8f0', background: '#fff' }}>
            <code style={{ fontSize: '13px', fontWeight: 700, color: '#6366f1', background: '#f0f4ff', padding: '2px 8px', borderRadius: '5px' }}>{prop}</code>
            <div style={{ fontSize: '11px', color: '#94a3b8', marginTop: '8px', marginBottom: '6px', fontFamily: 'monospace' }}>{values}</div>
            <div style={{ fontSize: '12px', color: '#64748b', lineHeight: '1.5' }}>{desc}</div>
          </div>
        ))}
      </div>
    </section>

  </div>
</Unstyled>
