{
  "name": "@orbit-ui/eslint-plugin",
  "private": true,
  "version": "0.0.1",
  "description": "CSS 순서 자동 정렬 ESLint 플러그인",
  "author": "Orbit UI Team",
  "main": "lib/index.js",
  "scripts": {
    "test:rule": "mocha --reporter nyan --experimental-modules tests/**/*.spec.js"
  },
  "devDependencies": {
    "eslint": "^10.4.0",
    "mocha": "^11.7.6"
  },
  "peerDependencies": {
    "eslint": ">=7"
  }
}
