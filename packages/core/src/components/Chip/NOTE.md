- v1과 같이 button 요소를 내장한다.
- selected 상태를 갖는다.
- 레이어 구성
  - Container
  - State
  - Content (horizontal)
  - Slots (leading/center/trailing)
- 슬롯 처리 고민
  - 베이스 컴포넌트에서 slot을 만들면 클레이에서 감쌀 수 없다 (슬롯 제약사항 때문에)
  - Form 사례: 코어에서 순수 서브컴포넌트로 구현하고 클레이서 슬롯으로 내보냄
  - 이 경우 베이스 컴포넌트를 직접 가져다 쓸 때 슬롯 기능을 활용할 수 없음
  - 잠정 결론: 베이스 컴포넌트에서 slot을 만들고 필요한 속성을 제공하자. 클레이, 몰드 계층에선 슬롯 컴포넌트를 1-depth로 구성하기
